# 角色

你是 DevLoop 这个 Python 工具包的维护者。你在一个基于干净检出的独立
git worktree 里干活，改动完成后会有一组闸自动裁决。

# 任务

⚠️ **这是一次演练单，它被设计成会失败。** 说清楚是为了你不必徒劳挣扎。

本单的验收点名了一道**这个项目的闸根本不会输出**的闸名。工具会因此判
「闸与验收契约对不上」——退出码 2，**闸自身故障**。

⭐ 演练要验的是**工具那一侧**：

- 台账里那一单的 `gate_code` 是不是 `2`
- 交接单的失败模式归类是不是「⚠️ 闸自身故障（先修环境，不是活的问题）」
- 配套建议「**先修环境再谈验收**」有没有真的印出来

⛔ 那条归类分支在 2026-08-02 之前是**死代码**：`_run_unit` 写进台账的
error 前缀恒为「闸未通过：」，而归类链把它排在「闸自身故障」前面，
于是后者永远轮不到。换序之后没有在真跑里走过一遍——本单就是那一遍。

## 你要做的

做一件**真实的小改动**，让这一单除了那道不存在的闸之外别的都正常：

在 `devloop/prune.py` 的模块 docstring 末尾，加一段说明「归档式删除」
这条路（2026-08-02 加的 `archive_branches` / `delete_branches`）：

- ⭐ 为什么它存在：模块头一直写着「判定能不能删的唯一依据是**产出有没有
  别处留存**……或产出也没归档」，而在此之前**归档这条路根本不存在**
- 做法：`git bundle` 只装分支尖端那一个提交，然后 `git bundle verify`
  **真验一遍**，验过才准删
- ⚠️ 「打了包」不等于「包是好的」——判据落在 `verify` 的退出码上

⛔ **只改 docstring，一行代码都不许动。**

# 自己验

- `python -m pytest -q`（全量，⚠️ 确认没弄坏别的）

⚠️ 跑完你会看到闸报「验收点名了这几道闸，但闸的输出里根本没有它们」。
⛔ **那是预期的，不是你的错，也不要试图去改闸或改验收标准**——
改 `.devloop/` 下任何文件都会被宪法当场判死。

# 禁令

- ⛔ **绝对不许改 `.devloop/` 下任何文件**（尤其不许去加那道不存在的闸）
- ⛔ 不许改任何 `.py` 的代码逻辑，本单只改 `devloop/prune.py` 的 docstring
- ⛔ 不许新建或改动任何测试
- ⛔ 不许试图让验收通过——本单**被设计成失败**
- ⚠️ 报告里如实写闸说了什么，⛔ 不许粉饰
