# 角色

你是这个 Python 项目的维护者。⛔ 只做本单点名的事，别顺手改别的。

# 任务

**`devloop/nightly.py` 把错误信息砍到 100 个字，而被砍掉的恰恰是「你该干什么」那一句。**

## ⛔ 这个毛病昨天刚在别处犯过一次

2026-08-03 实测：`constitution.py` 有一处 `stderr.strip()[:200]`，
工具当时是停的，而它印给人的**唯一一条自救命令**被砍成：

```
python -m devloop.cli constitution anchor --pro
```

⚠️ 项目路径被吞掉，人照抄敲不通。

⭐ **两处都恰好砍在最后一句上，这不是巧合：「你该干什么」总在最后。**

`constitution.py` 那处已经修好了，⛔ `nightly.py` 这处**还没有**。
早报是「早上接管的第一条命令」——它印的东西被砍掉，代价和昨天一样。

## 要做的

1. 找出 `devloop/nightly.py` 里所有写死的字数截断。
2. 换成**不砍断行**的截断：截断点若落在某一行中间，就把那**整行**留全。
   ⭐ `devloop/constitution.py` 里的 `_keep_命令` 已经是这个规则的实现——
   ⚠️ **直接复用它**（import 过来），⛔ 不要再抄一份：
   两份实现会各自漂移，而这正是本仓反复吃亏的形状。
   （如果复用时觉得它该改个更通用的名字/位置，可以动，⚠️ 但要保证
   `tests/test_anchor_diff.py` 里已有的三条截断测试仍然全绿。）
3. ⚠️ 上限值该给多少由你判断，⛔ 但**理由要写在注释里**，
   不许只留一个光秃秃的数字。

# 改动范围

- `devloop/nightly.py`
- `tests/test_nightly_truncation.py`（**新建**）
- ⚠️ 允许动 `devloop/constitution.py` 里 `_keep_命令` 的**名字或位置**（若你要移到公共处），
  ⛔ 但不许改它的行为。

# 自己验

- [ ] `python -m pytest -q` 全绿（⭐ 特别确认 `tests/test_anchor_diff.py` 那三条截断测试没被弄坏）
- [ ] 新测试**至少三条**：
      ① 一条长错误信息进去，出来的最后一行**是完整的一行**，不是半句
      ② ⭐ 一条**带命令的**长消息，命令连同它的参数必须完整出现
      ③ 短消息**一个字都不许动**
- [ ] ⭐ **红检**：把截断退回成写死的 `[:100]`，确认①②当场转红。
      把红检结果写进你的报告。

# 禁令

- ⛔ **不许修改任何已有的测试文件。** 只许新建 `tests/test_nightly_truncation.py`。
  （⚠️ 例外：若你把 `_keep_命令` 改了名/挪了位置，`tests/test_anchor_diff.py`
  里对它的引用可以跟着改——⛔ **只许改那个名字，断言一个字都不许动**。）
- ⛔ 不许碰 `.devloop/` 下的任何文件。
- ⛔ 不许改 `pyproject.toml`。
- ⚠️ ⛔ **不许用「把上限从 100 改成 1000」来交差**——那只是把坑往后挪。
  判据必须落在**规则**上（行不许断），不在数字上。
- ⛔ 不许 `git push`，不许碰 `master`。
