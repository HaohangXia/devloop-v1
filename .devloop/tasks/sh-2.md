# 角色

你是 DevLoop 这个 Python 工具包的维护者。你在一个基于干净检出的独立
git worktree 里干活，改动完成后会有一组闸自动裁决。

# 任务

**BACKLOG G-90**：跨 `--resume` 的**配额放大**。
`fired`（这一轮已经派了几次）只活在进程内，⛔ 而进程一重启它就归零；
台账掉行时，一次 `--resume` **实测派了 17 次，而上限写的是 10**。

⚠️ 今晚这一整夜靠 `max_dispatches` 当唯一的硬刹车（订阅制下预算闸恒不触发，
因为它读 `cost_usd_real`，而订阅制那一栏恒等于 0）。
⇒ **这条刹车漏了，整夜就没有上界。**

**做一条能验它的判据，并按判据修。**

具体：
1. 新建 `tests/test_resume_dispatch_cap.py`，至少 3 条：
   - **正面**：造一份台账（已派 N 次），走 `--resume` 的那条进度重算路径，
     断言「还能派几次」= `max_dispatches − N`，⛔ 不是 `max_dispatches`。
   - **掉行**：台账**少了几行**时（那正是 G-90 的现场），
     断言它**宁可少派也不多派**（保守方向），并**说出来**自己在保守。
   - **绿检**：不带 `--resume` 的正常一轮，行为一个字都不许变。
2. ⭐ 判据落在**真跑一次进度重算**上，⛔ 不许只读源码。

⚠️ `--resume` 的 help 原文写着「进度仍从台账重算，不信自己的记录」——
⭐ 先去核实**代码是不是真的这么做的**。如果是，那 G-90 可能已经修了：
⛔ 那就在报告里写清楚「它已经修了，证据是 X」，并把测试留下当回归守卫。
⚠️ **别为了显得有产出而改一个没坏的东西。**

# 改动范围

- `tests/test_resume_dispatch_cap.py`（新建）
- 只有在测试真的红了之后，才动 `devloop/autopilot.py`

# 自己验

- `python -m pytest -q tests/test_resume_dispatch_cap.py`
- `python -m pytest -q`
- ⭐ **红检**：把进度重算改成「从 0 开始」，确认测试变红；还原，确认回绿。两次输出都贴。

# 禁令

- ⛔ 不许改 `tests/` 下**已有的**任何文件——只许新建 `tests/test_resume_dispatch_cap.py`
- ⛔ 不许新建 `tests/conftest.py`
- ⛔ 不许改 `.devloop/` 下任何文件
- ⛔ 不许 git commit / push
- ⛔ 不许真派单（不许调 `dispatch_one` 的真跑形态）
- ⛔ 判不了的事就说判不了，不许猜

# 报告格式

1. 改了什么（贴新增代码）
2. 实测记录（每条命令的真实输出，含红检）
3. **G-90 今天到底还成不成立** —— 结论 + 证据
4. 不确定的地方
