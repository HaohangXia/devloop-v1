# 角色

你是 DevLoop 这个 Python 工具包的维护者。你在一个基于干净检出的独立 git worktree
里干活，改动完成后会有一组闸自动裁决（语法检查 + pytest 全套）。

⚠️ 本仓库的硬纪律：**文档里的每条状态都要能被一条命令验证**。
⛔ 写「已实现」必须附可执行的证据（文件路径 + 符号名）。

# 任务

`SPEC.md` 有一张**交付物清单**（搜「交付物」找到它）。2026-08-01 新增了
**四个模块**，⛔ 清单里一个都没有：

| 模块 | 干什么 | 何时加的 |
|---|---|---|
| `devloop/records.py` | 不可恢复记录的指纹与归档（T5 宪法第四条判据） | 2026-08-01 |
| `devloop/progress.py` | 派单过程的实时心跳（stderr，含守护线程） | 2026-08-01 |
| `devloop/audit.py` | 审计发现的解析 / 落盘 / 转任务书 | 2026-08-01 |
| `devloop/fanout.py` | 多派单的判据（只读默认并行、写默认串行） | 2026-08-01 |

**要做的事**：把这四行补进 `SPEC.md` 的交付物清单，格式与现有行**保持一致**。

⛔ **动手之前先自己核实每个模块真的存在**（`ls devloop/`），
并从每个模块的**模块 docstring 第一行**提取它的用途——⛔ 不许照抄我上面写的，
⚠️ 那可能已经过期。以代码为准。

⭐ 顺带核一件事：清单里若有**已经不存在的模块**，标注出来但**不要删**
——写进报告末尾的发现块由人来定。

⛔ 只改 `SPEC.md` 一个文件，只动那张清单。

# 自己验

按顺序跑，把**真实输出原样粘贴**进报告：

1. `git -C . diff --stat`
   → 预期：**只有 `SPEC.md` 一个文件**

2. `python -c "import pathlib; t=pathlib.Path('SPEC.md').read_text(encoding='utf-8'); print([m for m in ('records.py','progress.py','audit.py','fanout.py') if m not in t] or '四个都在')"`
   → 预期：打印「四个都在」

3. `ls devloop/*.py | wc -l`
   → 记下这个数，⚠️ 用来和清单里的行数对照（对不上就在报告里说明差在哪）

# 禁令

- ⛔ 只许改 `SPEC.md`。
- ⛔ 不许改 `devloop/` 下任何 .py，不许改 `tests/`、`.devloop/`、`PLAN.md`。
- ⛔ 不许 `git commit` / `push` / `add`。
- ⛔ **不许删清单里任何现有的行**——发现有问题就写进发现块，由人来定。
- ⛔ 不许照抄任务书里那张表的措辞。**以模块 docstring 为准**，
  它和任务书冲突时**以代码为准**。

# 改动范围

- SPEC.md
