# Claude Code 通道故障 · 诊断手册

> 建档：2026-07-26 ｜ 最近增补：2026-07-30（故障 13–19） ｜ 适用范围：**跨项目**（这些是 Claude Code 自身的行为，与具体项目无关）
> 相关文档：[README.md](README.md)（项目门面） · [PLAN.md](PLAN.md)（总规划与决策记录） · [SPEC.md](SPEC.md)（接口契约）

---

## 怎么用这份文档

1. **拿错误信息原文来搜。** 下面每条故障的标题就是报错原文，一字不差。先 `Ctrl+F` 搜报错里最独特的那几个词。⚠️ 少数几条没有固定报错原文（故障 8／11／13–19），标题写的是**现象**——⚠️ 2026-07-30 新增的**七条（13–19）全部**属于这一类：它们最坏的形态就是**什么都不报**（故障 14 的无声丢行、故障 17 的静默关闸），拿报错去搜是搜不到的。
2. **务必读「❌ 误诊过的方向」那一栏。** 这份手册最值钱的部分不是答案，是**已经走过的弯路**——2026-07-26 那次排查里，前两版诊断都错了，花掉大半个会话。别再走一遍。
3. **能跑 `devloop doctor` 就先跑它**（Phase 0 产出）。它一次性检查所有通道，比逐条对症状快得多。

---

## 核心心法：桌面进程「内」与「外」是两套完全不同的路

这是所有故障的总根源。**同一份 `~/.claude/settings.json`，被两条路径以完全不同的方式解读：**

| | 桌面进程**内**<br>（主对话／子代理／WebFetch／WebSearch／权限分类器） | 桌面进程**外**<br>（Bash 工具或终端派生的 `claude` 子进程） |
|---|---|---|
| **认证** | 桌面 App 内存中**已刷新**的会话（有效） | 读磁盘上的 `.credentials.json`（**可能过期**） |
| **认证优先级** | — | **OAuth 凭据 > `ANTHROPIC_AUTH_TOKEN` 环境变量** ⚠️ |
| **模型名来源** | `settings.json` 的 env（`ANTHROPIC_MODEL` / `*_DEFAULT_*_MODEL` / `CLAUDE_CODE_SUBAGENT_MODEL`） | 同左 |
| **端点** | 主对话用桌面自身的；辅助通道行为不一致（见故障 1/2/3） | `ANTHROPIC_BASE_URL` 生效 |
| **典型故障** | 认证正常，**模型名错配** → 报「模型不存在」 | 模型名正确，**认证被过期 OAuth 劫持** → 报 401 |

**一句话记住**：桌面里死的是「模型名」，桌面外死的是「认证」。**症状不同，根因不同，别混为一谈**——这正是那次排查前两版诊断翻车的地方。

---

## 故障 1 · `... is temporarily unavailable, so auto mode cannot determine the safety of <工具名>`

**完整报错**
```
deepseek-v4-pro[1m] is temporarily unavailable, so auto mode cannot determine
the safety of Bash right now.
```

**表现**：Bash、Workflow 等需要安全判定的工具全部被拒；但 Read／Grep／Glob／Write 等只读或低风险工具照常可用。

**❌ 误诊过的方向**
- 「DeepSeek 服务挂了」——看起来极像，因为报错里明写 temporarily unavailable。**但当时用户的终端 CLI 一切正常**，这条反证被忽略了，导致误诊持续了好几轮。

**✅ 真因**：权限分类器（auto 模式下判断命令安不安全的那条独立通道）的模型由 env 指定。若该模型在分类器所用的端点上不可达，任何需要判定的工具全被挡。

**判别方法**：只读工具能用、Bash 不能用 → 是分类器通道，不是主对话。

**解法**
- 临时：等（这条通道会自行恢复，实测过）；或切非 auto 的权限模式。
- 根治：别把分类器的模型指向第三方（见 PLAN.md 的 Phase 0 配置分层）。

**验证于**：2026-07-26，Claude Code 2.1.145 桌面版

---

## 故障 2 · `There's an issue with the selected model (<模型名>). It may not exist or you may not have access to it.`

**表现**：子代理（Agent 工具）**全部**当场死亡；WebFetch 同样报错。主对话完全正常。

**❌ 误诊过的方向**
- 「跟故障 1 是同一回事，DeepSeek 又抖了」——**不是**。注意措辞差异：`temporarily unavailable`（临时不可用，说明模型存在）vs `may not exist`（压根不认识这个名字）。**这两个词的区别就是两种完全不同的根因**，看到时要停下来分辨。

**✅ 真因**：桌面进程内的辅助通道用的是**桌面自身的 Anthropic 端点**（认证有效），但模型名从 env 里取，取到的是第三方（如 DeepSeek）的模型名 → Anthropic 不认识这个名字。

涉及的 env 键：
- `CLAUDE_CODE_SUBAGENT_MODEL` → 打死子代理
- `ANTHROPIC_DEFAULT_HAIKU_MODEL` → 打死 WebFetch（它用小模型做摘要）
- `ANTHROPIC_DEFAULT_OPUS_MODEL` / `_SONNET_MODEL` → 打死按别名指定模型的调用

**⚠️ 关键陷阱**：在 Agent 调用里**显式传 `model` 参数也救不了**——别名（`opus`/`sonnet`/`haiku`）会先经 `ANTHROPIC_DEFAULT_*_MODEL` 解析，正好落回那个坏名字。**env 优先级高于调用参数。**

**判别方法**：主对话正常但子代理必死 + 报错说 may not exist → 就是它。

**解法**：把 `CLAUDE_CODE_SUBAGENT_MODEL` 与三个 `ANTHROPIC_DEFAULT_*_MODEL` 从全局 env 中移除（PLAN.md 的 Phase 0）。

**验证于**：2026-07-26，Claude Code 2.1.145 桌面版

---

## 🔀 上网的备用通道（WebSearch/WebFetch 坏掉时）

`WebSearch` 与 `WebFetch` 是 Anthropic API 的服务端工具，会被故障 2／3 波及。但**以下通道走 MCP，不经过那条链路，实测可用**：

| 通道 | 用途 | 实测 |
|---|---|---|
| **Exa MCP**（`web_search_exa` / `web_fetch_exa`） | 通用网页检索与抓取——**WebSearch 的直接替代品** | ✅ 2026-07-26 |
| **GitHub MCP** | 读仓库源码、搜项目 | ✅ 2026-07-26 |
| Claude Browser / Playwright MCP | 需要真实浏览器时 | 未测 |

→ **别因为 WebSearch 报错就断言"上不了网"。先试 Exa。**（这条教训本身来自一次错误断言。）

---

## 故障 3 · `output_config.effort '<值>' is not supported when thinking is disabled on this model`

**完整报错**
```
API Error: 400 output_config.effort 'max' is not supported when thinking is
disabled on this model. Use effort 'high' or below, or enable thinking.
```

**表现**：WebSearch 直接失败。

**✅ 真因**：`CLAUDE_CODE_EFFORT_LEVEL=max` 是为某个第三方模型设的，但请求实际发往 Anthropic，那边对该参数有不同的校验规则。

**这条报错的诊断价值极高**：它的格式是 **Anthropic API 的参数校验错误**，等于直接证明了「请求发到了 Anthropic 而不是第三方端点」。排查时这是判断请求去向最硬的证据之一。

**解法**：`CLAUDE_CODE_EFFORT_LEVEL` 不要设在全局 env 里。

**验证于**：2026-07-26

---

## 故障 4 · `401 OAuth access token has expired. Re-authenticate to continue.`

**表现**：从桌面的 Bash 工具派生 `claude -p` 时立即失败，`duration_api_ms: 0`（说明请求根本没发出去，是本地预检就挡了）。

**❌ 误诊过的方向**
- 「环境变量没传给子进程」——**错**。实测 `env | grep ANTHROPIC` 证明 `ANTHROPIC_BASE_URL` 和 `ANTHROPIC_AUTH_TOKEN` **都在**子进程环境里。变量传了，只是没被采用。

**✅ 真因**：**Claude Code 优先使用 `~/.claude/.credentials.json` 里的 OAuth 凭据，忽略 `ANTHROPIC_AUTH_TOKEN` 环境变量。** 那份凭据过期了。

桌面 App 主循环不受影响——它在内存里自行刷新；但派生的子进程只读磁盘上那份旧的。子进程环境里的 `CLAUDE_CODE_SDK_HAS_HOST_AUTH_REFRESH=1` 就是线索：它被告知「刷新归宿主管」，于是自己不刷新。

**解法**
- **派单场景（推荐）**：加 `--bare`。它跳过 OAuth 与 keychain 读取，环境变量里的 key 才会生效。
- **终端自用**：跑一次 `claude` 重新登录，刷新 `.credentials.json`。

**验证于**：2026-07-26

---

## 故障 5 · `401 Authentication Fails, Your api key: ****XXXX is invalid`

**表现**：已经把 `ANTHROPIC_BASE_URL` 指向第三方端点了，请求也确实到达了对方（报错格式是对方的），但对方说 key 无效。

**🔑 关键线索**：**核对报错里那个 key 尾号**。如果它和你配置里的 key 尾号**对不上**，说明送过去的根本不是你以为的那把 key——多半是 OAuth 令牌被当成 api key 发过去了（即故障 4 的变体：端点换了，凭据没换）。

**✅ 真因**：同故障 4——OAuth 凭据抢占了 `ANTHROPIC_AUTH_TOKEN`。只是这次 base URL 指向了第三方，所以请求打到对方那里才被拒。

**解法**：加 `--bare`。

**验证于**：2026-07-26

---

## 已实证可用的派单写法（付费 api 后端）

> ⚠️ 原标题写的是「唯一正解」，**2026-07-29 被订阅后端推翻**——不再唯一。两个后端的必需项**正好互斥**，⛔ 照抄另一边就一定坏：
>
> | | 付费 api 后端 | 订阅后端（`kind = "subscription"`，提交 `106a35d`） |
> |---|---|---|
> | `--bare` | **必须加**，否则过期 OAuth 会劫持环境变量里的 key（故障 4／5） | ⛔ **不许加**——它的用途正是跳过 OAuth，而订阅要的就是 OAuth |
> | `ANTHROPIC_*` | 按下面写法注入 | ⛔ **一个都不许注入**，注入了就打到第三方端点 |
> | 成本口径 | 按价目表算 | 美元成本**是 0，不是未知**——记 None 会让自动驾驶第一单就停机 |

```bash
ANTHROPIC_BASE_URL="<第三方端点>" \
ANTHROPIC_AUTH_TOKEN="<key>" \
ANTHROPIC_MODEL="<模型名>" \
  # ⚠️ 这是 2026-07-26 的历史记录。现行派单**一律** stream-json，且订阅后端
  #    ⛔ 绝不加 --bare —— 见故障 9 与 SPEC §5.1.1。别照抄这一行。
  claude --bare -p "<任务>" --output-format json
```

- `--bare` **不是性能优化，是必需项**——没它就会撞上故障 4／5。
- `ANTHROPIC_AUTH_TOKEN` 与 `ANTHROPIC_API_KEY` 两种写法均可（都实测过）。
- **代价**：`--bare` 同时跳过项目 `CLAUDE.md` 与所有 hooks 的加载 → **工人读不到项目规则**，必须把规则摘要显式拼进任务书。
- **自检**：回执的 `modelUsage` 字段会写明实际使用的模型——**永远核对它，别假定**。

---

## 故障 6 · `Reached maximum number of turns (N)` / 回执 `subtype=error_max_turns`

**表现**：工人跑满轮数被截断，**报告为 0 字符**。台账正确记 `worker_ok=False`。

**❌ 误判过的方向**
- 「看回执的 `num_turns` 就知道离上限还有多远」——**错，那是两个不同的计量对象**。
  实测：同一批单全部传 `--max-turns 30`，某单回执 `num_turns=71` 却 `subtype=success`，
  另一单 `num_turns=31` 就撞了上限。数会话记录才看得出真相：
  `num_turns` 数的是**工具结果回传条数**（user 角色消息），`--max-turns` 限的是
  **去重后的 assistant 轮数**。比值 1.0–2.8 倍，随每轮塞了几个并发工具调用而变。

**✅ 真因与处置**：撞上限通常是**拆单太大**，不是模型不行——这两类在成本实验里必须分开，
否则会把编排方的失误算到模型头上（台账的 `truncated` 字段就是为此而设）。
读多写少的单（要通读整份大文档的）天然无法批量工具调用，比值接近 1:1，最先饿死。

**明细见 BACKLOG G-35。**

---

## 故障 7 · `claude auth status` 说 `loggedIn: true`，同一时刻 `claude -p` 报 401

**表现**：自检说「认证正常」，真派单立刻 401。

**❌ 误诊过的方向**（两版判据，错在同一处）
- 第一版看 `.credentials.json` 的 **mtime** 猜过期 → 错（令牌在内存刷新不回写文件，G-15）。
- 第二版改成问 `claude auth status` → **还是错**。

**✅ 真因**：**`auth status` 只看凭据文件在不在，不看过没过期。**

实测（2026-07-29，同一时刻同一台机器，原文见 `devloop/credentials.py:5-10`）：
```
claude auth status    →  {"loggedIn": true, "subscriptionType": "max"}
claude -p "回答一个字"  →  401 OAuth access token has expired
```

**判别方法**：出现「自检说认证正常 + 派单 401」这种组合，先怀疑**判据本身**，别怀疑网络。

**解法**：⛔ 不许拿 `auth status` 当「能不能派单」的判据。要确定答案只有一条路——真起一次子进程（`credentials.probe()` / `devloop doctor --probe`）。

**验证于**：2026-07-29，提交 `6b321f9`

---

## 故障 8 · 凭据文件写着「10 小时前就过期了」，但子进程照跑不误

**表现**：`expiresAt` 显示早已过期。据此拒派 → 拦下的是一批**本来能跑通**的活。

**❌ 误诊过的方向**：第三版判据「读文件里的 `expiresAt`，过期就拒绝派单」——**还是错**。
三版判据（mtime → `auth status` → `expiresAt`）全错，且都是同一个病：**拿一个间接读数替代真实行为**。换字段解决不了问题。

**✅ 真因**：**CLI 会用 `refreshToken` 自动续期，全程不需要人。**

实测（2026-07-29 18:46）：
```
测试前 expiresAt   08:22（10 小时前就已过期）
claude -p "回答一个字：好"   →  is_error false，正常返回
测试后 expiresAt   次日 02:46
```

⛔ 再换个字段读也没用——凭据文件里**根本没有 refreshToken 的过期时间**（字段只有
`accessToken` / `refreshToken` / `expiresAt` / `scopes` / `subscriptionType` / `rateLimitTier`），
续期成不成功，文件里判不出来。

⚠️ 这一版守卫**比不检查更坏**：它以「凭据过期」的名义停机，而真实原因是判据错了。

⭐ **对无人值守的意义（今天最重要的一条）**：一次登录只管 8 小时，但每次派单都是新子进程、
都会按需自动续期，所以 **8 小时这条线不再是跑一夜的障碍**。
⚠️ 前提是 refreshToken 本身没失效——两个月不用是会失效的（第一次栽的那回就是）。

**解法：模块定位改掉**（`devloop/credentials.py:36-39`）
- `check()` = **便宜的排除法**，只回答「肯定跑不了」（没文件 / 文件坏 / 过期**且连 refreshToken 都没有**）。⛔ 它**不**回答「一定能跑」。
- `probe()` = 真起一次子进程试。唯一可靠，但花额度 → 只给 `doctor --probe` 用。
- ⭐ 两者**不一致时必须单列报出来**（`devloop/doctor.py:87-92`）：分歧一旦被压下去，下次就得靠一整批 401 才能发现。

**验证于**：2026-07-29，提交 `6b321f9`

---

## 故障 9 · `When using --print, --output-format=stream-json requires --verbose`

**表现**：退出码 1、**零输出**。

**✅ 真因**：跟 stream-json 配套的 `--verbose` **是必需项，不是调试开关**。

**为什么非用 stream-json 不可**：只有它才吐 `rate_limit_event`——
`{"status":"allowed","resetsAt":1785332400,"rateLimitType":"five_hour"}`，
`resetsAt` 是精确的 **unix 秒**，是「撞额度当场停批」的判据来源。
`--output-format json` 那版**拿不到**这些。见 `devloop/dispatch.py:88-96`。

⚠️ 别和凭据那边的 `expiresAt` 混——那个是**毫秒**。

**解法**：`--output-format stream-json --verbose` 必须一起给。

**验证于**：2026-07-29，提交 `a8d6932`

---

## 故障 10 · `Warning: no stdin data received in 3s, proceeding without it`

**表现**：每单白等 3 秒。

**✅ 真因**：子进程继承了编排方的 stdin。

⛔ **真正致命的不是那 3 秒**：在**没有终端**的场合（计划任务、后台派单）继承一个
**永不结束**的 stdin 会直接把整批挂死——而无人值守恰恰全在这类场合。

**解法**：显式给空 stdin，`stdin=subprocess.DEVNULL`（`devloop/dispatch.py:130-135`）。

**验证于**：2026-07-29，提交 `a8d6932`

---

## 故障 11 · 主仓库的 git 身份被改成了工人身份

**表现**：在主仓库查 `git config user.name`，得到 `devloop-worker[sub-smoke]`、
`user.email` 得到 `worker@devloop.local`。

**✅ 真因**：**linked worktree 与主仓库共用 `.git/config`**（`extensions.worktreeConfig` 默认没开）。
所以在 worktree 里跑 `git config user.name` 是在**改主仓库的身份，而且改完不还原**。

**代价**：此后**维护者亲手写的 24 个提交**全挂上了工人身份——
这个机制存在的**唯一理由**（一眼分清人和机器）被它自己毁掉。

⚠️ **连带损坏**：`prune` 原本用 `git log --author=worker@devloop.local` 判分支空不空。
主线尖端带上这个邮箱之后，今后从主线开的**每一个**隔离分支都会被判「有未合并产出，别删」
→ 判据**永久失效**。已改成判**分支尖端那一个提交**：主题前缀 `work(` + 正文里的分支名。

**解法**：`git -c user.email=... -c user.name=... commit` —— 只作用于这一条命令，
不落盘、不污染，也不依赖 `worktreeConfig`（`devloop/worktree.py:117-131`）。

⚠️ **已污染的提交历史不改**（重写历史的代价远大于收益）→ 直接后果是
**任何依赖作者身份的判据都要换掉**，别再往上加新的。

**验证于**：2026-07-29，提交 `8d9f6f4`

---

## 故障 12 · `AttributeError: 'int' object has no attribute 'refs'`

**表现**：自动驾驶**首次真跑当场炸**。
⚠️ 两单的产出都是好的、五道闸全绿、分支也固化了——**纯粹是编排方自己摔的**，
摔在刚接上的那段线上。

**✅ 真因**：**变量名撞车**。宪法快照命名成 `before` 放在 while 循环**外面**，
而循环体里**早就有**一句 `before = len(prog.done_ok)`（看门狗用的完成计数）。
第一轮就把快照覆盖成 int，于是把整数传进了 T5。

**⚠️ 为什么演练和单测都抓不到**
- `--dry-run` 根本走不到 T5。
- 已有单测**直接调 `_run_unit`**，绕过了发生覆盖的那个作用域。
- **只有真跑会炸**——而它真的炸了。

⭐ 看门狗随后正确判定「连续 2 轮没有新的绿」并停机（K=2），退出码 3。
**这是四条失控防线里第一条真正触发过的。**

**解法**：改名 `snap_before`（`devloop/cli.py:670`）。新增 AST 判据的测试：
比对「循环外的快照变量」与「循环内被重新赋值的变量」有没有交集。
已做红检——改回撞车写法它报 `assert not {'before'}`。

**验证于**：2026-07-29，提交 `023726e`

---

## 🔑 排 13／15／17／18 之前先看懂这三层（模式触发机制）

故障 13／15／17／18 都发生在这条链上，不知道分层就会把症状归错层：

```
你打的字面量 ──正则──▶ 令牌文件 ──PreToolUse──▶ 命令放行 / deny
```

| 层 | 在哪 | 职责 |
|---|---|---|
| **识别层** | `UserPromptSubmit` 钩子（`tools/devloop-mode-hook.py`） | 在**模型看到消息之前**跑，**纯正则**，落盘令牌，用 `systemMessage` 回显。⛔ 回显由**脚本**打，不由 Claude 写——Claude 会判错，也就会写错 |
| **强制层** | `PreToolUse(Bash)` 钩子（`tools/devloop-spend-gate.py`） | 返回 `permissionDecision: "deny"`。**没令牌，花额度的命令物理跑不起来** |
| `CLAUDE.md` | 提示词 | ⛔ **只解释，绝不承担闸**——它是提示，会被注意力衰减和压缩稀释 |

⭐ **判断全部离开模型**，这是设计上的硬要求：用户明确否掉了「你用自然语言说、我来判断」这一版
（原话：「不行 我觉得这个判断方法也不好，我觉得需要一些特殊的字段来进行触发」）。

**分层定位法**：
- 打了代号但**没有回显** → 卡在**识别层之前**（消息压根没到钩子，见故障 13）
- 有回显，但该拦的命令跑起来了 → **强制层**出问题（见故障 17／18）

---

## 故障 13 · 触发代号被当成 shell 命令执行了（**两种形态**）

> ⛔ **2026-08-12 复发一次，形态不同、失效相同。** 两种都记在这里，⛔ 别只修一种。
>
> | | 第一种（2026-07-30） | 第二种（2026-08-12） |
> |---|---|---|
> | 打的是什么 | `!task 写个测试` | ```` ```bash ```` 块里的 `%task C:/pg/eco-ob 组4` |
> | 谁吃掉的 | CLI 的**输入分类层**（`!` 是内置 shell 模式前缀） | **代码块的「运行」按钮** —— 一点就送进 PowerShell |
> | 屏幕上看到 | `command not found: task ...` | `%task : The term '%task' is not recognized...` |
> | 实际发生了什么 | **钩子从没收到那条消息**，钱没花、活没派 | **一模一样** |
>
> ⭐ **第二种的责任在模型自己**：把 `%…` 写进 ```` ```bash ```` 块，
> 等于**替用户按下了错误的那个键**——那种块在这个 App 里带「运行」按钮。
> ⛔ **纪律：永远不许把触发代号写进 ```bash 代码块。** 要给用户看代号，
> 用行内反引号或纯文本，并明说「**打进对话框，不是终端**」。
>
> ⭐ **守卫（2026-08-12 加）**：`tools/devloop-spend-gate.py::_TRIGGER_IN_SHELL` ——
> 一条 Bash 命令**行首**出现 `%task`/`%auto`/`%go`/`%stop`/`%mode`（半角与全角都认）
> 就 deny，并把「这是聊天消息不是 shell 命令」当场说清楚。
> ⚠️ 判据落在**行首**：`echo '100%task done'` 这类不许误伤（自测里有绿检）。
> 红检做过：摘掉那一行 → `tools/test_mode_gate.py` 4 项不符预期。

### 第一种 · `!` 前缀被输入分类层截走

**表现**：在输入框打 `!task 写个测试`，Claude Code 去执行 shell 命令 `task 写个测试`
（多半是 command not found）。**模型根本没收到这条消息，`UserPromptSubmit` 钩子也看不见**。

**✅ 真因**：`!` 是 CLI 的**内置 shell 模式前缀**。CLI 二进制里就一行：

```js
function vG(H){if(H.startsWith("!"))return"bash";return"prompt"}
```

界面上还印着 `! for shell mode`。所以它在**输入分类那一层**就被截走了，压根到不了钩子。

**⚠️ 最阴的地方：半角失效、全角生效。** `startsWith("!")` 只匹配半角，所以全角 `！task`
**反而能用**。同一个字符两种宽度，**肉眼看不出来**——⛔ 因此也不能拿全角当主字面量
（用户随手切一下输入法，行为就翻转）。

**⛔ 自测当时为什么全绿**：它把 `{"prompt":"!task ..."}` 直接灌进钩子 stdin，
**绕过了 CLI 的输入分类**。这是下面「六种假绿」的第四种——**测了错的那一层**。
而上一个提交的正文列了 `@` `#` `++` `--` `跑::` 五种冲突，**唯独没查 `!` 自己**。

**判别方法**：识别层是**脚本用 `systemMessage` 回显**的（⛔ 回显不由 Claude 写——
Claude 会判错，也就会写错）。所以「打了代号但**没有回显**」= 消息压根没走到钩子，
而不是钩子判错了。

**解法：前缀改成 `%`**（现行字面量：`%` + 英文代号，对应 A／B／C **三档**模式）：

| 你打的 | 模式 | 令牌 |
|---|---|---|
| （什么都不打） | **A 直接干**（缺省） | — |
| `%task <要干的事>` | **B 派一单** | 要 |
| `%auto <项目> <要干的事>` | **C 唤起**（⛔ 不放行） | 要 |
| `%go` | **C 放行** | — |
| `%stop` | 撤销全部令牌 | — |
| `%mode` | 只看当前状态 | — |

`/devloop` 与 `%auto` 等价，且加了 `disable-model-invocation`（⛔ Claude 自己叫不动）。

**⛔ 前缀选型的实测依据**（每条都从 CLI 二进制核实过，不是猜的）：

| 候选 | 为什么废掉 |
|---|---|
| `!` | 内置 shell 模式前缀（见上）；全角 `！` 反而能用，**这个区别肉眼看不出来** |
| `/` | 斜杠命令 |
| `#` | 写进 `CLAUDE.md` 的快捷键 |
| `@` | **任意位置**弹文件补全 |
| `++` / `--` | 被 `C++` 和 `--dry-run` 打中 |
| `//` / `..` | 被粘贴的代码注释打中 |
| `。。` / `。。。` | 与中文省略号难区分 |
| `跑::` | 中文标点模式下 `:` 被改写成全角，**根本打不出来** |
| ⭐ `%` | CLI 里 `startsWith` **零命中**，且中文输入法**不改写**它（逗号句号会被改成全角，百分号不会） |

实现见 `tools/devloop-mode-hook.py:83-98`（选型依据就写在那段注释里）。

**验证于**：2026-07-30，提交 `1659c49`

---

## 故障 14 · 台账读不出来，或者统计数字比实际偏小

⚠️ **这一条有两种症状，第二种严重得多。**

| 症状 | 你看到什么 |
|---|---|
| **坏行** | 读台账的命令整个失败。修前是一句裸列表推导 `[json.loads(ln) for ln in ...]`，一行坏行直接抛 `JSONDecodeError`，被上层 `except ValueError` 显示成「输入错误：Unterminated string」——**不提文件名、不说是台账** |
| ⛔ **无声丢行** | **没有任何报错**，只是防线读到的数偏小 |

**✅ 真因**：台账追加**没加锁**。`--parallel` 是**同进程多线程**；`--detach` 的后台作业与
前台命令是**两个进程**，它们写同一个台账文件。

**判别方法**：⛔ **无声丢行没有任何自发信号，只能主动去撞**——
跑 `python tools/probe_ledger_concurrency.py`（复现脚本，写完自己数行数与坏行数）。
⚠️ 只跑一次通过**不算数**（见下面被推翻的那条结论）。

**❌ 被推翻的结论（这一条比修法本身更值钱）**：BACKLOG **G-41** 记的是
「16 线程并发写 → 16 行齐全、0 行无法解析 ✅ 未损坏」。
2026-07-30 复跑（`tools/probe_ledger_concurrency.py`）才发现，那是**一次**试验，
而且用的是**等长短行**。换成真实形态（8 线程 × 80 行、**不等长**——带 error 文本的行
比没有的长好几倍）：

```
40 次试验  →  40 次都丢行且出坏行
其中一次写出非法 UTF-8 字节，连 read_text 都抛 UnicodeDecodeError
```

⚠️ **一次通过的并发试验不构成性质。** G-41 那次是**侥幸**，不是证明——
⛔ 而它当时是按「已实测 ✅ 未损坏」记进 BACKLOG 的。
判据要问两件事：**试验数据的形状覆盖到真实形状了吗**（等长短行 vs 不等长长行）、
**它跑了几次**（1 次 vs 40 次）。

**⛔ 后果不是「少了几行日志」**：自动驾驶的每一条防线（花了多少 / 派了几次 / 哪些绿了）
**全都只从台账读**。
- 丢行 → 防线读到偏小的数 → **预算永不到顶、已绿的单被重派（重复花钱）**
- 坏行 → 整个台账读不出来 → **防线全部归零**

**解法**
- **写侧** `_append()`（`devloop/telemetry.py:106-136`）：进程内 `threading.Lock`（盖 `--parallel`）
  ＋文件锁（盖 `--detach` 与前台并存的**跨进程**情形）＋ `fsync`。
  ⛔ **拿不到文件锁也照写**——丢一行日志远好过丢一单活。
- **读侧** `load()`（`devloop/telemetry.py:140-176`）：逐行 `try`，⛔ **坏行必须抛出来**
  （`LedgerCorrupted`，报文件名与行号），**不许静默跳过**——静默跳过等于让「已花多少」偷偷变小，
  那正好把症状从「坏行」降级成更坏的「无声丢行」。
  ⚠️ 并用 `errors="replace"` 读，免得非法字节让读操作直接炸。

**修后复跑**：三组配置各 40 次，**零丢行零坏行**。

⚠️ **「台账数字偏小」还有另外两个成因**，别只查并发：`doctor` 压根没记（故障 15）、
花钱之后的处理先炸了（故障 19）。

**验证于**：2026-07-30，提交 `0e2518e`

---

## 故障 15 · 只跑了体检，额度却莫名少了一截（台账里查无此单）

**表现**：这一轮只跑过 `devloop doctor --project ...` 或 `doctor --probe`，一单都没派，
接着夜跑却很快撞额度；回头查台账，**一行相关记录都没有**。

**✅ 真因**：**`doctor` 真派单，真花订阅额度。**
- `_dispatch_smoke` 真起一次子进程走**默认后端**（= 订阅额度）
- `credentials.probe()` 的定位本来就是「真起一次子进程试」，它自己记过
  「一次最小调用产生**七万级 cache_creation token**」

而闸的注释表和 `/devloop` 命令文档**双双**把它列在「不花钱」里，**且一行台账都不留**。

**⚠️ 为什么这条要紧**：它是「夜跑标准流程**第 1 步**」。于是每次体检都在悄悄吃掉
当前那个 5 小时窗口，而三条失控防线（只从台账读）**看不见它**
→ **夜跑会在一个已经被吃掉的窗口上起跑。**

**判别方法**：**裸 `doctor` 不花钱；带 `--project` 或 `--probe` 就花。** 这两个参数是分界线。

**解法**
- 闸把带 `--project`/`--probe` 的 `doctor` 纳入**需令牌**名单
  （`tools/devloop-spend-gate.py:51-57`），裸 `doctor` 仍放行。
- `_record_smoke()` 把这一单记进台账（`devloop/doctor.py:140-162`）。
  ⚠️ **记账整个包在 `try` 里**——记账失败**绝不能**让体检本身报错：
  体检的用途是告诉你哪里坏了，它自己因为记账失败而红是帮倒忙。

**验证于**：2026-07-30，提交 `0e2518e`

---

## 故障 16 · 接力阶段 `--resume` 之后，下游工人说「看不见上一单的产出」

**表现**：`[stage] chain = true` 的阶段中途被打断，`--resume` 接着跑。下游单要么**必然失败**，
要么工人「自己重写一个顶上」——**产出静默分叉**（⚠️ 后者不报错，最难发现）。

**✅ 真因**：**接力点只活在进程内存里。** `base` 每次进程启动都重算，接力只在循环体内推进，
而 `Run.save` **不存它**。于是重启后下游单从**项目 HEAD** 建工作副本，
而前几单的产出只在**未合并的隔离分支**上、树里根本没有。

⚠️ **而崩溃恢复正是无人值守的立项理由**——这条 bug 精准地打在项目的立项前提上。

**判别方法**：**去查下游单那条分支的父提交**——接力正常时，它应当**就是上一单的提交**。
真跑验证过（stage nightly，2/2 绿）：`chain-b` 的父提交就是 `chain-a`；
diff 只有 **+30 行**（**追加不是重写**）；`report()` 真调用了上一单的 `branch_lines()`。
⛔ 若父提交是**项目 HEAD**，就是这条 bug（或者 `chain` 压根没开）。
另可直接看阶段状态文件里有没有 `chain_head`。

**解法**：`Run.chain_head` 落盘（`devloop/autopilot.py:175-200`）＋ `resume_chain_head()` 读回
（`devloop/autopilot.py:219-225`）；**推进接力点时立刻 save**（进程随时可能被杀）。

**⚠️ 顺带记清 chain 本身的两条边界**（提交 `64042ed`，G-60）：
- 接力点推到的是**编排方在闸全绿之后写的那条提交**，⛔ **不是**「拿 worktree 的 HEAD 当锚」。
- ⛔ **代价**：几单绑在一起，合最后一个分支就全拿到，**不能只否掉前一单**。
- `chain` ⛔ **默认关**。它存在的理由：不开的话每单都从**同一个 base** 建，`needs` 只保证先后顺序，
  后一单**看不见**前一单的产出 → 「写模块 → 给它写测试 → 修问题」这种自然计划跑不了，
  能跑的只剩「一批互不相干的小改动」——**而那恰恰是最不需要自动驾驶的活**。

**验证于**：2026-07-30，提交 `0e2518e`（chain 本身：`64042ed`，stage nightly 2/2 绿）

---

## 故障 17 · 闸突然不拦了：没有令牌，花额度的命令却照跑

**表现**：该 `deny` 的命令直接跑起来了，**屏幕上什么都不报**。

**✅ 真因一 · 钩子 fail-open（这是一条有意的权衡，不是 bug）**

两个钩子都是故意 `except: return 0`——**自己坏了不该挡住用户干活**。
**代价**：闸里**一个笔误**就会把整道闸**静默关掉**。
2026-07-30 真发生了：引用了一个还没定义的正则，**15 条断言从 DENY 变成放行**，屏幕上什么都不报。

**✅ 真因二 · 打错代号之后，上一轮的令牌继续活 10 分钟**

撤令牌的条件写成了「**压根没打代号**」（`elif not code`）。于是 `%tsak`、`%task`（空内容）、
`%go`（没 arm 过）这几种情况 `code` 都**非空**，B 令牌不撤，继续活满 `B_TTL = 10 * 60`
（`tools/devloop-mode-hook.py:79`）。

⚠️ **最难受的是 `%auto`**：注入给模型的上下文说「现在只许演练」，而**物理闸在放行真跑**——
**分叉的时候，闸是松的那一边。**

⛔ 自测当时全绿，是因为它在验「认不出的代号」之前**先 `rmtree` 了状态目录**，
**正好跳过泄漏路径**。改法：只要**落回模式 A** 就撤令牌（`%mode` 除外——查状态不该有副作用）。

**判别方法（本条重点）**：跑

```bash
python tools/test_mode_gate.py
```

⭐ 它把 **stderr 一并看住**——**有 `Traceback` 就当失败**
（`tools/test_mode_gate.py:55-70`，命中文案：「闸自己崩了（fail-open，等于没有闸）」）。
⚠️ 这是**唯一**能把 fail-open 照出来的信号：闸崩了的时候，闸自己是不会说话的。
**46 项全过**才算闸活着。

**验证于**：2026-07-30，提交 `1659c49`

---

## 故障 18 · 演练与真跑写在同一条命令里，整条被放行

**表现**：`autopilot --dry-run && autopilot` —— 后半截是**真跑**，闸放行了。

**✅ 真因**：判据落在**整条 Bash 命令**上（第一版 `if _DRYRUN.search(cmd): return 0`）。
于是这些全部被放行：

```bash
autopilot --dry-run && autopilot          # ← 后半截是真跑
autopilot  # 先 --dry-run 看看             # ← 注释里的字也算
echo --dry-run; dispatch ...
```

**⚠️ 复合是常态，不是边角**：实测记录里提到这两个子命令的 **206 条**里，
**204 条是复合命令**。所以这一条不是小漏洞——**它直接废掉了整道闸的全部卖点**。

**判别方法**：把整条命令按 `&&` `||` `;` `|` 换行**拆开**，逐段问
「**这一段自己**带 `--dry-run` 吗」。⛔ 一段带了**不给另一段授权**；
⛔ 注释里的 `--dry-run` **不算**。

**解法**：改成**按段判**（`tools/devloop-spend-gate.py:117-132`）——
`&&` `||` `;` `|` 换行 切段，**先剥 shell 注释**，
只有命中 `dispatch`/`autopilot` 的**那一段自己**带 `--dry-run` 才放行。
- ⛔ **先剥注释**：`autopilot  # 先 --dry-run 看看` 整段含 `--dry-run` 会被当演练放行
  ——**注释里的字不该有授权效力**。
- ⚠️ 分段是**启发式**（引号里的分隔符会被误切），但方向**偏严**：
  多切几段只会让更多段需要令牌，⛔ **不会放过真跑**。

**验证于**：2026-07-30，提交 `1659c49`

---

## 故障 19 · `✗ <单名>: FileExistsError` —— 钱花了，台账零行

**表现**：屏幕上**只有一行**异常，台账里**一个字都没提这一单花过钱**。

**✅ 真因**：`dispatch_one` 在 `subprocess.run` 返回**之后**（= **钱已经花掉之后**）
还有一串会抛异常的操作（`mkdir` / 解析 / 落盘）。它把异常抛出去，
上层判「钱花没花」的依据（`if res is not None`）就**永远读不到**。

**实测复现**：把 `.devloop/reports` 造成一个文件（`mkdir` 必炸）→
输出只有一行 `✗ t: FileExistsError`，**台账 0 行**。

⚠️ 这条与故障 14／15 是**同一个症状家族**（台账的数比实际小），但成因各不相同，
排查时要分开看：
- 故障 14 = 写进去了但**并发写丢了／写坏了**
- 故障 15 = `doctor` **压根没打算记**
- 故障 19 = 记之前**先炸了**

**解法**：花钱之后的整段拆进 `_finish()` 并**包 `try`**，出错也**返回**一个带
「⚠️ 钱已花掉但回执处理失败」的结果，让上层补记得到（`devloop/dispatch.py:144-158`）。

⚠️ 顺带：例外路径补记台账时**新造** `DispatchResult` 会丢掉 `rate_limit`，
已改用 `dc_replace`——⛔ **同一个坑主路径上已经踩过一次**。

**验证于**：2026-07-30，提交 `0e2518e`

---

## ⚠️ 闸「看起来绿」但其实没验的六种形态（已实测六种，全部修复）

比「闸红了」危险得多的是「闸绿了但什么都没验」——它给出的是**虚假的安全感**。已撞到的六种：

| 形态 | 具体案例 | 为什么难发现 |
|---|---|---|
| **守卫的目标不存在** | `.devloop/` 未被 git 跟踪 → 干净检出里没有它 → `git status -- .devloop/` 恒为空 → 恒 PASS | 输出「PASS 未被改动」，读起来完全正常 |
| **实现了但没接进调用路径** | 指纹校验写好了，但派单流程调用时没传参数 | 代码存在、测试也能测到，只是**真实路径上没走** |
| **判据选错维度** | 守卫写「它该不该存在」而非「工人有没有改它」→ 在两类项目上分别恒真、恒假 | 在其中一类项目上看起来完全正确 |
| **测错了代码 / 测了错的那一层** | editable 安装使包指向原目录，工人改坏 worktree 而闸测原目录 | **最危险**——它给出的是「看起来正确的绿」 |
| **验收正确但产出没保存** | 闸全绿、答案卷全过、台账完整——然后产出随 worktree 一起被删掉（G-26，L3 产出真实丢失） | 所有质量信号都只回答「产出对不对」，**没有一个信号回答「产出还在不在」**。丢失发生在全部绿灯之后 |
| **⭐ 自检存在、判据正确、但只覆盖了一部分输入** | token 去重函数写了自检「结果须与回执逐字段相等」，**在工人会话上通过了**；但同一函数还用在子代理会话上，那种记录是流式多帧的，从未被自检覆盖 → 输出 token 少算 9–30 倍，**错的数字已经对外报出去了**（G-42） | **最隐蔽的一种**：你手里真的有一份通过的自检报告。前五种是「没验」，这一种是「验了，但没验全」。→ **自检的覆盖面本身也要被检查** |

> ⭐ **2026-07-26 新增第六条纪律的第七次复发**：判断「后台任务是不是没跑起来」时，
> 我用了两个**间接信号**——输出文件 0 字节、`ps aux | grep` 没匹配到——就断定失败，于是重派。
> 实际上三次全跑了（任务需 300 秒，我只等了 180 秒；`ps` 的正则没匹配上 Windows 下的实际进程名）。
> **直接证据当时就在旁边：回执目录与台账。** 代价：多花两单。
> 修法：判断派单状态一律读 `.devloop/telemetry.jsonl` 与 `reports/`，不看输出文件大小、不猜进程名。

> ⚠️ **2026-07-29 与 07-30 的两轮审查里，这六种形态又复发了五例，而且全落在「守卫」自己身上。**
> ⛔ 这两天**没有**出现新形态（⚠️ 后来 2026-08-02 加了第七种，那是**上一层**的：
> 测试自己假绿，不是闸假绿）——⛔ 别把这五例当新形态记，
> 「**同一批形态一直在复发**」才是要记的事：
>
> | 复发（提交） | 落在哪一种形态 |
> |---|---|
> | ⛔ **最严重**：`cmd_autopilot` 调 `_run_unit` 没传 `before`/`ws_before`/`halt` → 宪法 T5 三道与撞额度停批**在自动驾驶路径上全是死的**（`8d9f6f4`）。⚠️ **而自动驾驶正是无人值守那条路**——人不看着的模式反而一道防线都没有 | **实现了但没接进调用路径** |
> | `[stage.accept] require_pass` **一行代码都没读过**——G-53 空守卫的复刻，而且就写在**引用 G-53 的那个文件里**（`8d9f6f4`） | **实现了但没接进调用路径** |
> | `ws_before = None` 覆盖了 T0 快照。两条守卫它的测试都绿：一条**自己**传 `ws_before=`，另一条只用 AST 查**关键字名在不在**——**验「传没传」，不验「传的是不是真快照」**（`1659c49`） | **判据选错维度** |
> | `!` 前缀的自测把 `{"prompt":"!task ..."}` 直接灌进钩子 stdin，**绕过了 CLI 的输入分类**（`1659c49`，故障 13） | **测了错的那一层** |
> | 「打错代号要撤令牌」的自测，在验之前**先 `rmtree` 了状态目录**，正好跳过泄漏路径（`1659c49`，故障 17） | **测了错的那一层** |
>
> ⭐ 第二条尤其值得停一下：**知道这个坑、并且在同一个文件里引用了这个坑的编号，也不足以避开它。**
> 修法是 `plan.effective_require_pass`（阶段级 ∪ 任务级，⚠️ **只读单不并**）。
>
> ⚠️ 同一轮还抓到一条**守卫有盲区比没守卫更坏**的：`--detach` 静默吞掉 `--wait-for-reset`，
> 而那条不变量测试的**参数化列表里没有这个新参数**，于是它**对这个参数恒绿**（`8d9f6f4`）。

> ⚠️ **2026-08-02 第三轮：这六种里又复发三例。**
> ⭐ 同一天还**首次**出现了一个上一层的形态（测试自己假绿），单列为第七种，见下节。
> ⛔ 这一次的复发全部发生在**修复者自己身上**（我），而且是在**知道这六条**的前提下：
>
> | 复发 | 落在哪一种 |
> |---|---|
> | VOID 判决加进闸之后，CLI 的打印表没跟着加 → `KeyError` **崩在 `commit_result()` 之前**，工人干完的活直接丢掉 | 实现了但没接进调用路径 |
> | `--why-parallel` 的提前返回写了、注释还专门解释了为什么必须提前，⛔ 而 argparse 的 `required=True` 在它之前就拒了 | 实现了但没接进调用路径 |
> | `templates/gates.sh` 第 130 行**逐字**就是 G-68 立项时那条假绿——⚠️ 而它是每个新项目的抄写源 | 守卫的目标不存在（复制传播版） |

---

## ⭐ 第七种：**测试自己是假绿**（2026-08-02 新增）

⛔ 上面六种讲的都是「**闸**看起来绿」。这一轮撞到的是上一层：

> **「修完了 + 测试全绿」离「修对了」还差一轮有人来专门挑刺。**

当天写的 11 条新测试，**在写出来的同一天**被独立复核证明有三种绕法能全绿通过、
而缺陷 100% 原样复活。三种形态：

| 形态 | 当天的实例 | ⭐ 怎么防 |
|---|---|---|
| **判据判的是「提到了」不是「做到了」** | `remaining` 的判据是「表达式里出现 `max_dispatches`」→ 把 `b.max_dispatches - prog.dispatches` 删成 `b.max_dispatches`（缺陷全恢复）照样通过 | 判**结构**不判**子串**：断言它是一个 `BinOp` 且 `op` 是 `Sub` |
| **判据判的是「调了」不是「用了」** | 断言「有一次 `plan_wave` 调用」→ 调了、参数全对、**把结果丢掉**再自己切一次，照样通过 | 断言**返回值被绑定到那个名字**上 |
| **⛔ 为一个从未存在过的字符串写的守卫** | `assert "sp.parallel]" not in src`，而真实历史里那行是 `[:max(1, sp.parallel)]`——源码里是 `sp.parallel)]`。**把带缺陷的原版放回去，它照样通过** | 红检必须**用真实的历史版本**，不是用自己想象的错误写法 |

⚠️ 第三条最值得停一下：它**通过了红检**——因为我红检时用的是我脑子里那个反模式，
不是仓库里真实存在过的那一行。⛔ **红检用错了输入，等于没红检。**

---

## ⭐⭐ 假红也有形态，而且**比假绿贵**（2026-08-11 新增）

⛔ 前面七种讲的全是「不该绿的绿了」。这一节讲反过来的那半边 ——
**不该红的红了**。它比假绿贵，理由很直接：

> 假绿让你**少修一个真 bug**；⛔ 假红让你**去修一个根本不存在的 bug**。
> 前者的代价是一个缺陷，后者的代价是**全部时间**。

**账单**：2026-08-08 一道假红烧掉 **514 万 tokens**、产出 **0 个文件**，
⚠️ 还把一个 **42 秒**就改对了的工人逼得**删掉自己正确的代码**。

### 三种形态

| 形态 | 实例 | ⭐ 怎么防 |
|---|---|---|
| **① 对照物过期** | 闸的基线存档录于 7 个提交之前，中间隔着一次故意改变世界的改动 → **数学上必然红** | 体检模式（`DEVLOOP-PREFLIGHT: 1`）：开跑前 0.55 秒验一次「这份对照物配不配得上这份代码」 |
| **② ⭐⭐ 判据的维度错了** | 想验「吃饱了还吃没有」，判据写成「这一回合末的能量比上一回合末高」——⛔ 而**代谢在同一回合里先扣了一刀**，掉到线下再吃回来是**完全合法**的。当场喷出 **7983 条**假红 | ⭐ 外面量不到的东西，就**把它做成量得到的**（往代码里加计数器），⛔ 而不是退而求其次找个代用品 |
| **③ 正则吃得太宽** | 检查「引用的路径存不存在」时，把 `BACKLOG.md#G-124` **整串**当文件路径去查 → 把**真路径**报成失效 | 先按 `#` 切开：前半查文件在不在，后半查章节名查不查得到 |

### ⚠️ 第 ② 种最值得停一下

它是「**判据的维度错了**」（第三种假绿）的**镜像**。
⛔ 同一个病根：判据量的不是那件事本身，而是一个跟它相关但不等价的东西。

- 当它偏保守 → 假绿（该红没红）
- 当它偏激进 → 假红（不该红却红）

⭐ **所以「判据的维度对不对」这一问，要在红检**和**绿检**两个方向上各问一遍。**
⛔ 只做过红检的判据可能对**任何**输入都报警 —— 和一根坏掉的火警没区别。

### ⛔ 还有一条：红检的「破坏」可能压根没生效

2026-08-11 实测：做红检时那条替换字符串**根本没匹配上**，
**什么都没坏**，于是判据打了个绿灯 —— ⚠️ 而「破坏了但守卫没反应」
和「压根没破坏成」在退出码上**长得一模一样**。

⭐ 做法：破坏之后**先 `grep -c` 确认那个字符串的计数真的变了**，再去看闸的颜色。

### ⛔ 守卫自己也会有洞，而**有洞的判据比没判据更坏**

同一天另一支守卫只认 `snap["x"]`、不认 `snap.get("x", …)`，
于是把同一个字段**同时**报成「存了没读回来」**和**「存档✔读档✔」——
**同一支守卫自己打自己的脸**。
⭐ 它会让人去修一个根本不存在的问题。

---

## ⭐ 正则的词边界 `\b` 在中文标点上不成立（2026-08-11 新增）

**症状**：一把「最硬那档」的判据悄悄失效，而且**没有任何征兆**。

**实例**：对账脚本用 `grep -oE '\b[0-9a-f]{16,}'` 抓世界哈希。
⚠️ 而哈希印在**中文括号**里（`（77a3fa91d33d1ae8）`）——
`（` 是多字节字符，⛔ `\b` 在它旁边**不成立**，于是**两个真哈希被整个漏掉**。

**后果**：那一档判据从「比 2 个哈希」变成「一个都不比」，
⛔ 而脚本照样打绿章。⚠️ **悄悄削弱判据比没有判据更危险。**

**修法**：别用 `\b`。取 `[0-9a-f]{16,}` 再用长度过滤（`awk 'length($0)==16'`）。
⚠️ 顺带排掉了 `最大偏差 = 0.00000000000000000` 里那串 17 个零 ——
⛔ 那玩意儿以前被当成一个「哈希」记进过基线表。

⭐ **一般化**：凡是判据里出现 `\b`、`\w`、`\s`，先问一句
「**这段文本里会不会有非 ASCII 字符**」。中文项目里答案基本都是「会」。

### ⛔ 还有一条更根本的

那批测试当初退化成「读源码形状」的理由，是我写在 docstring 里的一句话：

> 「要验它得跑真单，那**花额度**，不能拿来做测试。」

⭐ **那句话是错的。** 复核当场把 `_run_unit` 换成一个只写台账的桩，
**零额度**真跑完整个 `cmd_autopilot` 循环并数出实际派单次数
（见 `tests/test_autopilot_dispatch_count_e2e.py`）。

⛔ 所以第七种最深的一层是：**为「只能用代理判据」找的那个理由，本身要先被检验。**
「测不了」这三个字出现在注释里的时候，它就是下一个要被推翻的假设。

---

## ⛔ 收尾纪律：改完代码**必须**同步文档，不等人问

2026-08-02 用户连问两轮「这些改动都写进文档了吗」，⛔ **两次实查都是没有**：

| 漏的 | 后果 |
|---|---|
| `devloop nightly` 加进 CLI 当天 | 四份文档**零提及**——那条命令等于不存在 |
| `prune --archive/--delete/--discard` | 同上，照文档做事的人找不到它 |
| 刚合进来的两份模板 | 新项目接入照 `templates/` 抄，没写进文档等于没有 |
| 测试条数 | 写着 589 而实际 612——⚠️ 那句话是当**证据**用的 |

⭐ 用户的原话：**「这个文档其实才是最重要的东西。」** 他是对的：
`SPEC.md` 是契约、`BACKLOG.md` 是缺陷台账、本文件存着七种假绿的教训。
⛔ 文档腐烂 = 下一个人照着错的前提做决定。
实证：SPEC 里模板份数那一格**与磁盘事实不符过两次**。

### ⭐ 答案不是「记住」，是让机器逼着做

⚠️ 一条只活在脑子里（或只写在文档里）的规矩必然腐烂——那正是第 ② 种假绿
（实现了但生产路径够不着）在**流程**上的形态。

`tests/test_docs_in_sync.py` 机械对账四件事：

| 检查 | 会抓到什么 |
|---|---|
| 每个子命令都要在 SPEC + README 里 | 加了命令没写文档 |
| 每个长开关都要在 SPEC 里（白名单需写明理由） | 加了开关没写文档 |
| `templates/` 每份文件都要被 SPEC 点名 | 加了模板没写文档 |
| 文档里声称的测试条数必须是真的 | 那个当证据用的数字变旧 |

⭐ 判据全落在 **AST** 上（不是子串），⛔ 注释里的示例不会误伤——
误报会训练人忽略检查，这是本文件写过的话。

⚠️ 而闸跑的是 `pytest -q` **全量**，所以这道检查**自动进闸**：
任何一单改了 CLI 却没改文档，`pytest` 那道闸当场红，产出合不进主线。

### ⛔ 它挡不住什么（别把它当成「文档已经好了」）

| 管 | 不管 |
|---|---|
| 有没有被提到 | 提到的那段话准不准 |
| 开关有没有出现 | 语义讲没讲清 |
| 模板有没有被点名 | 模板本身好不好用 |

⚠️ 语义层面仍然要人自己过一遍。⭐ 机械检查只挡住「压根没写」这一种，
而那恰好是**最常发生**的一种。

---

## ⭐ 早上接管：三步，⛔ 没有第四步

无人值守跑完一夜，第二天早上：

```bash
devloop nightly --project <项目>      # 一屏读完：否决位 + 每支分支的闸判定
git -C <项目> show <sha>              # 只看你打算合的那几支
git -C <项目> merge <分支>            # 合（⚠️ 宪法 C-2/C-4：这一步要人亲自点头）
devloop prune --project <项目> --archive <目录> --delete   # 或者弃（先打包再删）
```

### ⛔ **没有 `git revert` 这一步，别再设计一遍**

2026-08-02 设计「每轮自动 commit 到 GitHub 以便回档」时核实的：

| 词 | 在本项目里的实际含义 |
|---|---|
| **回档** | ⛔ **不存在**。主线永远不会被自动改动——全仓唯一写 git 的地方是 `worktree.py::commit_result`，且提交前硬断言 `HEAD == 隔离分支`；`checkout`/`switch`/`push`/`reset` 全仓零次出现（`tests/test_no_git_write_verbs.py` 钉住）。⭐ **不合并本身就是回档。** |
| **翻 log** | ⚠️ 翻错了 log。主线的 `git log` 里**没有昨夜任何信息**。真正的记录在两处：每支 `devloop/*` 分支的**尖端提交信息**（逐字写着 `工人产出 · 闸全绿\|闸未过\|未跑闸`），以及 `.devloop/telemetry.jsonl`。 |
| **保底** | ⭐ 用 `prune --archive`：`git bundle` + **真跑 `bundle verify`** + `MANIFEST.md`（含取回命令）。⚠️ 它比一份 markdown 提交更抗丢，且不碰主线、不碰远端、不需要凭据。 |

### ⛔ 为什么不做自动 commit / 自动 push

三条，每条都足以否决：

1. **自动 commit 的朴素实现要 `git checkout -b`，会移动主工作区 HEAD。**
   而 `prune.scan()` 的 `git branch --merged` **不带 committish 参数**——
   它的含义是「合进**当前 HEAD**」，而 `prune --delete` 现在会真删。
   ⚠️ `halt --kill` 走 `taskkill /T /F`，无 `finally` 无 `atexit`，
   进程能死在 checkout 与 checkout-back 之间 → **主仓永久停在报告分支上** →
   可删集合算错 + `resolve_base()` 让宪法锚在错基准上（**错基线产出可信的绿**）。

2. **自动 push = 把未经人看的工人自由文本发布出去。**
   报告内容是台账的 `error` 原文、`gate_detail`、`findings.evidence`
   ——全是 LLM 写的。而宪法 B-4「对外发送」登记状态是 `out-of-reach`，
   处置写死为「**事前不给能力**」。⛔ 自动 push 就是给能力。

3. **HTTPS 凭据过期时 `git push` 会永久挂住**（弹交互、不退出、不报错、不留报告）。
   ⚠️ 那把「失败」变成了「什么都没发生」——⛔ 而那恰是这个功能要治的症状本身。

---

## ⭐ 动手前照着过一遍（七种形态 → 现在有没有机械守卫）

⚠️ **诚实口径**：能机械化的只有一半。剩下的靠判断，⛔ 别假装有守卫。

| 形态 | 现在有没有机械守卫 |
|---|---|
| ① 守卫的目标不存在 | 🔶 部分。`gates.py` 拦「点名了不存在的闸」「exit 0 却零 PASS」；⛔ 但「判据恒真」本身没法机械判 —— **靠红检** |
| ② 实现了但没接进调用路径 | 🔶 部分。多处 AST 接线测试；⭐ `tests/test_no_undefined_names.py` 抓「只在少走的分支上引用未定义名字」；⛔ 抓不到 KeyError 那一类 —— 那要靠「判定集合与打印表同源」（`gates.VERDICTS` + `test_判定集合与打印符号表必须同步`） |
| ③ 判据选错维度 | ❌ **没有**。只能靠：**判据要落在能直接量的东西上，⛔ 不许用代理指标** |
| ④ 测了错的那一层 | 🔶 `.devloop/gates.sh` 有「被测代码来源」那道闸；⛔ 其它层次没有 |
| ⑤ 验收正确但产出没保存 | ✅ `commit_result` + `prune`（且 2026-08-02 起 `empty` 不再参与删除判定）+ `records.py` 归档 |
| ⑥ 验了但没验全 | ❌ **没有**。只能靠追问：**这份试验的输入形状覆盖真实形状了吗？跑了几次？** |
| ⑦ 测试自己假绿 | ❌ **没有**，且**最难自查**（写测试的人和被骗的人是同一个）。⭐ 唯一有效的手段是**派独立视角要求推翻**，见下 |

### ⭐ 唯一被证明有效的手段

2026-08-02 一天之内，三轮独立复核**每一轮都推翻了上一轮**，
包括推翻我写在提交信息里的七条结论。⛔ 而这些结论在被推翻之前，
每一条都有「实测」二字撑着。

**做法**：修完之后，另派一组，任务书写「**证明这些修复是错的**」——
⛔ 不是「请核对」。⚠️ 「请确认这条对不对」拿到的永远是「对」。

---

**共同的排查方法**：不要问「闸有没有跑」，要问「**如果我故意搞破坏，这道闸会不会红**」。红绿双测里的**红测**就是干这个的，而且红测必须针对每一道闸单独做，不能只测整体。

---

## 排查时的通用纪律（从那次翻车里总结的）

1. **报错措辞的细微差别是最强的线索。** `temporarily unavailable` ≠ `may not exist`；`OAuth token expired` ≠ `api key invalid`。别把它们归成一类。
2. **报错的「格式」暴露请求去了哪。** Anthropic 的参数校验错误 vs 第三方的认证错误，格式完全不同。
3. **反证要认真对待。** 「用户的终端 CLI 是好的」这条事实，第一轮就摆在那里，却被当成噪声。**任何与假设矛盾的既有事实，都必须被解释掉，不能绕过。**
4. **能测就别推理。** 那次排查里，读配置读了三遍没结论，三条命令定的案。
5. **`duration_api_ms: 0` 意味着请求压根没发出去**——是本地预检失败，不是服务端问题。这个字段能一秒区分「本地配置问题」和「远端服务问题」。
6. **⛔ 退出码只能从命令本身取，不许从管道后面取。**
   `cmd | head -5; echo $?` 拿到的是 **head** 的退出码，永远是 0。
   **2026-07-27 一天之内栽了四次**：三次误判「后台作业返回 0」、一次误判「未知后端没报错」，
   每次都花时间去查一段其实正确的代码。
   正确写法二选一：
   ```bash
   cmd > /dev/null 2>&1; echo $?      # 要退出码就别接管道
   set -o pipefail                     # 或者显式打开（注意它改变整条流水线的语义）
   ```
   ⚠️ 这条与第 5 条同源：**拿一个看起来相关的数当直接证据**。

7. **⛔ 单测绿不代表接线通。** 2026-07-29 一天之内，在**同一个形状**上栽了四次：
   - 成本函数是对的，但派单路径没把价目 key 传下去（`1ff4dbb`，订阅单成本记成 None）
   - 凭据判据是对的，但判的那个字段与实际行为不符（`6b321f9`，见故障 8）
   - 闸的判据是对的，但**维度**错了——挡住作弊的同时把合法的 TDD 写测试一起挡了（`97f40a1`）
   - T5 宪法接上了，但传进去的是计数不是快照（`023726e`，见故障 12）

   **四次全部是「真跑」抓到的，单测和 `--dry-run` 一个都没抓到。**
   ⚠️ 这与第 5／6 条同源，但更进一步：那几条是「拿间接读数当直接证据」，
   这一条是**拿「测过了」当「接通了」**。⛔ 一条路径没被真跑走过，就不算接上。

   **⛔ 2026-07-30 把这条收紧成一句话：「pytest 绿」不构成「这一项能用」的证据。**
   两天里的真 bug **全部**是真跑／真读二进制／真做并发试验抓到的，
   ⛔ **单测与 `--dry-run` 一次都没抓到**。形态各不相同，但同属上面那六种假绿：

   | 真 bug | 单测为什么绿 | 假绿形态 |
   |---|---|---|
   | 自动驾驶路径上 T5 三道全死（`8d9f6f4`） | 测的是 `_run_unit` 内部，没测调用方传没传 | 实现了没接线 |
   | `require_pass` 一行没读过（`8d9f6f4`） | 没有任何测试针对它 | 守卫的目标不存在 |
   | `ws_before = None` 覆盖快照（`1659c49`） | 测试**自己**把真快照传进去 | 判据维度错 |
   | `!` 前缀根本到不了模型（`1659c49`） | 自测直接灌钩子 stdin，绕过 CLI 输入分类 | 测了错的那一层 |
   | 台账并发丢行（`0e2518e`） | ⛔ 更坏：**旧结论 G-41 是一次通过的实测**，只是数据形状（等长短行）避开了触发条件 | 验了，但没验全 |

   ⚠️ 最后一行是这条纪律的边界情形：**手里有一份通过的实测报告，也不等于结论成立**
   ——要追问「这份试验的**输入形状**覆盖到真实形状了吗」「它跑了**几次**」。
   G-41 是**一次**试验，复跑 40 次是 **40 次全败**。

   ⛔ **自评纪律**：把「写了 ＋ 单测绿」写成「能用」，正是这个项目一直在防的事。
   报告一项能力时，要么给出**真跑**的出处（提交号／台账／回执），
   要么如实写「已实现，未真跑验证」——⛔ 两者之间没有中间地带。

8. **⭐ 能直接读到答案的地方，一律不许用特征匹配去猜。** 本项目已**反复**栽在同一模式上（完整清单见 BACKLOG G-15 · G-24 · G-35 · G-37；⚠️ 不在此处记次数——三处分别写过五次/五次/七次，可数的计数器必然分叉）：用文件时间戳猜认证是否过期（~~实际 `claude auth status` 直接给 JSON~~ ⛔ **2026-07-29 这个「直接接口」本身也被推翻了**：`auth status` 只看文件在不在，见故障 7；改读 `expiresAt` 也不对，见故障 8。**这里唯一的直接答案是真起一次子进程**）· 用路径里有没有某个词猜模块加载来源（实际 `__file__` 就是完整答案）· 用「端点抖动」猜故障原因（实际报错格式直接指明了去向）。**先找有没有直接接口，再考虑推断。**

---

## 配套自检工具（`devloop doctor`，Phase 0 产出）

文档解释「为什么」，脚本回答「现在坏没坏」。**已实现**的检查项：

| 检查 | 判据 | 备注 |
|---|---|---|
| 全局 env 洁净度 | 是否残留会打死辅助通道的 9 个键 | 见故障 2／3 |
| 工人配置 | 能否加载，且来源是独立文件还是回退路径 | 来源必须报出来 |
| Anthropic 认证 | ~~调 `claude auth status` 解析 JSON~~ → **`credentials.check()` 的排除法**，只答「肯定跑不了」 | ⚠️ 判据被推翻**三次**，三次都是拿间接读数替代真实行为：① 文件时间戳（G-15）② `auth status`（故障 7，它会撒谎）③ 文件里的 `expiresAt`（故障 8，CLI 自己会续期）。⛔ 三条都不许再用来拒派 |
| claude 可执行文件 · Git Bash | 显式定位，排除 System32（WSL 入口） | ⚠️ 原表这里写「见故障 9」，但本手册**从来没有过**那一条（`git log -S` 查证：从未存在）；2026-07-29 新增的故障 9 与它无关 |
| 派单通道 | 真派一单，核对回执 `modelUsage` | 唯一能证明通道活着的方式。⛔ 命令行**必须复用 `dispatch.build_cmd`**，不许在这里再拼一遍——2026-07-29 交叉核对抓到它原本写死 `--bare` + `--output-format json`，而真派单早已改成 `stream-json --verbose`、订阅后端**绝不加 `--bare`**，于是它验的是**另一条路**（`devloop/doctor.py:167-177`） |
| 凭据真探针 | `--probe` 真起一次子进程 | ⚠️ 花额度，所以默认不跑；⭐ 与排除法不一致时必须单列报出来（见故障 8） |
| 桌面子代理通道 | **Python 侧不可见，必须在桌面会话内实测** | 工具能自检的边界到此为止 |

**触发时机**：任何配置改动之后、换机器之后、以及「感觉哪里不对」的时候。
**跑法**：`python -m devloop.cli doctor --project <项目路径>`（省略 `--project` 则跳过派单实测）。

> ⛔ **`doctor` 带 `--project` 或 `--probe` 时真花订阅额度**（见故障 15）。
> 2026-07-30 起：这两种形态**需要令牌**才跑得起来（裸 `doctor` 仍随便跑），
> 且这一单会被 `_record_smoke()` 记进台账。
> ⚠️ 在此之前它一行账都不留，而它是「夜跑第 1 步」——**夜跑会在一个已被吃掉的 5 小时窗口上起跑**。

---

## 两天的实测数字（2026-07-29 → 07-30）

⛔ 这些数字是给「核对得上」用的，不是成绩单。

| 项 | 数 |
|---|---|
| pytest 用例 | **377**，0 失败（两天前是 291） |
| `tools/test_mode_gate.py` | **46 项全过** |
| 2026-07-29 真派单 | **11 次**，美元成本 **$0.0082** |
| 自动驾驶真跑 | **3 次**：1 次当场崩（故障 12）、2 次 2/2 全绿 |

⚠️ 那 `$0.0082` 是**一笔 `doctor` 误走 deepseek** 产生的，已如实记账；其余全走订阅额度 = $0。
⭐ 订阅制的美元成本**是 0，不是未知**——记 `None` 会让自动驾驶第一单就停机。

⚠️ **诚实口径（⛔ 别把它读成「防线已验证」）**：「撞额度当场停整批」这条路
**从未真触发过**——台账里 `failure_class = "ratelimit"` 出现 **0 次**。
真跑到的只是 `rate_limit_event` 里 `status: "allowed"` 的**解析路径**。
⛔ 停批本身仍属「已实现，未真跑验证」。

---

## 故障 20 · 打了 `%task` / `%go`，屏幕上什么都没发生

**2026-07-31 与 08-01 各撞到一次，原因不同。**

### 症状 A：`%task` 打在句子中间

```
放行 %task 派 e1-dedup-prey-layer 这一单
       ↑ 前面有字 → 不触发
```

⛔ 代号必须在**第一行的第一个字符**。前面有任何字都不算数。

⭐ 拦住是**对的**——行首锚定正是「只认你的真实按键、不认粘贴内容」这个属性的来源。
若允许句中触发，模型复述你的话时就能把自己放行。

**现在会怎样**（`6ced08f` 之后）：屏幕上会出现一条可以直接照抄的提示——

```
⚠️ **这条没生效**：`%task` 在句子中间，而代号必须在**第一行行首**。
   照抄重发：%task 派 e1-dedup-prey-layer 这一单
```

⚠️ 在那之前是**零回显**——你以为放行了，模型收到的是「模式 A」。

### 症状 B：`%go` 说「没有待批准的阶段（或已超过 30 分钟）」

⛔ **原因是时钟从错的地方起算**（G-69）：批准的有效期是从你打 `%auto` 那一刻算的，
而中间那段「模型演练并汇报」也在吃同一个 30 分钟。

> **演练得越认真，你的批准有效期越短。**

**修法已落地**：改从 `%go` 那一刻起算，且 `%go` 的回显会告诉你**批准的是什么**。

**如果仍然撞到**：两条**连着发**，中间别让模型干活——

```
%auto <项目> <要干的事>
```
（模型只回一句确认）
```
%go
```

⚠️ 想先看状态：打 `%mode`，它只看不改。

---

## 故障 21 · 闸报 `✓ PASS`，但它其实一条都没验

**症状**（eco-ob 首跑实录）：

```
✓ 闸·禁改清单: worktree 内无 .devloop/（该项目未跟踪它，符合预期）
```

它诚实地说了「无可查」，**却报了 PASS**。而它被写进 `--require-pass`
——⛔ **点名一道恒过的闸，等于没点名**。

**修法**（`c20e26f`）：闸的协议新增第四档 **VOID**。项目侧 `gates.sh` 遇到
「本项目里这道没有可验对象」时应报 `VOID` 而不是 `PASS`：

```sh
printf 'VOID\t禁改清单\t本项目未跟踪 .devloop/，无可查\n'
```

⚠️ **VOID 与 SKIP 的区别**：两者对「能不能放行」含义相同（都不算通过），
但排查方向完全不同——**SKIP 要查谁开了开关，VOID 要查为什么这个项目里它是空的**。

⛔ **别在 devloop 侧用启发式去猜哪些 PASS 是空的**（比如「detail 里没数字」）
——那会误伤 `基线守卫: ref_*.json 未被改动` 那种真验过的。**只有闸自己知道。**

---

## 故障 22 · 批次退出码 0，但其实是撞额度停的

⛔ **2026-08-01 修掉的真 bug（G-70），改动之前一直在。**

退出码原来判的是「还剩几单没派」：

```python
if todo: return 3
return 1 if failed else 0
```

**额度在最后一波撞上时** `todo` 已空 → 退出码 **0**——上游会判定「这批活干完了」。

⚠️ 串行默认下够不着这个洞（撞墙必然留下未派的单）；**只读默认改成并发之后才暴露**。

**怎么确认自己撞的是这个**：翻台账最后几行，看有没有

```json
"failure_class": "ratelimit"
```

或者回执里的 `rate_limit_event` 是 `status: "rejected"`。

**修法**：判据改成 `if halt.tripped or todo: return 3`。

---

## 故障 23 · 自动驾驶跑到第 3 单必失败，报「记录被抹」

```
宪法命中 A-2 不得写到隔离区之外（记录被抹）
（<项目>/autopilot/<阶段>.json：前缀被改写（抹掉历史再补））
```

⛔ **这是误报**（G-71，`26cd678` 已修）。那个文件是 **autopilot 自己的运行记录**，
它每一轮都要重写（`run.save()`），而记录守卫的判据是「**只追加**」。

⚠️ 「只追加」对**账本**成立（`telemetry.jsonl` / `findings.jsonl` / `reports/` / `tasks/`），
对**可变状态文件**不成立。

**修法**：`records.py` 排除 `jobs` / `handoff` / `autopilot` 三个目录
——⭐ 判据现成：`.gitignore` 早就把它们定性为「**运行产物，不是源码**」。

**如果你在旧版上撞到**：升级；或临时把 `--require-pass` 里的宪法项摘掉
（⚠️ 那会同时关掉真正的记录守卫，⛔ 只能当临时手段）。
