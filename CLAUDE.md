# DevLoop — 硬约束

给 AI 编码代理用的编排壳：**并行 + 隔离 + 脚本验收闸 + 人在环**。

> ⛔⛔ **别再写成「贵模型规划 + 便宜模型执行」。** 那是立项时的说法，
> 「省钱」这条理由 **2026-08-01 就被实测推翻**（`PLAN.md` §0.1 的 C 档），
> 2026-08-15 用户裁决**明确跳过便宜模型**（D-COST-01，理由：省不了 token）。
> ⚠️ 台账实测：2026-08 起 **15 单全部走 subscription（opus），deepseek 0 单**。
>
> ⭐ **派单今天剩下的三个理由，一个都跟省钱无关**：
> **并行**（一次能跑好几件）· **隔离**（各自 worktree，改坏了整个丢掉）· **无人值守**（跑一夜）。
> ⛔ 工人吃的是**你自己的订阅额度**——派单**不省额度，是多花**。排优先级时记住这一点。
>
> ⚠️ 这一行为什么错了两周：结论写在 `PLAN.md`（要主动去读），
> ⛔ 而**每轮自动加载的这份**照抄了旧话。⭐ 同一个形状：**结论没回流到必读的那份**。
Python 包（`devloop/`）+ 项目侧 shell 闸（`templates/gates.sh` → `<项目>/.devloop/gates.sh`）。

---

## 0. ⭐⭐ 这个仓是**目的**，靶子项目是**手段**

`C:/pg/eco-ob`（Godot 生态模拟）是**靶子**，不是产品。它存在的唯一理由是**把这个工具用出毛病来**。

> **用户 2026-08-11 原话**：「我们的主要目标是这个工具，eco-ob 是我们的手段。
> **我感觉你每次都在忘这一条。**」

⛔ **这条已经复发过两次**，两次都是「写下来」失效：
`_trials/eco-ob-01/50_关于工具的结论.md` 的 T-7（2026-08-01，原话「我把手段当成了目的」）；
`BACKLOG.md` 的 G-124（2026-08-08，编了号、标着未修，三天后又倒进 4 条新的）。
⭐ **所以判据不能是文字。**

### 判据：靶子的闸版号 **不许超过**模板

```
grep -o "DEVLOOP-GATES-REV: *[0-9]*" templates/gates.sh <靶子>/.devloop/gates.sh
```

- **靶子 > 模板** = ⛔ 改进闸的办法**卡在靶子上没搬回来**，下一个接闸的项目一分好处拿不到。
- 三处会喊：`doctor.py::gates_template_drift`（体检）· `tools/devloop-mode-hook.py::_drift_line`（**每轮注入，靶子仓里每条消息都看得见**）· `tests/test_gates_template_drift.py`。
- ⭐ 要让它归零，**必须真的去编辑 `templates/gates.sh` 的内容** —— 写字改不动它。而搬完它**自己就闭嘴**。

⚠️ 射程写在 `templates/constitution.toml` 的 `C-6`（`status = "partial"`）。
⛔ **判不了**：不落在 `gates.sh` 上的教训（那些进 `templates/task.md` / `TROUBLESHOOTING.md`）；
「只改数字不搬内容」；「改了闸却不给版号加一」。⛔ 别把它读成「回流已覆盖」。

---

## 1. 权威分层（问的问题不同，权威不同）

| 你在问 | 谁说了算 |
|---|---|
| **现状 / 做没做 / 怎么实现的** | `devloop/*.py` · `tests/` · `templates/` ← **文档在这条链上最不可信** |
| **契约**（子命令、退出码、闸协议、文件格式） | `SPEC.md` ← ⚠️ `tests/test_docs_in_sync.py` 会强制它与代码同步 |
| **为什么这么设计 / 决策点状态** | `PLAN.md` |
| **已知缺陷与待办** | `BACKLOG.md` —— ⭐ **一页纸，只放还开着的**（2026-08-16 从 4037 行砍到 127 行）|
| **修过什么 / 历史** | `BACKLOG-ARCHIVE.md`（`G-NN` 编号，老链接照样跳）⛔ 别往待办本里倒历史 |
| **踩过的坑怎么绕** | `TROUBLESHOOTING.md`（`## 故障 N`） |
| 任何冲突 | **用户当场的话 > 一切** |

---

## 2. ⛔ 红线（照做就出事）

- ⛔ **密钥永不进仓**。它们在 `~/.claude/devloop-backends.json`。
  读后端配置时只许打印 name/kind/model/price_key，⛔ **不许打印任何凭据字段**。
- ⛔ **只在用户明确要求时提交**（`git commit` / `git push` 都是）。⛔ **永不 push**。
- ⛔ **合并隔离分支到主线要用户逐单批准**（宪法 C-2/C-4）。⛔ 不许自合。
- ⛔ **`constitution anchor` 只有用户能跑** —— 「锚一旦能自愈，它就不是锚」。
- ⛔ `C:/pg/eco-ob-p4` 是**冻结的评测基线**，已 `git worktree lock`。⛔ 一个字节都不许写。
- ⚠️ **改用户全局配置（`~/.claude/settings.json`）必须用户自己批准。**
  ⭐ 今天的机制刻意不动它 —— 要用的注入点已经挂在 `UserPromptSubmit` 上了。

---

## 3. ⭐ 判据纪律（这个工具的全部价值都在这几条上）

- **红检**：故意弄坏 → 守卫必须报警。**绿检**：什么都不改 → 守卫必须是绿的。
  ⛔ **只做过红检的守卫可能对任何输入都报警 —— 和一根坏掉的火警没区别。**
  ⚠️ 红检之前**先确认「我改的那个字符串真的变了」**：破坏没生效时，绿是假的（2026-08-11 实测撞到）。
- **判据要落在能直接量的东西上**，⛔ 不许用代用品。
  ⭐ 外面量不到，就**把它做成量得到的**（往代码里加计数器），⛔ 而不是退而求其次。
- **回归对账只能答「跟以前比变没变」，⛔ 答不了「改对了没有」。**
  ⭐ 每一组改动都必须另配一条**正面判据**。
  ⚠️ 2026-08-11 实测：eco-ob 七个套件全绿、失败一条没多，而代码是错的。
- ⛔ **反复出现的形状（第 12 次）**：
  > **同一件事有两条路。修好的永远是「有人盯着」的那条，漏掉的永远是「没人盯着」的那条。**

  第 12 次就长在这个工具自己身上：`gates_template_drift` 只验「靶子落后于模板」，
  ⛔ 「靶子领先」（= 教训没回流）判绿并撒谎「rev 7，与模板一致」。

---

## 3.1 ⛔⛔ 下一步该干什么，判据只有一条（2026-08-16 立）

> **不是「又修好了几条」，是「今天能不能派出一单并跑完」。**

⚠️ **实测（⛔ 不是感觉）**：

| 期间 | 真派出去的单 | 仓库自己长了 |
|---|---|---|
| 2026-07-26 → 08-02 | **95 单** | +44,537 行 |
| 2026-08-02 → 08-16 | **4 单**（最后一次 **08-08**）| +11,689 行，删 377 行 |

⇒ **这两周的工作没有让工具多干成一件活 —— 它让工具「更正确地描述自己没在干活」。**
测试 15,532 行 > 工具本体 10,374 行；待办本一度 4,037 行服务 10 条真待办。

⭐⭐ **这是「把手段当目的」的第三次复发**，前两次是 eco-ob（T-7 / G-124）。
⛔ 那两次的判据（闸版号）**盯不住这一条**。

**⭐ 所以每次要动手之前，先问这两句：**
1. 这条不修，会挡住「**并行 / 隔离 / 无人值守**」中的哪一条？—— **一条都不挡就别做。**
2. 要派单的话：**这一单能回答一个我还不知道答案的问题吗？** ⛔ 答不上来就别派。

⚠️ 2026-08-16 我自己栽在第 2 句上：提议「派一单验证工具」，复盘发现它能答的问题
**已经有 95 个样本**，动机是「想证明这两周没白干」——⛔ 那是情绪，不是判据。

⭐ 三条价值今天的真实状态：**隔离**一直在用 · **并行**真跑过 25 批（末次 08-02）·
⛔ **无人值守 0 次**（07-29 那「20 小时 30 单」实测是手敲的，中间 3 处空档 7.5h/11h/1.5h）。

---

## 4. 状态指针（⛔ 这里不存状态，存了就会腐烂）

- **进度 / 下一步 → `PLAN.md` 的决策点状态表**。
- **待办 → `BACKLOG.md`**。
- **测试条数、子命令清单 → 别抄，`tests/test_docs_in_sync.py` 会替你查。**
- ⛔ **写文档时锚点用「符号名 / 章节名」，别用行号** —— 行号有半衰期，你加的注释本身就会把它推移。
