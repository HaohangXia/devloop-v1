# DevLoop · 缺陷档案（历史）

> ⛔ **这里不是待办本。** 还开着的在 `BACKLOG.md`，那是一页纸。
>
> **这份文件是什么**：2026-08-16 之前 `BACKLOG.md` 的**全文原样**，一个字节没改。
> 113 条里 **79 条已闭 · 20 条是笔记 · 4 条是取舍**——⭐ 都是历史，查得到就行，
> ⛔ 不该每次打开待办本都要翻过它们。
>
> **为什么拆**：拆之前 4,037 行 / 113 条，⛔ 而**真正开着的只有 10 条**。
> ⇒ **97% 的篇幅在服务 10 条待办。** 用户 2026-08-16 原话：
> 「不要把东西越做越多越做越复杂了，记得我们的初心。」
>
> ⚠️ 交叉引用（`G-NN`、`#g-nn`）**全部保留在本文件里**，老链接照样能跳。

---

# DevLoop · 缺口台账

> **这份文件为什么存在**：2026-07-26 Phase 1 验收时，缺口清单只说在了对话里、没有落盘，用户追问「记录了吗」才暴露。**口头清单会蒸发**——而 PLAN.md 是设计文档、状态会腐烂它，所以缺口单独存放。
>
> **规矩**：① 每条必须写明**触发条件**（什么时候必须修），不写「有空再说」；② 修完就划掉并注明证据；③ 新缺口来自实测或审计，不来自感觉。

---

## ✅ G-125 · 拿存档当对照的闸，不知道「基准是哪个提交」（2026-08-09 修）

**怎么暴露的**：2026-08-08 第一次把「逐比特中性」接成真闸，然后真派一单。

⛔ **那道闸数学上必然红**：对照存档录在 `5e746f1`，派单基准是 **7 个提交之后**的
`f7636381ab35`，中间隔着一次标题就写着「速度=出手频率」的、**故意改变世界**的改动。

⚠️ 后果不是「闸报了个错」，是**它把一个做对了的工人逼疯**（工人原话）：

> ①「我确认逻辑无懈可击但仍破基线，说明我漏了某处副作用。」
> ②「我已把改动 revert。现在跑一次纯 baseline，验证工具的确定性。」

⭐ 它 **42 秒**就改对了（同样改法主模型验过：快 45%、逐比特不变），
⛔ 然后**把自己对的代码删了**，追了 49 分钟不存在的 bug，
超时交白卷 —— **5,146,226 tokens 换 0 个文件**。

**修法**：编排方把基准提交塞进环境变量 `DEVLOOP_BASE_COMMIT`
（`gates.py::run_gates` 新增 `base_commit` 参数，`cli.py::_run_unit` 传 `base`）。
⛔ 闸自己**算不出**这个数 —— 它跑在 worktree 里，那儿的 HEAD 已经带上工人的提交。
⭐ **知道基准的是编排方，所以由编排方传。**
守卫：`tests/test_gate_gets_base_commit.py`（4 条，含调用点 AST 检查）。

---

## ⬜ G-129 · 便宜模型这条路（⛔ 明确跳过，⭐ 以后再考虑）

**2026-08-15 用户裁决（D-COST-01）**：「跳过便宜模型…… 目前先将所有的项目和任务都跳过它。」
理由原话：「**它并不能减少我们的 token 消耗**。」
⭐ 但用户明说了：「**作为我们待办，之后可以考虑继续改进**」——⛔ 所以这条是**搁置**，不是否决。

**⚠️ 搁置之前留下的实测数字（⛔ 别丢了，以后重启这条路时它就是起点）：**

| | 只读侦察 | **真写代码** |
|---|---|---|
| deepseek（便宜） | 41/46 = 89% | ⭐ **5/9 = 56%** |
| opus（贵） | 7/8 = 88% | ⛔ **11/23 = 48%** |

⛔ **注意这张表说的不是「便宜模型不行」** —— 它写代码的合格率**比贵模型还高**（样本小：9 vs 23）。
⚠️ 停用它的真实理由是「订阅额度 = $0」，⭐ 而 `$0 ≠ 免费`：额度是稀缺资源，
**而且跟编排方共用一个池**（实测两次超时的错误里写着「5 小时会话上限」「周上限已用 81%」）。

**⭐ 什么时候该重启这条路**（推翻 D-COST-01 需要的证据）：
同一份任务书派两次（便宜 / 贵各一次），便宜那次在**合格率不降**的前提下，
把**订阅额度**的消耗显著压下来。⛔ 「API 单价便宜」不算 —— 那换的是钱，不是额度。

⚠️ 做这个对照实验本身要花两份额度/钱，⛔ 所以别顺手做，等有明确动机再做。

---

## ✅ G-136 · 接入验收绕开了唯一杀过人的东西（2026-08-16 修）

**事实**：`SPEC.md` §6 接入清单里唯一的验证动作是「派一个只读任务」，
⛔ **而只读单恰恰是唯一不建工位、不跑闸的那种**
（`cli.py`：`writes = args.tools != "readonly"`，闸只在 `if wt:` 里跑）。
⇒ **接入验收把闸整个绕开了，还要花订阅额度。**

⭐ 实测账单：靶子项目连败 4 单，其中 **3 单死在闸上**，不是死在作业上。
铁证：2026-08-05 那一单工人**一个文件都没动**，闸照样红。

**改法**：§6 从三步改四步，**新增**第 3 步
`devloop gates --project <新项目> --commit HEAD`（**零额度、几秒出结果**），
判据「退出码必须是 0，FAIL 必须是 0 行」。
⛔ **原只读那步降为第 4 步、原文保留，不许删** —— 它同时核对
「能跑通 / **回执模型正确** / 禁令被遵守」三样，⭐ 而闸只答得了第一样；
「回执模型核对」在整份清单里**只有这一次**。

**判据**：`tests/test_onboarding_checklist.py`（7 条）。
**红检**：① 删掉绿检那步 → 红 4 条；② 保留绿检但删掉只读那步 → 红 1 条。⭐ 两个方向各红对了地方。
⚠️ 其中一条专门守**前提**：哪天只读单也跑闸了，这几条断言就变成在防一个不存在的问题
——那时该回来改测试，⛔ 不是继续照旧前提写文档。

### ⚠️ 驳回验收员的一条建议

v2 里写「`devloop gates` 不跑 `require_pass` ⇒ 零改动全绿挡不住 SKIP/VOID 混过去」——⛔ **错**。
2026-08-16 实测：**全 SKIP / 全 VOID / 一行不打，三种都判退出码 2**（闸自身故障）。
⇒ 工具早就拦得住空转，这条判据不用加。

### ⚠️ 天花板（明说）

这几条守的是**措辞**，不是行为 —— 它是代用品。
⭐ 更稳的收口：把绿检这一步做成一个**子命令**，说明书只引用命令名
⇒ 文案漂移就**不可能**改变行为。⛔ 未做。

---

## ✅ D-PREFLIGHT-01 · 开跑前体检只装自动驾驶，⛔ 不装手工派单（2026-08-16 定）

决定与射程写在 `PLAN.md` §11 后面。⭐ 一句话：**体检自己也会错，
而它错的时候手工派单是唯一还能把活派出去的门。**

⛔⛔ 决定性实证就是 **G-134**：过去几天体检在拒本仓 **6/7 = 86%** 的计划，
⚠️ 而理由是**体检自己报不全闸名**。若手工那条路当时也被拦，工具会整个不可用。

⇒ 待办里 `S0-01` / `S0-04`（「手工派单也要先体检」）**就此关闭**。

---

## ✅ G-135 · 并行时卷宗张冠李戴、重试时互相覆盖（2026-08-16 修）

**实测**（对抗式验收里真跑 20 遍并行两单）：

| 现象 | 次数 |
|---|---|
| ⛔ 目录里**只剩一份**卷宗，另一份被静默覆盖、屏幕零提示 | **12 / 20** |
| ⛔ 「闸全过」的结论盖在一份**闸判红**的产出上（正文写的是另一单的改动） | 1 / 20 |

### ⛔ 根因是**两件事**，不是「一件事的两处」

| | 场景 | 根因 |
|---|---|---|
| ① | **并行**串卷宗 | `cli.py` 取 `telemetry.load(...)[-1]` —— **账本最后一行** |
| ② | **重试**时第一遍被盖掉（顺序跑，与并行无关，**天天走**） | `dossier.py` 用**任务名**当文件名 |

⭐ **顺序不能反**：文件名是从取到的那一行上抄下来的。先改文件名 = 把错抄的名字换个写法。
⭐⭐ 红检坐实它们确实是两件事：**各撤一处，各自只红自己那一条**。

⚠️ 而 `telemetry.py` 自己的 docstring 白纸黑字写着「**配对靠 `unit_id`，⛔ 不许靠任务名**」——
**规矩早写下来了，代码在破坏它。**

### 修法（⛔ 为什么不是「按 unit_id 回账本里挑自己那行」）

⭐ `telemetry.record` 改成**把它刚写下去的那一行交回来**，`cli.py` 直接用。
⛔ 「回头去挑」多一个失败模式：**挑不到自己那行怎么办？**
而最省事的定义（「那就还用最后一行吧」）**恰好就是这个毛病本身** —— 写下去谁也看不出来。
⚠️ 顺带少读一次整本账，而账本只增不减。

文件名 → `{stage}-{task}-{unit_id}.md`。⛔ **不是只用单号**：那样目录里是一排时间戳，
人得一份份打开才知道哪份是哪份 —— ⚠️ 而屏幕上那句「卷宗在这里」一个字都没存盘（G-133 的 6b）。

### 判据（⛔ 不靠跑并行碰运气）

守卫 `tests/test_dossier_identity.py`（4 条），**单跑一单、秒级、确定性**：
① 跑一单，在它记完账之后**必然**多塞一条「隔壁那一单」的记录 → 卷宗正文与文件名都不许沾隔壁；
② 同一份任务书**连跑两次** → 必须留下 **2 份**卷宗。
**红检**：两处根因各撤一处 → 各自只红自己那一条。⭐ 这同时证明了它们是两件事。

### ⚠️ 驳回一条验收员的建议

代理提出「把 `dossier` 从 `records.py::RUNTIME_DIRS` 拿掉，那是静默丢卷宗的直接原因」——⛔ **错**。
那个清单管的是**取指纹时剔除**，不是删文件。拿掉它会让记录守卫从第 2 单起天天误报（G-71 同款），
⚠️ 而误报会让人把守卫整个关掉，比没有守卫更坏。⭐ 已在 `records.py` 就地写明「别据此拿掉」。

### ⚠️ 顺带发现一条代用品判据（未修，记账）

`tests/test_skip_gate_after_worker_killed.py::test_跳过闸时台账记null不是零`
用的是「`_run_unit` 的**源码文本**里不许出现某个字面值」。
⛔ 我在注释里写了那几个字，它当场变红 —— **假红**。
⭐ 真判据应该落在**行为**上（造一单跳过闸的，断言台账那一格是 null）。
⚠️ 今天靠「注释里绕开那几个字」躲过去了，⛔ 那是给判据让路，不是判据在守东西。

---

## ✅ G-134 · 开跑前体检点不到闸的名字，把自己拒之门外（2026-08-16 修）

**改前实测**：本仓 7 份计划的开跑前核对 **拒 6 份**，只有一道都没点名的 `demo` 放行。
拒绝理由全是「语法 / 测试守卫 / 禁改清单 没在体检里露面」——⚠️ **而那几道本身好好的**。
⇒ 「开跑前先验判据」这道保险（G-127，为省下 514 万 tokens 而生），
⛔ **把自己变成了唯一还在拒单的东西**。

### ⭐ 判据是**双向**的（⛔ 单向会漏掉更贵的那一半）

| 方向 | 差集不空的后果 | 改前 |
|---|---|---|
| **真跑有、体检没有** | ⛔ 点名它 → **还没花一分钱就被拒** | 4 道：`语法`/`改动守卫`/`测试守卫`/`禁改清单` |
| **体检有、真跑没有** | ⛔⛔ 点名它 → 体检**放行** → **花完钱**才收「闸与验收契约对不上」 | 1 道：`环境自检` |

### ⛔⛔ 只读脚本读不出来 —— 这是本条最值钱的部分

我先按「读脚本」的办法把体检补齐，两个方向的差集**都显示归零**。
⭐ **然后真跑一次闸，当场暴露两个洞**：

- `环境自检`：体检报它，而真跑那段**只 `exit 2`，一行判定都不打**
- `改动守卫`：只在**脏工作区**那条路上以 SKIP 出现；而真跑**一律**在干净检出里
  （`cli.py::_run_unit` 与 `gates.py::run_on_commit` 都硬写 `clean_checkout=True`）
  ⇒ 它是一个**谁也没法点名的名字**

⇒ **判据要落在能直接量的东西上** —— 守卫 `tests/test_preflight_roster.py`
**真跑一次闸**再对账，⛔ 不读脚本文本。

⚠️ 中途还栽了一次：我给那三道按 `DEVLOOP_CLEAN_CHECKOUT` 分支报 VOID，
⛔ 而体检自己天然拿不到那个标记 ⇒ 永远走 VOID ⇒ 体检判定从 `ok` 掉成 `unknown`（= 谁也没验过）。
⭐ 所以那条「体检自己必须是绿的」的绿检不能省。

### 改了什么

- `.devloop/gates.sh`：体检补 `语法`/`改动守卫`/`测试守卫`/`禁改清单`；
  `闸 0 环境自检` 改成**先打判定行再退**；`闸 2` 在干净检出下也打一行 `改动守卫`
- `templates/gates.sh`（⭐ **回流**）：占位名 → 5 个真闸名 ·
  **源码目录早退挪到体检之后**（⛔ 原来排在前面 ⇒ 目录不在时体检一个字都不打，
  而「一行都没有」被读成 `unknown` = **放行**，最该拦的那种坏唯一拦不住）·
  对照物存档那块加目录存在判断（⛔ 原来无条件跑 ⇒ 没存档的新项目**每次都 BAD、永远拒单**）
- 两处 `DEVLOOP-GATES-REV` 8→9

### 判据与红绿检

守卫 6 条（`tests/test_preflight_roster.py`）。
**红检**：① 从体检删掉一道 → 红 2 条；② 把 `环境自检` 改回只 exit → 红 1 条（各红对了方向）。
**绿检**：改后本仓 **6/7 放行**；`devloop gates --commit HEAD` **6 过 / 0 未过**。

⛔ **`verify-0802.toml` 必须保持被拒**，理由是「基线守卫」——它是一份**故意点空**的计划，
用来演练「闸与验收契约对不上」。⚠️ 别为了凑 7/7 去改它或补一道假闸（守卫里已钉死这一条）。

### ⚠️ 订正一条我自己写错的

执行方案 v1/v2 里写「`pytest` 出现在 7/7 份计划，而真闸叫『测试』，是复制粘贴错误」——⛔ **错**。
`pytest` **就是本仓闸的真名**（体检真报出了它）；「测试」是**模板**里那道的名字。
两份是不同项目的闸，⛔ 不是同一个错误。

---

## ⬜ G-133 · 2026-08-16 对抗式验收带回、⛔ **本轮明确没修**的 7 条

⚠️ 都不是 G-130~G-132 引进的（是本来就在的），⛔ 但既然被量出来了就不许装没看见。
⭐ 按「会不会让人做出错误决定」排序。

| # | 缺口 | 级别 | 判据/证据 |
|---|---|---|---|
| a | **工人自己提交**时 `sha` 为空 ⇒ 走老量法 ⇒ 卷宗又报 0、台账记「白干」、后一单从旧起点重来 | P1 | 验收员在真仓上端到端跑出来：分支里明明改了 2 个文件，工具三处一致报 0。⭐ 新尺子量得出来（指着 `species.json`），只是**这条路没用它** |
| b | **改名**时 `--name-only` 只报新路径 ⇒ 「不许碰的文件被搬走了」一个字不提，还印「无越界」 | P1 | 临时仓实测：普通 `mv` 也被识别成改名；`--name-status` 能看到 `R100 旧 新` |
| c | `changed_since` 抛错时**整份卷宗不生成**，且错误原因整段被吞成 `RuntimeError` 五个字 | P2 | 端到端复现：屏幕只剩「⚠️ 卷宗没写成」。⚠️ **越出事越没证据** |
| d | 卷宗认「台账最后一行」是自己那一单 ⇒ **并行时张冠李戴**，⛔⛔ **而且两份卷宗会互相覆盖** | **P0** | ⭐ **2026-08-16 真复现了**（见下方订正） |
| e | 「成本算不出（价目表里没这个模型）」在**卷宗/交接单**两条路仍是错的归因（真相是被死线打死） | P2 | 同一归因在**自动驾驶**那条路早就修好了 —— ⭐ 又是「只修有人盯的那条」 |
| f | 卷宗末尾「你自己去核」的两条命令，分支号为空时**照样跑、吐无关结果** | P3 | g1c 那份照着跑会看到「35 个文件、−4052 行」，而真相是零改动。⛔ 不报错的错答案比报错更贵 |
| g | `test_没有基点时不许假装工人啥也没改` **名不副实**：它验的恰恰是「返回空」，而空印出来就是「一个字都没改」 | P3 | ⭐ 顺带结论：「基点为空」这条路**生产上到不了**（两条真实路径都追过），⛔ 别去修一个不存在的毛病 |

### ⛔⛔ 2026-08-16 订正（对账时实测推翻了上表两处）

**① (d) 从 P2 升 P0，⛔ 且比上表写的更严重。** 代理真复现了两遍：

| 实跑 | 结果 |
|---|---|
| A 单收工、B 单刚开跑，给 A 出卷宗 | 抓到的是 **B 那行开跑记录**（轮数/花费/结论全空），落盘文件名也变成 `stage-B.md` |
| A、B 先后收工各出一份卷宗 | ⛔ **两份文件名都是 `stage-B.md`，目录里最后只剩一份** |

⚠️ 根因有**两处**，⛔ 只修一处病还在：
`cli.py:470` 取 `telemetry.load(...)[-1]`（还可能抓到没有结论的**开跑行**）·
`dossier.py:259` 文件名取自**抓错的那一行**（`row.get('task')`）。
⭐ 台账里 `unit_id` 字段已经有了（`record_open` 与 `record` 都写），按本单编号取行今天就可行。

**② 「成本记 0 → 自动驾驶停整夜」只修了一半，⛔ 我 2026-08-16 一度报成「已修」。**

修好的那半：回执捞回来了，`cost_usd_synthetic` = 2.450258（不再是 0）。
⛔ **没修的那半**：`autopilot.py:144` 只读 `cost_usd_real`——
而那一栏靠 `prices.json` 换算，表里**没有 `claude-opus-4-7`**（只有 deepseek 两款和 `claude-opus`）
⇒ 恒为 `null` ⇒ 照样落进 `unknown_cost` ⇒ **照样把整夜停掉**。
⭐ 亲手复核：`grep cost_usd_synthetic devloop/autopilot.py` → **0 命中**，捞回来的钱它看不见。
⇒ 这半**仍是 P0**，且它就是「挂一夜」跑不起来的直接原因之一。

**触发条件**：(a) 在**允许工人自己提交**之前必须先修（今天禁令拦着，但那是软约束）；
(b)(c) 在卷宗被当成放行依据之前；(d) 在**并行真跑**之前——⛔ 它今天就成立；
(e) 顺手；(f)(g) 文字账，随时。

---

## ✅ G-130 · 卷宗的「实际改了 N 个文件」对每一单都报 0（2026-08-15 修）

**怎么发现的**：审四条「我们是不是把成功判成了失败」的指控时，
逐条量真跑残骸对应的分支。`f3-pyramid-calibrate`（2026-08-06）那一单——

| | |
|---|---|
| 工人回执 | `subtype=success` · 26 轮 · 38 分钟 |
| 工人真改了 | `game/data/species.json`：`graze_max` 0.18→0.72、`herbivore_efficiency` 0.35→0.0875 |
| 产出 | **已提交**，至今躺在 `devloop/f3-pyramid-calibrate-…` 分支上 |
| ⛔ 卷宗记 | **「实际改了 0 个：⚠️ 一个都没有」** |

**⭐ 根因：判据的维度错了——提交前该问工作区，提交后必须问提交。**
`cli.py::_run_unit` 的顺序：`changed_files()`（对） → `commit_result()`（工作区从此干净）
→ ⛔ `changed_files()` 再来一次（恒为空）→ 写进卷宗。
⚠️ **不是 f3 独有：只要产出被提交了，那一行就是假的。**

**修法**：`worktree.py::changed_since(base)` 走 `git diff --name-only base..HEAD`；
`cli.py` 改成 `wt.changed_since(base) if sha else (wt.changed_files() or [])`。
⛔ 基点是坏的时候**抛异常**，不静默返回空（「查不了」≠「没改动」）。
守卫：`tests/test_dossier_changed_after_commit.py`（4 条，红检去掉新量法 → 2 条红）。

---

## ✅ G-131 · 卷宗对人说「没点名的闸红了不影响结论」——**是假的**（2026-08-15 修）

**⭐ 从头推一遍**（`gates.py::run_gates`）：`require_pass` 那段满足之后，
最后一行是 `return GateResult(code, ...)` —— **原样透传脚本退出码**；
模板的 `bad()` 置 `fail=1` ⇒ 任何一道 FAIL 都让 code=1；
就算脚本印了 FAIL 却 `exit 0`，「自相矛盾」那段判 2。⇒ **两条路都不放行。**

⇒ `require_pass` 的语义是「**至少**这几道要过」，⛔ 不是「只看这几道」。
只有 `skip()`/`void()` 不置 `fail=1`，所以**只有没点名的 SKIP/VOID 才真的不影响**。

**实测**：验收员真跑了 **10 种退出码/判定组合**（FAIL+exit1+点名 → 1、FAIL+exit0 → 2、
`require_pass` 为空 → 1、点名一道不存在的 → 2 …），**一组反例都没有**。
⚠️ 我最初写的「唯一的变量就是它」**不准确** —— 那次红检同时动了判定和退出码两处
（模板的 `bad()` 把两者绑死了，单动一个造不出来）。⭐ 结论不变，但对照没做干净。

⛔ **为什么这条比它看起来重**：卷宗的全部意义是让人放行前不必重读 172KB 事件流。
一句「这几道你不用看」正好把人从**唯一一道真的拦下了这一单**的闸前面支开——
⚠️ 而卷宗自己的开篇就写着「**一份写得漂亮的卷宗让人跳过复核，比没有更坏**」。

**⭐ 又是那个形状（第 19 次）**：同一件事两条路——**闸怎么判**（`gates.py`，有测试盯着）
与**卷宗怎么对人说**（`dossier.py`，没人盯）。修好的永远是有人盯的那条。

**修法**：`dossier.build` 新增 `gate_verdicts`（道名→判定），`cli.py` 从 `gres.lines` 传；
没点名的 FAIL 改口为「**照样判这一单不通过**」，SKIP/VOID 单列并说明「没验 ≠ 没事」；
⛔ 编排方没传判定时**按最坏情况当 FAIL 算**。
守卫：`tests/test_dossier_unnamed_red_gate.py`（6 条）。
⭐ 其中一条**真跑一次 `run_gates`**，把两条路绑在同一个断言里——
哪天 `require_pass` 真改成「只看点名的」，那条会红，逼着同一次改动把措辞一起改掉。

---

## ✅ G-132 · ⛔ 修 G-131 时，把假话换了个方向又说了一遍（2026-08-16 修）

**怎么发现的**：G-130/G-131 落地后跑了一轮**对抗式验收**（13 个只读代理，5 个视角
各自攻一个方向 + 双镜头反驳）。⭐ 它们攻出来的，比我自己红检抓到的多。

### ⛔ ① 验过并通过的闸被写成「没验」

G-131 第一版把「没点名的闸」分成**两拨**：FAIL / 其余。
⇒ **验过而且通过**的落进「其余」，被印成「都是 SKIP/VOID——没验」。

⚠️ 这就是 G-131 那句假话的**镜像**：原来「没验的说成没事」，改完变成
「**验过没事的说成没验**」。⛔ 而 `require_pass` 缺省是空的
⇒ **每一道闸都落进这一拨** ⇒ 几乎每一单都印，
紧挨着的下一行还写着「N 过 0 未过」——**一份卷宗自己跟自己打架**。

**修法**：分**三拨**（红 / 验过通过 / SKIP·VOID），因为事实本来就有三种。
另加第四拨「编排方没传判定」，⭐ 明说「按最坏情况当红算，**但这是猜的**」。

### ⛔ ② 判据装在零件上，没装在螺丝孔上

G-130/G-131 各配 4 条测试全绿，⛔ **可 8 条全都直接调零件**。验收员实测：

```
把 cli.py 那行改回 `changed = wt.changed_files() or []`   → 818 条一条不红
把 `gate_lines=` 那整段传参删掉                            → 818 条一条不红
```

⇒ **两处修好的东西，下一个人随手改回去，屏幕全绿。**
⭐ 又是那个形状（第 20 次）：零件有人盯，**零件有没有被装上去**没人盯——
⚠️ 而 2026-08-06 真出事的恰恰是后一截。

**修法**：新增 `tests/test_dossier_callsite.py`（4 条）。
⭐ 其中一条**真跑整条 `cli._run_unit`**（假工人真改文件、编排方真提交、真闸真跑），
然后**读生成的卷宗**——⛔ 它不认任何写法。另三条是 AST 调用点检查（便宜，专抓「整段删掉」）。
**五个变异逐个重放，全部被抓到。**

### ⛔ ③ 越界那一格 100% 误报（G-130 通电才第一次开火）

`_in_scope` 拿**声明整行原文**去跟裸路径比。而真任务书写的是给人看的话：

```
- `game/data/species.json`（加 4 个 `breed_cost`）
```

⇒ 永远不相等 ⇒ 把**明明允许改的三个文件全判成越界**。
⚠️ 以前 `changed` 恒为空（那正是 G-130 修的毛病），这段**从没被真数据触发过**。
⭐ **修好一个洞会让下游另一个洞第一次开火** —— 本项目第二次撞到。

**修法**：`_declared_paths()` 按本项目惯例从反引号里抽路径（顺带滤掉 `_try_breed`
这类符号名）；抽不出来就**明说「越界判不了」**，⛔ 不许拿整行硬比。
`_in_scope` 补上 `fnmatch`（`models.py` 早写着支持通配符，实际比不了）。

### ⛔ ④ 同一道闸打两行时，前面那个红被吞掉

判定用字典存 ⇒ 后一行盖前一行。「第一次没过 → 重跑 → 过了」是很自然的闸写法，
`SPEC.md` 没有一条禁止。⇒ 刚堵上的洞从侧门原样回来。
**修法**：改传 **(道名, 判定) 的清单**，任一行 FAIL 就算红。

### ⚠️ ⑤ 非 ASCII 文件名

`git diff --name-only` 对中文名会加引号并八进制转义 ⇒ 卷宗里是乱码，
⛔ 且拿去比对会判**假越界**。**修法**：加 `-z`，按 NUL 切。

---

## ✅ G-128 · 「教训卡在靶子上没回流」第一次有了判据（2026-08-11 做）

**⛔ 用户点破的问题**（原话）：
> 「我们的主要目标是这个工具，eco-ob 是我们的手段。**我感觉你每次都在忘这一条。**
>  有什么方法可以让你做完一个阶段的、比如用 eco-ob 测试完，能自己回到正轨上来吗？」

**第一性拆解**（拆到不能再拆）：
1. 主模型跨轮次**没有持久注意力**，每轮上下文重建。
2. 能穿过重建的只有两样：**自动加载的文件** 和 **钩子注入的文字**。
3. ⛔ 而「忘了目的」的那一轮，**恰恰是不会主动去读讲目的那份文档的那一轮**。
4. ⇒ 牌子只能立在**每轮必到**的地方；⛔ 立在「已经想起来了才会经过」的地方（比如花钱闸）是维度错了。
5. ⛔ 判据**不能是文字** —— 「写下来」这条路已被实测证伪两次（T-7、G-124 本身）。字是免费的。

**⭐⭐ 而判据其实早就在仓里，只是它在撒谎。**

`doctor.py::gates_template_drift` 写的是 `if a >= b: return Check(True, ..., "与模板一致")`。
实测对 eco-ob 返回 `ok=True, "rev 7，与模板一致"` —— ⛔ **那句话是假的**（模板停在 rev 4）。
⚠️ 而它假的那个方向，**恰好就是「改进闸的办法卡在靶子上没搬回来」**。

> ⛔ **同一个形状又来了一次，这次长在工具自己身上**：
> **同一件事有两条路。修好的永远是「有人盯着」的那条。**
> 「靶子**落后**于模板」有人盯（G-114）；「靶子**领先**于模板」没人盯。
> 旁证：7 条测试全在「落后」方向，`grep -cE "领先|新于|ahead"` = **0**。

**做法**（⛔ 不建新台账、不数提交笔数、不加提交闸、不动用户全局配置）：

| 改哪 | 改什么 |
|---|---|
| `doctor.py::gates_template_drift` | `a >= b` 拆三支；抽 `gates_rev_pair()` 给体检和钩子**共用同一处判据** |
| `tools/devloop-mode-hook.py::_drift_line` | **每轮注入**：靶子领先才说话；⛔ 其余一律空串（不欠账的日子一个字都不多） |
| `tests/test_gates_template_drift.py` | 补「领先」方向 + 绿检 |
| `tests/test_mode_hook_drift_line.py` | 新建：1 条红检 + 2 条绿检 + 1 条防空转 |
| `templates/gates.sh` · `.devloop/gates.sh` | **把体检模式真的搬进去**（rev 4 → 7，PREFLIGHT 0 → 6 处） |
| `templates/constitution.toml` · eco-ob 那份 | C-6 `status = "human"` → `"partial"` + 写实射程 |
| `CLAUDE.md`（**新建**） | 目的地此前**一个自动加载的字都没有**，而手段那边有 161 行 |

**判据**：`靶子的 DEVLOOP-GATES-REV − templates/gates.sh 的 DEVLOOP-GATES-REV` **必须是 0**。

⭐ 它好在两点，缺一不可：
- 要让它归零，**必须真的去编辑 `templates/gates.sh` 的内容** —— ⛔ 写字改不动它；
- ⭐ 搬完它**自己就闭嘴**。⛔ **会自己闭嘴的告警才不是噪音**；一上来就在拦、且拦不完的告警，两周内必被关掉。

**红检 / 绿检**（都实跑过）：
- ⭐ **红检 0（最硬）**：改完守卫立刻跑，对 eco-ob **必须**是 `ok=False「领先 3 版」`。
  ⛔ 上线第一天就绿 = 假绿，当场作废。实跑：红了。
- ⭐ **绿检 0**：同一次跑，对工具仓自己（两边同版）**必须**仍是 `ok=True`。实跑：绿。
- ⭐ **红检（守卫被摘）**：把 `a > b` 改回 `a >= b` → 新测试必须红。实跑：红。
- ⭐ **收官验证**：把 PREFLIGHT 真的搬进模板之后 → `ok=True`，**注入的那两行消失**。实跑：消失了。

**⛔ 天花板，说清楚不含糊**（写进 C-6 的 `degrade`）：
① 只把模板里的数字改大而不搬内容 —— 一个字符的事，⛔ 机器分不出；
② 改了闸却**不给版号加一** —— 那时两个数相等，守卫静默绿，**这是真洞**；
③ 不落在 `gates.sh` 上的教训（进 `task.md` / `TROUBLESHOOTING.md` 的那些）⛔ 完全没覆盖。

**⭐ 上线当天它就抓到一个**：把体检模式搬进 `templates/gates.sh` 之后，我**忘了工具仓自己装的那份**。
`test_真devloop仓上这条必须是绿的` 当场红，报「本项目 rev 4，模板已到 rev 7」。⭐ 代价 0。

---

## ✅ G-127 · 开跑前先验判据本身（2026-08-09 做）—— G-126 的机制形态

**第一性：一个拓扑上的洞。**

工具的防线**全在花钱之前**拦：宪法的锚、作业格式校验、额度闸。
⛔ **唯独「判据本身对不对」要花完钱才知道** —— 它写在闸里，而闸只在工人干完之后才跑。
⚠️ 而判据恰恰是最容易写错的那一样：翻遍历史，**工人从来不是瓶颈**。

**做法**：闸多一个**体检模式**（`DEVLOOP_PREFLIGHT=1`）——
⛔ 不跑任何大考，只检查自己的前提，每道闸打一行
`PREFLIGHT<TAB>名<TAB>OK|BAD|VOID<TAB>说明`，⭐ **几秒钟**。

| 判定 | 调用方 |
|---|---|
| `ok` | 放行 |
| `bad`（闸明说前提不成立） | ⛔ **拒绝派单** |
| `unknown`（没声明 / 声明了没实现） | ⚠️ **大声喊出来，但放行** |

⭐ **端到端实证**：同一件事，昨天要 50 分钟 + 514 万 tokens 才知道；
今天 **0.55 秒**就点名 ——
「存档录于 `5e746f181a0e`，本单基准 `f7636381ab35`，**隔着 7 个提交**，
这道闸**数学上必然红**，工人做什么都过不了」。

**⛔ 做的过程里当场交了两次学费（都记下来）**

1. **第一版靠超时兜底 → 挂死 5 分钟。**
   不认识开关的闸会照常跑全套（26 分钟），⛔ 而 `subprocess` 的 timeout
   杀得掉 bash、**杀不掉 bash 起的 godot** —— 孙进程攥着管道，超时形同虚设。
   ⭐ 第一性修法：**体检必须构造上就便宜**，⛔ 不能靠超时把昂贵变便宜
   → 判据落在**一行声明**上，不声明就**一行都不跑**。
2. **第一版是两态 → 一单都派不出去**（10 条回归当场变红）。
   ⛔ 把「闸还没适配」也当成拒绝理由，⚠️ **会逼人去关掉守卫** ——
   ⭐ 这个项目最不想要的结局就是「守卫太吵，于是被摘掉」。
   → 改成三态，借既有的 **VOID（无可验之物）** 语义。

**守卫**：`tests/test_preflight_criteria.py`（8 条）。
⭐ 含一条**专钉要害**的：`test_体检必须在演练里就跑` ——
⛔ 第一版把体检插在了 `--dry-run` 返回**之后**，那等于**原样复现了要修的那个洞**。

**项目侧**：`eco-ob/.devloop/gates.sh` 已实装（`DEVLOOP-GATES-REV: 6`），
逐比特那道闸比对 `_redline/BASELINE_COMMIT` 与 `DEVLOOP_BASE_COMMIT`。
~~⚠️ 模板 `templates/gates.sh` **还没同步** —— 触发：下一个项目接闸之前。~~
⭐ **2026-08-11 已同步**（模板 rev 4 → 7，`grep -c PREFLIGHT` 模板 0 → 6 处；
工具仓自己那份 `.devloop/gates.sh` 同步跟上）。

> ⛔ **顺带记一笔学费**：上面那句「触发：下一个项目接闸之前」**不是判据，是「我会记得」的另一种写法**。
> 它在这儿躺了 3 天，⚠️ 而同期这个能力已经在靶子上省下了「50 分钟 + 514 万 tokens → 0.55 秒」。
> ⭐ 现在它有判据了：`doctor.py::gates_template_drift` 的「领先」分支 —— 见 G-128。

---

## ⭐⭐ G-126 · 只做红检、不做绿检 —— 判据可能对**所有**输入都报警

**这是 G-125 的根因，也是更值钱的那一条。**

主模型给每个新守卫都做**红检**（故意弄坏 → 必须报警）—— ⭐ 这个习惯是对的。
⛔ **但从没做过反过来的那一半**：

> **什么都不改 → 它必须是绿的。**

⚠️ 一个只验过「坏东西会报警」的守卫，**可能对所有东西都报警** ——
那和一根**坏掉的火警**没区别。而它会以「工人不行」的形式报出来。

⭐ **绿检有多便宜**：G-125 那道闸，一条命令、几秒钟，退出码就是 1。**派单前没跑过。**

**修法**：写进 `templates/task.md` 的「自己验」段（连同另外三条真烧过钱的教训）。
⚠️ **为什么必须进模板**：「不许让工人自己跑大考」这条 2026-08-04 就总结出来了，
⛔ 但只抄进两份**具体任务书**、**从没进过模板** —— 于是 08-05 复发、08-08 又复发。
⭐ **教训只有进了模板，下一个人才看得见。**

⛔ **凡是拿存档/基线当对照的判据，绿检是强制的** ——
那是所有判据里**最容易腐烂**的一种，因为「上一版」每天都在变。

**⚠️ 还没做的（触发条件写在这儿）**：
- 项目侧的闸要真的去读 `DEVLOOP_BASE_COMMIT` 并在对不上时报 **VOID**
  （⛔ 不许报 PASS 也不许报 FAIL）。**触发：下一次给任何项目接基线类闸之前。**
- 考虑让 `doctor` 顺手做一次绿检。**触发：绿检再漏一次就做。**

---

---

---

---

---

## ⭐ G-123 · 计划里的 `[stage.env]` 到不了闸（2026-08-08 实查，**未修**）

**是什么**：给 eco-ob 加第 5 道闸「逐比特中性」时，想用一个环境变量按单开关它，
于是在计划 toml 里写了 `[stage.env] DEVLOOP_BITWISE = '1'`。

⛔ **那个块工具根本不解析。** `plan.py` 里 `env` **0 命中**；
`gates.py::run_gates` 的环境是 `{**os.environ, "DEVLOOP_CLEAN_CHECKOUT": ...}`
拼出来的 —— 计划里写什么都到不了闸。
⚠️ 而 `config.py::worker_env()` 那条路是给**工人**的，不是给闸的。

**后果**：闸会**全程 SKIP**，而屏幕上一切正常 —— ⭐ 又是「判据写好了没接上」。
⛔ 这次是在派单**之前**自查到的；⚠️ 若没查，那一单会跑得漂漂亮亮而判据从没执行过。

**当时的绕法**：改用一个**入了库**的标记文件（`game/tools/_redline/BITWISE_ON`）。
⭐ 好处是 worktree 里检出得到、开关状态在 git 里看得见、要关就删文件。
⛔ 但那是项目侧的权宜，不是工具的能力。

**该怎么修**：`[stage.env]` / `[task.env]` 落地成真的 ——
`plan.py` 解析 → 传给 `run_gates` 的 env。⚠️ 沿用 `worker_env()` 已有的那套拒绝清单
（`ANTHROPIC_*` / `CLAUDE_*` / `PATH`），⛔ 别让项目配置决定闸执行哪个二进制。

**⭐ 顺带记一条更该防的**：计划里**写了但没人读**的键，工具应该**当场报错**，
⛔ 而不是静默忽略。⚠️ 一个被静默忽略的配置项 = 一个不会执行的判据，
而它长得和「已经生效」一模一样。

---

## ✅ G-124 · 任务书模板缺「先落基线、判据自己要红检」（2026-08-08 提，**2026-08-11 修**）

`templates/task.md` 的「自己验」段**没有任何一条**要求：

1. ⛔ 改动**之前**先落一个基线数字（否则「变快了」不可核对）；
2. ⛔ 判据脚本自己要做**红检**（故意弄坏一次，看它认不认）。

⚠️ 本轮两次最贵的误判都属于这一类：
- 性能事前估「降一个数量级」，实测 **28%**（因为估的那个量本来就没那么贵）；
- 对账脚本第一版**是瞎的**：把改后日志里六个真状态量改坏，它照样打「全部逐比特中性」、退出码 0。

~~⭐ 而这两条教训今天只写在 eco-ob 的过程记录里 ——
本仓 `BACKLOG.md` / `PLAN.md` / `SPEC.md` 里 **0 命中**。~~

**2026-08-11 修法**：`templates/task.md` 的「先读完下面四条」扩成**七条**，新增 ⑤⑥⑦ ——
⑤ 回归对账答不了「改对了没有」，每单必须另配**正面判据**；
⑥ 改之前先落基线数字，且跨轮次比绝对值前先用天然对照组归一化机器噪声；
⑦ 红检之前先确认「破坏真的生效了」。

> ⛔⛔ **这条自己的复发轨迹才是重点**：G-124 在 2026-08-08 就白纸黑字写着
> 「这两条教训今天只写在 eco-ob 的过程记录里 —— 本仓 0 命中」，**编了号、标着未修**，
> ⚠️ 然后**三天后又倒进 4 条新的**（正面判据 / 假红的维度 / 守卫自带洞 / 破坏没生效）。
>
> ⭐ **结论：「写进 BACKLOG 并编号」不是判据。** 它和「我会记得」是同一种东西 ——
> 两次都做到了，两次都复发。⭐ 真正管用的判据见 G-128。
⚠️ 对照：「静默降级」那条教训有 9 处命中 —— 说明不是没有登记习惯，是**这条漏了**。

⛔ 它同时能防住工人写「明显更快了」这种不可核对的报告。

---

## ⭐⭐ 2026-08-06 · **那三样「只有超时才验得到」的，昨夜自己撞上了**

⚠️ 我在 08-05 反复写过：G-106 / G-110 / G-111 这三条
**只在工人被死线打死时才执行，一次顺利的跑永远碰不到**，
⭐ 要验它们得**故意**把死线调到 1 分钟制造一次失败。

⛔ **结果第三次真派单（`h-pyramid`）自己超时了**——工人在标定时反复跑
Godot 全量回归，撞上 3000 秒死线。⭐ **那三样当场全部生效。**

### 逐条对照 08-04 那次同样的超时

| | 2026-08-04（修之前） | **2026-08-06（修之后）** |
|---|---|---|
| 屏幕上印什么 | `工人 **完** · 用了 3000s` ⛔ **一句假话** | `工人 ⛔ **断** · 超时被杀（3000s 死线——**不是它自己跑完的**）` |
| 这半截花了多少 | ⛔ **永远算不出来**（事件流被扔了） | ✅ **7,209,744 tokens**，外加额度状态与恢复时刻 |
| 接下来干什么 | ⛔ 白跑 19 分钟验一份不存在的产出 | ✅ `⏭ **闸没跑**——产出是半截的，验它说明不了任何事`，并给出人工验的命令 |
| 工人写的东西 | ⛔ 留在 worktree 里没提交，差点被 `prune --force` 铲掉 | ✅ **自动固化到分支**（`05e38599c2b8`），删 worktree 也丢不了 |
| 台账 | ⛔ **一行都没有** | ✅ 开跑行 + 收工行配成一单 |

> ⭐ **08-04 那次踩的每一个坑，这次一个都没踩。**

### ⛔ 但它同时抓到一个新的：停机理由甩错了锅

停在「**有单算不出成本**」，而说辞是：

> 「1 单的 cost_usd_real 是 null（**多半是价目表里没有那个模型**）」

⛔ **那句话在这里是错的。** 真因是**工人被死线打死、根本没交回执**，
与价目表毫无关系。⚠️ 而 `check_limits` 那条判据本身是对的
（算不出成本就停，⭐ 这次它正确地拦住了继续烧钱）——**错的只有归因那句话**。

⚠️ 这与 G-116（墙钟掐死的闸被说成「闸自己坏了」）是**同一种病**：
**判据对了，而它给人的下一步指错了方向。**
⭐ 而 `DispatchResult.timed_out` 这个结构化字段**已经存在**（G-111 加的），
⛔ 只是这条说辞没读它——**又一次「加了字段，只教会一部分读者」**。

⚠️ **未修**，登记在此。

## 🟢 G-121 · ⛔ **真跑第一分钟就抓到的**：正在烧钱的活被读成「已干完」（2026-08-05）

⭐ **这一条的价值不在缺陷本身，在它是怎么被发现的**：
DevLoop **第一次跨项目自动驾驶从头跑到尾**（25 分钟，退出码 3），
而它在**开跑第一分钟**就暴露了这个缺陷——⛔ **786 条自测一条都没抓到**。

### 症状

跑起来之后敲 `devloop halt --project C:/pg/eco-ob`：

```
没有正在跑的作业。
```

⚠️ 而那一刻它正在跑、正在烧订阅额度，工位目录也确实建出来了。

### 根因：⛔ **加了一种新账本行，只教会了一部分读者认它**

G-108 加的「开跑行」会被 `telemetry.units()` 当成一个单元返回（带 `interrupted`）。
而 `jobs.Job.done_units()`——顾名思义是「**已完成**的单」——把它也算了进去。

于是 `status()` 算出「没有 missing」⇒ 判 `done`「1 单跑完」
⇒ ⛔ `halt` 只列 `running` 的作业 ⇒ **看不见它**。

⭐ **「开跑了」不是「干完了」。** 判据落在 `interrupted` 这个结构化字段上。

### ⛔ 这是那个形状的第九次

> **同一件事有两条路。修好的永远是「有人盯着」的那条。**

G-101 · G-107 · G-112 · G-113 洞四 · G-114 · G-117 · 锚只重了 eco-ob 忘了 devloop ·
G-120（`--wait-for-reset` 只推了一个墙钟）· **本条**。

⚠️ 而 G-120 与本条**都是我在写「防止这个形状」的修法时犯的**。

> ⭐ **光记住这个形状，防不住它。**

### ⭐ 这一跑还验到了什么（第一次有真跑证据）

| | |
|---|---|
| 真死线带倒计时（G-106） | ✅ `距 3000s 死线还剩 2970s` —— 08-04 那次这里一个字都没有，正是它让人误判 |
| 开跑先落账（G-108） | ✅ 台账里有 open 行，带大白话说明 |
| 空目标报 VOID（G-119） | ✅ `○ 禁改清单：无可验之物`，⭐ 且**没有**造成假红（计划没点名它） |
| 墙钟真收窄（G-107） | ✅ 开跑印「它会真的刹住正在跑的单」 |
| 急停看得见（G-117） | ⛔ **不生效 → 当场抓到（本条）→ 修好 → ✅ 验通** |
| 复核卷宗 | ✅ **第一次真的写出来了**（08-03 做的，此前一份都没生成过） |
| 零改动判失败 | ✅ 第一次派单工人交白卷，被正确判失败并重试 |

⛔ **仍未验到**：进度条区分「完/断」· 超时留材料 · 工人死了跳过闸
——⭐ 那三样只在**工人超时**时才执行，一次顺利的跑永远碰不到。

## 🟢 G-120 · ⛔ **`--wait-for-reset` 当天加、当天被查出是废的**（2026-08-04）

⭐ **这是那个形状的第八次，而且是我在写「防止那个形状」的修法时犯的。**

墙钟有**两个口径**：

| 口径 | 谁读它 | 管什么 |
|---|---|---|
| `wall_deadline` | `plan.clamp_to_wall` | 收窄**子进程**死线（工人、闸） |
| `run.started` | `autopilot.check_limits` | 判「整批跑了多久，还能不能派下一单」 |

⛔ G-118 那一版**只推了 `wall_deadline`**。于是撞额度睡 4 小时醒来，
`check_limits` 第一句就是「已跑 260 分钟，上限 90 分钟」→ 停机。

> ⭐ **白睡 4 小时，一个字不干。**

⚠️ **最刺眼的是**：那一版我自己写的注释就是
「**墙钟也要跟着推——否则醒来第一件事就是被墙钟判超时**」
——⛔ **写下那句话的人只推了一半。**

> ⭐ **知道该做什么，和真的两处都做了，是两件事。**
> ⛔ 而「同一件事有两条路」这个形状，光靠记住它并不能防住它——
> 今天它在我**明确警惕着它**的情况下又发生了一次。

判据用 AST 找那个 `if`，⛔ 不 grep 源码（今天注释骗过一次），
并额外钉住「`check_limits` 确实读 `run.started`」这个前提
——⚠️ 否则上面那条会变成一句没人核对的话。

### ⚠️ 顺带记两条**不是缺陷、但会咬人**的

1. ⛔ **`f-feeding.toml` 已经作废两次**：① 它那一单的活 `ccb1bf8` 已经做完，
   而 `base = 'HEAD'` ⇒ 工人开工无活可干；
   ② 它 `require_pass` 点名了 `禁改清单`，⭐ 而 G-119 让那道闸在 eco-ob 上
   诚实地报 VOID（该项目不用 git 管 `.devloop/`）⇒ 退出码 **2「闸自身故障」**。
   ⚠️ **那不是改坏了，是让一句假话现形了**——以前它假 PASS。
   ⛔ 但**任何 eco-ob 的计划照抄那三个名字都会同样下场**。
2. ⚠️ **`max_dispatches` 与 `retries` 相等时，交接单永远生成不出来**：
   `check_limits` 先撞「派单次数到顶」break，`plan_next` 轮不到，
   ⭐ 那段「重试耗尽 → 写交接单」的代码走不到。
   ⛔ 而「交接单说得清楚吗」正是那份计划自己列的观察点之一。

## 🟢 G-115 ~ G-119 · 上路之前补的五条（2026-08-04，已修）

> ⭐ **这五条有一个共同形状，而那个形状本身比五条加起来更值钱**：
>
> > **同一件事有两条路。修好的永远是「有人盯着」的那条，
> > 漏掉的永远是「没人盯着」的那条。**
>
> 历史上它已经重复了七次：硬拒只装手动那条（G-101）· 三道红线只传手动那条 ·
> 模板改了副本不跟（G-114）· 台账改了写侧没改读侧（G-113 洞四）·
> 刹车装了没接到正在跑的单（G-107）· 变量塞了没告诉工人（G-112）·
> ⛔ **锚只重了 eco-ob 忘了 devloop 自己（当天第七次，我干的）**。
>
> ⚠️ 而**挂一夜走的路，恰恰全是「没人盯着」的那一条**。

### 🔴 G-115 · 早报把「你现在该干什么」那半句剪掉了（**同一毛病第三次**）

`nightly.py` 那个 `[:100]`。⚠️ G-103 白纸黑字记着这毛病犯过两次，
并且点名 `nightly.py` 是其中一次——⛔ **而它从来没被拿掉**。

⭐ 撞额度那种停机，被剪掉的正好是唯一一句自救说明：
「…恢复后重跑本阶段即可续上」。**而挂一夜之后人敲的第一条命令就是 `nightly`。**

⭐ **「你该干什么」这句话在中文里总在最后，而截断总是从后面砍**
——两者相加 = 这个截断**专门**砍掉最该留的那一句。

修法：**这一行不截**（`detail` 是工具自己拼的，长度天然有界），
外加一条判据钉住「源码里不许再出现这种硬截断」。

### 🔴 G-116 · 墙钟掐死的闸，早报说成「闸自己坏了」

G-113 洞五点过名，⛔ 而当时的修法**只写进了屏幕输出**，没写进台账。
⚠️ 而挂一夜的定义就是**没人看屏幕**——第二天早上留下的只有台账里一个
孤零零的 `gate_code=2`，早报据它印「⛔ 闸自身故障 N 单——**先修环境**」。

⭐ 于是人被指去修一个**根本没坏**的环境。

修法：台账新增 `gate_wall_killed`（⛔ 不动 `gate_code` 的语义，
加的是**为什么**）；`nightly` 分开报。
⚠️ 老行没有这个字段 → 落进**保守**那一档（当成环境可能坏了）。

### 🔴 G-117 · `devloop halt` 看不见正在跑的自动驾驶

`jobs.launch` 只在 `dispatch --detach` 那条路上被调用（它**起后台进程**），
而 `autopilot` 跑在**前台**，从不登记 ⇒ 急停回你一句「**没有正在跑的作业**」。

⛔ 半夜想叫停，只能守在那个窗口按 Ctrl-C
——⚠️ **而「挂一夜」的定义就是没人守在窗口前**。

修法：新增 `jobs.register_self()`（⛔ 不起任何新进程，只落一份 job.json，
pid 记自己），`cmd_autopilot` 开跑时调它。⭐ `status` / `halt` / `prune` 一行没改。

### 🔴 G-118 · 撞额度就停在半夜，不会自己接着跑

`dispatch` 早就有 `--wait-for-reset`，⚠️ `autopilot` **没有**：撞墙即 `Stop` + `break`。
⭐ 于是「挂一夜」实际是「挂到撞墙为止」，而订阅的 5 小时窗口多半在半夜就撞上。

修法：给 `autopilot` 也加上，⛔ 并照抄 `dispatch` 那条纪律——
**算不出精确恢复时刻的上限（周上限）不许自己等**：拿 5 小时去估会一路撞墙，
每次醒来再撞一次，⚠️ **而每次撞都真花额度**。
⭐ 等待时长要**加进墙钟死线**，否则醒来第一件事就是被墙钟判超时。

### 🔴 G-119 · 空文件夹拿到两个「通过」

实测（真跑，不是推的）：拿一个**只有空 `game/` 文件夹**的目录跑 eco-ob 的闸：

```
PASS  基线守卫      ref_*.json 未被改动
PASS  哈希常量守卫  基线哈希常量未被改动
```

⛔ **两个「通过」，而那个目录里什么都没有。**
⚠️ 因为「未被改动」在文件**根本不存在**时**恒真**。

⭐ 第一种假绿：**守卫的目标不存在**。与「禁改清单」是同一个病
——⚠️ 那一道 08-01 就在模板里修好了（G-88），而这两道**从来没人查**。

修法：两道都先问一句「它在不在」，不在就报 VOID。
⭐ 模板同步（rev 3 → 4），并把这条规矩写成一句话：

> **每一道「查有没有被改动」的守卫，都要先问一句「它在不在」。**

⭐ 双向实测：空目录 → 三道全 VOID；真项目 → 两道照旧 PASS。

---

## 🔴 G-113 · ⛔ **G-106~G-112 那批修法，自己的判据几乎全是代用品**（2026-08-04）

> ⭐ **这一条比它上面那七条都重要**，因为它讲的是「判据本身出了什么问题」。

七条修完、**761 条测试全绿**之后，五路对抗复核当场找出三个洞——
⛔ 而 761 条测试**一条都没抓到**。

### ⛔ 洞一：G-111 在真跑时**当场崩**，而它的四条判据全绿

```python
telemetry.record(..., gate_ok=(gres.passed if wt else None), ...)
```

⚠️ 判据是 `wt`，不是 `gres is not None`。G-111 跳过闸之后 `wt` 仍为真而
`gres` 是 `None` → **`None.passed` → AttributeError**。
⭐ **整条修法在真跑时是废的**，而它正是为那唯一一个场景写的。

**为什么没被抓到**：那四条判据全是 `inspect.getsource` + AST——
⛔ **源码文本是「行为」的代用品，而这次源码看着完全正确。**

### ⛔⛔ 洞二：我补的那条「真跑」判据，第一版**照样抓不住**

补了一条真跑 `_run_unit` 的测试之后，拿有 bug 的实现红检——**它还是绿的**。两个原因：

| 我写的断言 | 为什么分辨不出 |
|---|---|
| `assert not ok` | ⛔ `AttributeError` 被 `_run_unit` 外层的 `except Exception` **吞掉**，这一单照样记成失败 |
| `committed.get("gate_ok") is None` | ⛔ **「压根没调用过」与「调用了并传 None」返回同一个值** |

⭐ 能分辨的只有两条：**出没出异常**、**`commit_result` 那个键在不在**。
改完之后红检双向都对：有 bug → 红并点名 `AttributeError`；修好 → 绿。

> ⚠️ **同一条纪律，我在同一场会话里连续违反了三次**——
> 一次写实现（G-111），一次写判据，一次写判据的判据。
> ⭐ 「判据要落在能直接量的东西上」不是一条读一遍就会的规则。

⭐ 顺带：那条真判据**当场又抓到一条**——卷宗那段也假设 `gres` 存在，
被它自己的 try 吞成一句「⚠️ 卷宗没写成」。⛔ 于是**最需要复核材料的那种单**
反而永远没有卷宗。

### ⛔ 洞三：我写了一句**假注释**，而我几小时前刚修掉同一类的另一句

新写的注释宣称 `--resume` 时墙钟会从上一轮的起点续算。
⚠️ 而 `Run(sp, started_at, time.time())` 的第三个参数**永远是「现在」**。

⭐ 与我当天刚从 `cli.py:171` 删掉的那句「工人有 max_turns 上限但**没有硬性时限**」
是**同一类错**。⛔ **它会重犯**，所以现在有一条判据专门盯着它。

⚠️ 语义本身**有意保留**为「每次调用重新计时」——`--resume` 的主要用途就是
「撞额度→等几小时→接着跑」，改成「本阶段总共」会让它开跑即超时。
⭐ 但它必须**说出口**：续跑时现在会印一行「墙钟从现在重新计时」。
**一个没说出口的语义就是一个陷阱。**

### ⛔ 洞四：台账新格式漏改了两处读者

| 漏改处 | 后果（实测） |
|---|---|
| `jobs.py::done_units` | 一个被强杀的作业报「**2 单跑完了**」，而它本该报「进程已不在，只完成 1/2」——⛔ **急停从此看不见还在烧钱的活** |
| `telemetry.summarize` | 台账里只要有一单被半路杀掉，`devloop stats` **直接 KeyError 崩掉** ——⭐ 出事第二天早上你最想跑的命令，恰恰是唯一会崩的 |

⚠️ 还有一条更细的：「各档相加恒等于总数」这条不变式在 `gate_buckets` 那层成立，
⛔ **在人真正会看的 `summarize` 那层不成立**——「被中断」这一档根本没印出来。
⭐ 不变式要在**人眼前那一层**成立才算数。

### ⚠️ 洞五：墙钟掐死的闸被记成「闸自身故障」

被收窄之后的闸超时走 `code 2`，而 `code 2` 的下游处置是「**先修环境**，别看下面的绿」。
⛔ 于是第二天早上人被指去修一个**根本没坏**的环境。
⚠️ 这与本项目一直在打的「把闸坏了说成活没干好」是同一枚硬币的另一面，方向反了。

### ⛔ 两处文档写错了事实

1. **复盘正文说工人「跑了十几遍」全量回归**——⛔ 算术上不可能：
   写完代码后只剩 2756 秒，而单次 486~905 秒 ⇒ **最多 5 遍**。
   ⚠️ 而且那次的事件流已经被丢掉了（那正是 G-110 说的事），⭐ **这个次数无处可查**。
   ⛔ 本台账下面就记着一模一样的教训（「后台跑完 12 分钟」→ 实际 8 分 47 秒）。
2. **eco-ob 任务书让工人去补 `energy_density`**——⛔ 那个字段 `2667fbb`
   已经补进去了（14:50:35，**比那一跑早半小时**），文件里还留着注释说明它在等这一单。
   ⚠️ 任务书是当天 18:49 重写的，重写时没回头核对。
   ⭐ 本项目自己的话：「**工人执行完全正确，错的是任务书的前提**」。

---

---

## 🟢 G-114 · 模板修好了，装出去的那份从来不跟（2026-08-04，已修）

⭐ **这一条是 G-88 的第二次发作**，而它证明了一件更一般的事：
**「修在模板里」和「修到项目上」是两件事，而没有任何东西盯着后者。**

### 实测

| | |
|---|---|
| 工具模板 `templates/gates.sh:148` | `void "禁改清单" "…无可验之物"` ✅（2026-08-01 修的） |
| eco-ob 装的那份 `.devloop/gates.sh:75` | `pass "禁改清单" "…符合预期"` ⛔ |
| eco-ob 里 `void()` 这个函数 | ⛔ **压根没有**——整个 VOID 判档从没装进去 |
| `f-feeding.toml` 的 `require_pass` | ⛔ **点名了这道闸** |

⚠️ 也就是说：那一单跑完，屏幕上会有一行「禁改清单 通过」，
⛔ 而它冠名要验的东西（`.devloop/`）在 eco-ob 里**根本没被 git 跟踪**，
它验了 **0 条**。⭐ 第一种假绿，长在计划最倚重的那道闸上，多活了三天。

### ⛔ 判据不能是「必须和模板一模一样」

⚠️ 项目本来就该改自己的 gates.sh——那是它自己的验收命令。
⭐ 判据落在**版本代号**上：模板带 `DEVLOOP-GATES-REV: <n>`，
装出去的那份记下它源自哪一版，`doctor` 比对。

⛔ **「不知道」不许当成「没问题」**：老副本没有代号，那时的回答是
`ok=None` +「不知道它基于哪一版」——⚠️ 正是**沉默**让那个假绿多活了三天。

### 顺带确认

devloop 自己那份**故意没有** VOID 那一支，而那是对的：
本仓的 `.devloop/` 被 git 跟踪（`git ls-files .devloop` 有输出），
那道闸在这里真有东西可验。⛔ 照抄模板反而会造出一条永远走不到的分支。

⚠️ 两份 gates.sh 都在各自宪法的锚里——⭐ 改完必须由**人**重新锚定。

## 🟢 G-106 ~ G-112 · 首次跨项目真派单查出来的七条（2026-08-04，已修）

> **⭐ 这一批的价值不在缺陷本身，在它们是怎么被发现的**：这是 DevLoop 第一次
> 在**别的项目**上真跑一单（`%auto C:/pg/eco-ob f-feeding`）。
> 之前 13 个提交、715 条测试，**一次真派单都没有**——按项目自己的口径，那不叫能用。
>
> ⚠️ 而这一跑**没跑完**：主控读错了自己的日志，在工具还差 11 分钟就会自己停下时
> 用 shell `timeout 4200` 把它掐了。⛔ 读错的根因就是 G-106。

### 那一跑的真实时间线（⛔ 与主控当时的理解完全不同）

```
15:18:37  worktree 建好 · 已同步 game/.godot      ← ✅ 跨项目缓存同步生效
15:18:37  工人 开始（预计 300s）
15:22:42  工人最后一次写文件                        ← ⭐ 写代码只用了 245 秒
   …此后 2756 秒（45 分 56 秒）：工人自己反复跑 Godot 全量回归…
      ⚠️ **跑了几遍无从查证**——事件流被丢了（那正是 G-110）。
      ⭐ 按单次 486~905 秒算，上界是 5 遍。⛔ 别写「十几遍」，那个数没有出处。
16:08:38  工人 完 · 用了 3000s                     ← ⛔ 其实是被 3000s 死线打死
16:08:39  闸 开始（预计 900s）                      ← ⛔ 明知没回执还起闸
16:27:39  闸 仍在跑 · 已 1140s
===退出码=124===                                   ← ⛔ 主控掐的
```

**决定性证据**（判据落在能直接量的东西上）：`.devloop/reports/` 里 **08-04 零文件**。
而 `dispatch._finish` 的**每个分支**都会落盘 ⇒ 那一跑走的是 `dispatch.py` 提前
return 的超时分支 ⇒ 工人从未交出回执。

---

### 🔴 G-106 · 进度条把「超时被杀」「抛异常」都印成「完」

`progress.py::phase` 的收尾行写在 `finally:` 里，**无条件**印「完」。实测三种结局：

```
正常 完 · 用了 0s      ← 真干完
超时 完 · 用了 0s      ← 子进程被杀
炸了 完 · 用了 0s      ← 阶段内抛异常
```

⭐ **第一种假绿长在给人看的实时视图上**——那个字与任何可观测事实都不对应。
⛔ 它骗的不是测试，是操作员，而且当场骗到了：主控读到「完」→ 判定工人干完了
→ 把随后 19 分钟的闸解释成「工具卡住」→ 掐掉整跑。

**两条路都堵**：① 抛异常（⚠️ 用 `except BaseException`——`KeyboardInterrupt`
不是 `Exception`，而 Ctrl-C 是人中断夜跑最常用的方式）；
② 异常被上游吞成返回值（`dispatch.py` 把 `TimeoutExpired` 转成带 error 的返回值）
——⭐ 这条只能由调用方主动报，所以 `phase()` 现在 yield 一个 `Phase` 结局槽。

**顺带**：真死线必须和参考值一起印。那一跑屏幕上只有「预计 300s」，
而真正会杀进程的是 3000s，两个数写在不同文件里、毫无关联。
⛔ `cli.py:171` 那句注释「工人有 max_turns 上限但**没有硬性时限**」是**错的**，已删。

### 🔴 G-107 · `max_wall_min` 刹不住正在跑的单，而且那个数先天到不了

计划写 `max_wall_min = 60`，实跑 80 分钟。两层：

1. `check_limits` 的墙钟那条在「派下一单**之前**」问（函数 docstring 原文）
   ——一单开跑后那 80 分钟里**没有任何东西看表**；
2. 工人死线 3000s + 闸死线 1800s = **80 分钟** > 60。

⛔ **我的第一版修错了方向**：做成「装不下一单就拒绝开跑」。
⚠️ 那是在**否决一个数**，而该做的是**让那个数变成真的**；而且它会误伤
（闸的 1800s 是天花板不是预期），实测当场把 `test_chain.py` 三条打红。

⭐ **正解**：`dispatch_one` 和 `run_gates` **本来就都收 timeout 参数**——
按剩余墙钟收窄它们（`plan.clamp_to_wall`），刹车就接上了。
新增 `gates.GATE_TIMEOUT_S` 模块常量：⛔ 印出来的死线和真生效的死线必须同源。

### 🔴 G-108 · 进程被杀 / Ctrl-C 时台账一行都不留

台账跑前 10 行、跑后**还是 10 行**。⛔ 不是「兜底没兜住」，是**两条记账路径
都排在整单最后**：主路径在工人+闸+固化全做完之后，补记在 `except Exception` 里。
⚠️ 强杀不走 `except`；Ctrl-C 也不走（`KeyboardInterrupt` 是 `BaseException`）。

⛔ **改成 `finally` 没有用**——强杀时 `finally`/`atexit` 一行都不执行。
⭐ 要动的是**时刻**：`telemetry.record_open()` 在 `dispatch_one` **之前**落一行。

配套（⚠️ 缺一条就出事）：
- `telemetry.units()` 把「开跑行 + 收工行」收成一单——⛔ 按行数算会把一单数成两次，
  计划写最多派 2 次、跑完**一单**就被自己的刹车停掉；
- `gate_buckets` 加**独立的**「被中断」档——⛔ 折进「没跑闸」或「无判定记录」
  会让这件事消失在别的数里，而「各档相加恒等于单元数」那条不变式必须保住；
- `nightly` 点名报它。⚠️ 2026-08-04 第二天早上那条命令答的是「✅ 没问题」。

⭐ **判据第一次真的杀了一个进程**：`Popen.kill()` + 先断言 `finally` 确实没跑
（⛔ 否则测的是别的东西），再数台账。此前这条铁律的「测试」是
`inspect.getsource` + 字符串断言——**断的是源码文本，不是行为**，
于是它一次都没被机器验证过。

### 🔴 G-109 · `prune` 把「有未提交改动的 worktree」判成「可清理」

真现场原文：

```
可清理：
  devloop/f1-feeding-formulas-20260804-… @ 2667fbb —— 已合并进主线
  git worktree remove --force C:/pg/.devloop-worktrees/eco-ob-f1-… && git branch -D …
```

⭐ 它把工人 50 分钟的活判成「可以删」，还把 `--force` 命令替人拼好了。
⛔ **而它这么判的理由，正是「产出没保存」这件事本身**：一个提交都没有
⇒ 尖端 == 基准 ⇒ 从 HEAD 可达 ⇒ `git branch --merged` 收录它。

> **判据落在了代用品上。** `merged` 是「产出已别处留存」的代用品，
> 而它在最要命的那一种情形下**正好反过来**。能直接量的是 `git status --porcelain`。

修法：`Branch.dirty`（⚠️ 三态：没工位 / 问出来的条数 / **问不出来**——
⛔ 「不知道」不许当成「干净」），脏工位一票否决。
⭐ 顺带修头行：`_files_changed` 只数**已提交**的差异，于是那半截活在账上是
「0 个文件」（实测原文「待处理产出：1 个分支合计改动 0 个文件」）。

### 🟠 G-110 · 工人被死线打死时，它已经吐出来的东西被整个扔了

```python
except subprocess.TimeoutExpired:
    return DispatchResult(task.name, None, None, error=f"工人超时（{cfg.timeout_s}s）")
```

⭐ `TimeoutExpired` 手里就攥着 `.output` / `.stderr`——**其中就有 usage 事件**。
⛔ 一眼没看就扔了，于是那 50 分钟花了多少**永远算不出来**。

⚠️ 最刺眼的是**不对称**：同一个函数里另外两种失败（无 result 事件 / 回执不合契约）
**都**把原始流存成 `.raw.txt`，唯独最该留材料的这一种不留。

修法：`_keep_scraps()` 统一处理「钱已花掉但拿不到合规回执」的所有路径
+ `quota.sum_tokens()`（⛔ 一条 usage 都没有时返回 **None 不是 0**）。
⭐ AST 判据顺带抓出**第三处**同类漏洞（`_finish` 抛异常那条，注释写着
「钱已花掉」而照样一个字不留）。唯一豁免 `FileNotFoundError`——进程根本没起来。

⚠️ 差点埋的雷：措辞里**必须留着「超时」二字**，`autopilot.py:445` 按
`"超时" in e` 归类失败模式，换个说法会让它静默掉进「其它」。已加判据钉住。

### 🟠 G-111 · 明知工人死了，还花 19 分钟去验那半截产出

16:08:38 工具已经知道没有回执，16:08:39 照样起了一道最长 1800 秒的闸。

⚠️ 与既有规矩「宪法命中也照样跑闸」**不冲突**——那条管的是**干完了但碰了红线**的
产出，闸对它有话说；这里的产出是**从中间被截断**的（可能改完 A 还没改 B），
对一个中间态求值什么都不说明。⭐ 更要紧的是反过来的风险：**半截的改动碰巧过闸**。

判据用结构化的 `DispatchResult.timed_out`，⛔ 不许 match `error` 的自由文本
（本项目在闸的归类上栽过一次「会被测试名劫持」）。

⭐ **短路不许把路堵死**：worktree 留着、人自己跑闸的命令印出来，
而且**产出照样固化**（`gate_ok=None` = 「未跑闸」）——⛔ 不提交正是 G-109 的成因。

### 🟠 G-112 · 环境变量注入了，却从没告诉工人它叫什么

2026-08-01 修的是「塞变量」那一侧（`[worker].env` → `GODOT_BIN`），
⛔ 漏的是「告诉它」那一侧。⚠️ 于是 08-04 工人上来照样敲 `godot`、照样报
找不到，然后手工翻目录——**同一个坑原样又踩一次**。

`build_prompt` 现在把 `worker_env()` 印进稳定前缀（⚠️ 排序，⛔ 否则缓存归零）。

---

### ⚠️ 不属于工具缺陷，但它才是那 46 分钟的真因

任务书「自己验」一节要求工人跑全量回归并汇报 `M1 回归` 的结果。
⭐ 工人照做了，而 eco-ob 单次全量回归要 486~905 秒——**它把 2756 秒全花在这上面**
（⚠️ 具体几遍查不到，事件流被 G-110 丢了；按单次耗时算上界 5 遍）。
跑完之后，闸又把同一套测试从头跑一遍。**同一件事花三份时间。**

⛔ 已改任务书：禁止工人自己跑全量回归，只允许 `--check-only` 语法检查；
守恒自查改成**用算的不用跑的**；⛔ 明确「验收是闸的事，不是你的事」。

---

## 判定标准

| 级别 | 含义 |
|---|---|
| **🔴 阻断** | 挡住下一阶段，必须先修 |
| **🟠 前置** | 不挡当前，但在某个明确动作之前必须修 |
| **🟡 择机** | 不影响正确性，攒着一起修 |

---

## 索引（状态只在这里维护，条目按编号固定不动）

> **为什么是索引而不是分节**：此前用 `开放缺口` / `已关闭` 两个标题分节，
> 每次状态变化都要搬动条目，已经漂移两次——最严重的一次是 **G-38（HIGH，
> 触发条件「Phase 4 收尾前必须完成」）被埋在「已关闭」标题下**。
> 按标题扫描的读者必然漏掉它，那正是这份文档存在的理由被抵消。
> 改成索引后，状态只有一处要维护，条目位置固定，交叉引用不会失效。

| 状态 | 编号 | 严重度 | 是什么 |
|---|---|---|---|
| ✅ 已闭 | [G-97](#g-97) | HIGH | 锚存**磁盘字节**的 SHA-256，而 `autocrlf=true` 下 git 存 LF → 受保护文件**改过之后再用 git 还原**（`checkout --` / `stash pop` / `reset --hard`），git 重写它、写成 CRLF → ⛔ A-1 报「受保护文件与锚不符」，而**内容一个字没变**。⚠️ 我为此判错了两次（先太宽、再太窄），最后在真仓上实地撞到才定准。修法：`_sha` 先归一行尾 |
| ✅ 已闭 | [G-105](#g-105) | HIGH | ⛔ **差异表在人按下批准的那一刻对他说了假话**：5 个明明在旧锚里的文件被印成「（当时不存在）」、时间戳印成「（读不出来）」、一个新旧指纹**完全一样**的文件被列进「你要批准的东西」、同一个文件出现两遍。⭐ 而那张表存在的唯一理由就是让人看清自己在批准什么 |
| ✅ 已闭 | [G-104](#g-104) | HIGH | ⭐ **同一个形状第三次**：`.devloop/dossier/` 没进 `.gitignore` → 卷宗每单一份写进项目 → `git status` 看得见 → 宪法 A-2 从**第二单起每单命中**（`ws_before` 整阶段只采一次）→ 一夜十单后九单全是误报。⚠️ 与 G-93 一模一样，而 `dossier.py` 开头还写着「我避开了 G-93」 |
| ✅ 已闭 | [G-103](#g-103) | HIGH | 工具停着，而它印给人的**唯一一条自救命令被砍成半句**（`stderr[:200]`，屏幕停在 `constitution anchor --pro`）。⚠️ `nightly.py` 犯过同一个毛病（砍 100 字），⭐ 两次都恰好砍在「你该干什么」那句上——**那句话总在最后** |
| ✅ 已闭 | [G-102](#g-102) | HIGH | 三档的账是**做平的**不是真平的：同族豁免让 C-1a 借 C-1b 的名额过关（三档相加 17、散文 18，⛔ 而守卫这条的测试是绿的）；`by` 判**字数**（9 个字废话能过、指向不存在的代码也能过）；`[[protected_ref]]` 是静默后门（统计算它、`load()` 从不解析它） |
| ✅ 已闭 | [G-101](#g-101) | HIGH | 硬拒 `full_tools_when_detached` **够不着自动驾驶**——`refuse_preflight` 唯一调用点在 `cmd_dispatch`，计划里写 `tools="full"` 就能让工人整夜握着 WebFetch。⭐ 根因是参数叫 `detach`（一个只存在于另一条路的开关） |
| ✅ 已闭 | [G-100](#g-100) | CRITICAL | 工人 `git push origin HEAD:refs/heads/x` **推送成功而宪法 0 命中**——`check_refs` 只遍历 `before.refs`，T0 时不存在的 ref 天然隐身。⛔ 而 toml 声称挡它的是一条静态检查（扫工具自己的源码），**判据的主语错了** |
| ✅ 已闭 | [G-99](#g-99) | CRITICAL | 工人改闸 + 废掉已有测试 + `git commit` → 闸报 `PASS 测试守卫 tests/ 未被改动`、`PASS 禁改清单 .devloop/ 未被改动`（**两句都是假的**），宪法报「无宪法命中」。⛔ `.devloop/**` 与 `tests/**` 一条都没进 `[[protected_tree]]` |
| ✅ 已闭 | [G-98](#g-98) | HIGH | `constitution anchor` 是**盲签**：不判断锚是否已存在、不打印新旧差异、不要确认、不备份旧锚。⚠️ 整套设计依赖「人是那道闸」，而那道闸被蒙着眼睛——用户当天就是这么按下去的 |
| ✅ 已闭 | [G-96](#g-96) | MED | 新增第三档 `[[judged_elsewhere]]` 后，10 个 `ConstitutionResult` 构造点**只给 2 个传了**（还都是故障路径）→ TOML 登记了、测试全绿，⛔ 而人读到的那句尾巴**一次都没提过第三档**。另 `check_workspace` 传 `[]` → 真命中时印出「未登记任何『判不了』的条款」这句**假话** |
| ✅ 已闭 | [G-95](#g-95) | MED | 给 `ConstitutionResult` 加字段时**插在了 `stderr` 前面** → 所有按位置传参的调用点整体错位，`"受保护文件与锚不符"` 被当成条款表**逐字拆开**，印出「另有 9 条判据在代码里（受、保、护、文、件、与、锚、不、符）」。⚠️ 647 条单测全绿——⛔ 又是实跑一条命令才看见的 |
| ✅ 已闭 | [G-94](#g-94) | HIGH | `--discard` 写好了但**控制流够不着**：`if not args.delete: return 0` 排在它前面 → 单给 `--discard` 时一支没删还印「只归档了」。⚠️ 同时给 `--delete` 才碰巧走得到（`c195256`） |
| ✅ 已闭 | [G-93](#g-93) | HIGH | `.devloop/findings.jsonl` 没被 gitignore → `cmd_audit` 一写它活工作区就变，而 `ws_before` **整阶段只采一次** → 从第 2 单起每单命中宪法 A-2，**全阶段连坐**（`863af0f`） |
| ✅ 已闭 | [G-89](#g-89) | HIGH | 一单里多个失败原因**后写覆盖前面全部** → 宪法命中 + 闸 FAIL 时，「需要人批准」整档丢失，自动驾驶拿满 retries 去撞一堵要人开的门（`f559621`） |
| ✅ 已闭 | [G-88](#g-88) | HIGH | `templates/gates.sh` 第 130 行**逐字**就是 G-68 那条假绿；协议块还写着「工具只认这三样」——⛔ 每个照模板接入的新项目都复现同一个事故（`ab7ac6a`） |
| ✅ 已闭 | [G-87](#g-87) | HIGH | `exit 1` 却一行 FAIL 都没印 → 判 1，建议「看是哪一道没过」而一道都没有；retries 拿空失败反复烧额度。与 G-84 对称的另一半（`cfc178e`） |
| ✅ 已闭 | [G-86](#g-86) | **CRITICAL** | 失败归类靠对 `error` 子串匹配 → **被测试名劫持**：`test_闸自身故障要归到闸自身故障` 挂掉就让「活没干好」归成「先修环境」。⭐ 台账加结构化 `gate_code`（`3c4729b`） |
| ✅ 已闭 | [G-85](#g-85) | HIGH | 三条代理判据能被绕过、缺陷 100% 复活而全绿；且「要验它得跑真单」这个前提**本身是错的**——零额度 E2E 台架已证明（`011d33b`） |
| ✅ 已闭 | [G-84](#g-84) | **CRITICAL** | 闸印了 FAIL 却 `exit 0` → **放行**（passed=True 而摘要写着「1 未过」），坏产出直接流下去（`1815337`） |
| ✅ 已闭 | [G-83](#g-83) | HIGH | `prune` 的 `empty` 参与删除判定 = **数据丢失通道**：`empty=True ∧ merged=False` 那格会给出 `git branch -D` 删掉唯一一份未合并产出。L4 是误判的活样本（`7f80da5`） |
| ✅ 已闭 | [G-82](#g-82) | HIGH | 刹车读台账走的是 `telemetry.load` **点名批判过的那份裸列表推导**，三处（`read_progress`/`_rows_for`/`jobs.done_units`）（`f71c79e`·`ab9dae2`） |
| ✅ 已闭 | [G-81](#g-81) | MEDIUM | `dispatch --why-parallel` 的提前返回**够不着**——argparse 的 `required=True` 先拒了。文档里印的命令照抄就报错（`f7ab616`） |
| ✅ 已闭 | [G-80](#g-80) | HIGH | `check_disk` 的 `if s > 0` 把「空壳」「量不出来」「合法小值」压成一档，前两种退回写死的 200MB；⛔ 常数已删（`da385dd`） |
| ✅ 已闭 | [G-79](#g-79) | HIGH | 点名的闸 FAIL 被判成「闸自身故障」；`summary()` 在故障路径上吞掉全部行级明细；归类链里「闸自身故障」是死代码（`f60ec06`） |
| ✅ 已闭 | [G-78](#g-78) | **CRITICAL** | 并行波次冲过 `max_dispatches`（溢出 = 并发数 − 1）——订阅制后端上「烧了多少」这个维度唯一还活着的刹车（`4b8ca7b`） |
| ✅ 已闭 | [G-77](#g-77) | **CRITICAL** | `audit.append` 整文件重写触发宪法的只追加判据 → 一批里第 2 单起全部连坐；且无锁，8 路并发只活下来 1 条（`2e13266`） |
| ✅ 已闭 | [G-76](#g-76) | **CRITICAL** | VOID 判决让 CLI 崩在 `commit_result()` **之前**，工人干完的活直接丢掉；`_MARK` 还撞了名（`2e13266`） |
| 🟠 未闭 | [G-90](#g-90) | MEDIUM | 跨 `--resume` 的配额放大：`fired` 只活在进程内，台账掉行后一次 resume 实测派 17 次 vs 上限 10 |
| 🟠 未闭 | [G-91](#g-91) | MEDIUM | 台账非法 UTF-8 落在**字符串里**时静默通过（`�` 让 JSON 仍然合法），字段已损坏而 `load` 看不见 |
| 🟠 未闭 | [G-92](#g-92) | LOW | `_HEADROOM_MB = 500` 无任何实测依据，而删掉 200 之后它成了**唯一兜底** |
| ✅ 已闭 | [G-71](#g-71) | HIGH | 记录守卫把 **autopilot 自己的运行记录**误判成「抹除」→ 每次自动驾驶跑到第 3 单必失败。⭐ 真跑抓到（`26cd678`） |
| ✅ 已闭 | [G-70](#g-70) | **CRITICAL** | 退出码判据是 `todo` 而非 `halt.tripped` → **额度在最后一波撞上时被报成「成功」**，上游会以为这批活干完了（`c1fc0e0`） |
| ✅ 已闭 | [G-69](#g-69) | HIGH | 模式 C 的令牌 TTL 从 `%auto` 起算 → **演练得越认真，用户的批准有效期越短**；`granted_at` 字段本来就写着但闸从不读 |
| ✅ 已闭 | [G-68](#g-68) | MEDIUM | 闸只有 PASS/FAIL/SKIP 三档 → 「我没东西可验」只能报 PASS，**点名一道恒过的闸等于没点名**。新增 VOID 档（`c20e26f`） |
| ✅ 已闭 | [G-67](#g-67) | MEDIUM | 触发代号打在句子中间时**静默失败**（零回显）——用户以为放行了，模型收到的是模式 A（`6ced08f`） |
| ✅ 已闭 | [G-66](#g-66) | **HIGH** | 自动驾驶不支持并行（`t = ready[0]`）→ **按波派单**（2026-08-02）。⭐ 判据在 `plan.load()` 执行——那是唯一还没花钱的时刻；⛔ `chain` 与 `parallel` 互斥 |
| 🟠 未闭 | [G-65](#g-65) | HIGH | **撞额度停批从未真触发过**（台账 `ratelimit` **0 次**）；真跑到的只是 `allowed` 解析路径 —— 怎么在不真撞的前提下验它 |
| 🟠 未闭 | [G-64](#g-64) | HIGH | 「以为免费其实收费」：`doctor` 已挡进闸（`0e2518e`）；~~⛔ 但 `--probe` 仍零行台账~~ **已补**（2026-07-30，`credentials.probe(project=)` + `doctor` 把 project 传下去，测试 `test_两条探针路径都必须留台账`）；⚠️ 仍未闭：**别处还有没有**同类「以为免费其实收费」的路径，没系统查过，`devloop eval` **闸不拦且零行**——同一个形状没查完 |
| 🟠 未闭 | [G-63](#g-63) | MEDIUM | `!` 是 CLI 的 shell 模式前缀，`!task` **根本到不了模型**（已换 `%`）；⛔ 残余：闸的 deny 文案仍在教用户打 `!` |
| ⚖️ 取舍 | [G-62](#g-62) | — | 模式触发的护栏**不是密封舱**：钩子 fail-open（已真发生：15 条断言静默变放行）· 自我授权判据是字符串匹配 · 分段是启发式 |
| 🟠 未闭 | [G-61](#g-61) | MEDIUM | 测试守卫的彻底版（拿基线那份 `tests/` 跑工人的代码）⚠️ 原误编 G-58（撞号），2026-07-30 改号 |
| ✅ 已闭 | [G-60](#g-60) | HIGH | `needs` 只排序、后一单看不见前一单的产出 → `[stage] chain`（`64042ed`，⛔ 默认关）；⚠️ 接力点重启丢失 2026-07-30 才补（`0e2518e`） |
| ⭐ 记录 | [G-59](#g-59) | — | **宪法上报链路首次真实演练**：闸全绿而宪法命中，整单判失败、产出不丢、主线未动。信任爬梯档 2 的前置条件补上了 |
| 📌 记录 | [G-58](#g-58) | — | 两个真实项目接入宪法；eco-ob 当场补了「纯散文守卫」与「自提交绕过闸」两个洞；p4 的「没有闸」是取舍 |
| ⚖️ 取舍 | [G-57](#g-57) | — | **便宜路线停用为默认**（又花钱又差，实测）；框架与能力全留；⚠️ 代价是写操作暂无执行者 |
| 📌 记录 | [G-56](#g-56) | — | 第三轮审查 39 条分三批修完；元教训：6 条是「实现了但没接线」，全都有绿测 |
| 📌 记录 | [G-55](#g-55) | — | Phase 7：宪法（散文→机械判据）+ 阶段计划 + 四条失控防线；LangGraph 未接，理由与触发条件在条目里 |
| ✅ 已闭 | [G-54](#g-54) | CRITICAL | **分支劫持**：工人改一下 worktree 的 HEAD，产出就落到用户主线，而工具报「已固化到隔离分支」 |
| ✅ 已闭 | [G-53](#g-53) | CRITICAL | **闸可以一条都没验却报全过**；一个环境变量能把真实验收变成空转 |
| 📌 记录 | [G-52](#g-52) | — | 评测清洗后的干净基线；评测侧漏了 G-46 的轮数订正，三题撞顶被当成答错 |
| ✅ 已闭 | [G-51](#g-51) | CRITICAL | Phase 6 三路审查：--detach 的 argv 反推、进度按行数计、status --all 恒 0、评测答案落进被考仓库 —— **已修** |
| 📌 记录 | [G-50](#g-50) | — | 急停落地（默认只列不杀 · 说清钱不退 · 杀进程树）+ 四条自查裁决 + 两作业串台账的取舍 |
| 📌 记录 | [G-49](#g-49) | — | Phase 6 非阻塞派单：7 单前台干等 18.1 分钟 → detach 1 秒返回（数字已订正） |
| 📌 记录 | [G-48](#g-48) | — | 评测集红测成立：弱模型掉分且掉在 undecidable；flash 省 3.6 倍 |
| 📌 记录 | [G-47](#g-47) | — | Phase 5 评测集首跑 19/20；唯一错的那题是「用可验证的半句为整句背书」 |
| ✅ 已闭 | [G-46](#g-46) | MEDIUM | 轮数天花板默认 30→120。**预测器方案已被自己的数据否掉**，正解是「没用掉的轮数不花钱」 |
| 📌 记录 | [G-45](#g-45) | — | 「稳态预付为 0」实测成立，但判断力侧不为 0（3/7 重跑） |
| 📌 记录 | [G-44](#g-44) | — | 拆单成本三桶拆分：每批必付 $0，稳态第 1 单回本（推算未实测） |
| ✅ 已闭 | [G-42](#g-42) | CRITICAL | token 去重取首帧而非末帧，子代理成本低估 9–30 倍（已对外报错数字，已订正） |
| ✅ 已闭 | [G-41](#g-41) | MEDIUM | 并行竞态：已换 naming.stamp（pid+计数器），红测 4 路同名并发 4/4 通过。⛔ **但表里「并发写台账未损坏」那条结论 2026-07-30 被推翻**（40/40 丢行，修法见条目内订正） |
| 📌 记录 | [G-38](#g-38) | — | 贵模型对照臂**已跑**，结论为负（转为持续测量项，见 RESULT.md §5） |
| 📌 记录 | [G-03](#g-03) | — | SPEC 剩余未实现的接口 = Phase 5–8 的产出，不是缺陷 |
| ⚖️ 取舍 | [G-07](#g-07) | — | 守卫只在干净检出里有效 —— 这正是每单开独立 worktree 的理由 |
| ⚖️ 取舍 | [G-10](#g-10) | — | 闸验工作区而非提交 —— **这是故意的**：先提交会让三道守卫失效 |
| ✅ 已闭 | [G-28](#g-28) | HIGH | 成本口径：价目表已接入 + 26 行旧台账已迁移（26/26 算得出真实价，零需要猜） |
| ✅ 已闭 | [G-30](#g-30) | MEDIUM | 任务书情态动词：模板已改「必须报」，第二批用的就是改过的版本 |
| ✅ 已闭 | [G-33](#g-33) | HIGH | 换成 @@DEVLOOP 分隔块：**结构上**没有转义规则可违反（不是「分隔符够罕见」） |
| 🟠 未闭 | [G-35](#g-35) | MEDIUM | `num_turns` 与 `--max-turns` 不是同一个计量对象 |
| 🟡 未闭 | [G-04](#g-04) | LOW | `devloop` 未注册到 PATH，只能用 `python -m devloop.cli` |
| 🟡 未闭 | [G-08](#g-08) | LOW | M1 回归 486 秒，并行后会成瓶颈 |
| ✅ 已闭 | [G-27](#g-27) | LOW | `devloop prune` 列出可清理分支，⛔ 只列不删（分支上装着产出） |
| 📌 记录 | [G-19](#g-19) | — | 任务书设计评审应固化为流程 |
| 📌 记录 | [G-22](#g-22) | — | 试验场改用 DevLoop 自身（决策） |
| 📌 记录 | [G-23](#g-23) | — | `.devloop/` 应当被版本控制 |
| 📌 记录 | [G-25](#g-25) | — | 能力边界探测第一批数据 |
| 📌 记录 | [G-31](#g-31) | — | 首次捕获的模型自身错误（能力边界数据点） |
| 📌 记录 | [G-34](#g-34) | — | 抽查实测 6/6 属实 |
| 📌 记录 | [G-37](#g-37) | — | 编排侧的三个操作失误（常驻纪律） |
| ✅ 已闭 | [G-00](#g-00) | — | Windows 控制台编码崩溃 |
| ✅ 已闭 | [G-01](#g-01) | — | 写操作路径完全未验证 |
| ✅ 已闭 | [G-02](#g-02) | — | 三条错误路径只写了代码没触发过 |
| ✅ 已闭 | [G-05](#g-05) | — | 配置仍从 settings.json 回退读取 |
| ✅ 已闭 | [G-06](#g-06) | — | 单次样本 |
| ✅ 已闭 | [G-09](#g-09) | — | 裸 bash 在 Windows 上解析到 WSL |
| ✅ 已闭 | [G-11](#g-11) | — | 闸的基准选错了 |
| ✅ 已闭 | [G-12](#g-12) | — | 干净检出缺少被 gitignore 的构建缓存 |
| ✅ 已闭 | [G-15](#g-15) | — | 自检用文件时间戳推断认证状态 |
| ✅ 已闭 | [G-16](#g-16) | — | 桌面子代理通道不可用 |
| ✅ 已闭 | [G-17](#g-17) | — | 禁改清单守卫是空的 |
| ✅ 已闭 | [G-18](#g-18) | — | 确定性闸内部含非确定性判据（⚠️ 该修复本身有缺陷，见 G-36） |
| ✅ 已闭 | [G-20](#g-20) | — | 指纹校验实现了但没接进自动流程 |
| ✅ 已闭 | [G-21](#g-21) | — | 禁改守卫在两种项目状态下走向两个极端 |
| ✅ 已闭 | [G-24](#g-24) | — | 闸可能测错代码（editable 安装下的静默失效） |
| ✅ 已闭 | [G-26](#g-26) | CRITICAL | 隔离分支上没有提交，产出随 worktree 删除而丢失 |
| ✅ 已闭 | [G-29](#g-29) | HIGH | 判定必须看产物，不能看 self_check 自评 |
| ✅ 已闭 | [G-32](#g-32) | HIGH | 台账说了失败却不说为什么失败 |
| ✅ 已闭 | [G-36](#g-36) | CRITICAL | eco-ob 的闸在测试失败时以退出码 127 崩掉 |
| ✅ 已闭 | [G-39](#g-39) | HIGH | 契约在为一个从未实现的安全属性背书 |
| ✅ 已闭 | [G-40](#g-40) | CRITICAL | 闸的协议外退出码被当成「活没干好」 |

| ✅ 已闭 | [G-106](#g-106) | CRITICAL | 进度条把「超时被杀」「抛异常」都印成「完」——骗到了操作员 |
| ✅ 已闭 | [G-107](#g-107) | CRITICAL | 墙钟刹不住正在跑的单，而那个数先天到不了 |
| ✅ 已闭 | [G-108](#g-108) | CRITICAL | 进程被杀 / Ctrl-C 时台账一行都不留 |
| ✅ 已闭 | [G-109](#g-109) | CRITICAL | `prune` 把有未提交改动的 worktree 判成「可清理」 |
| ✅ 已闭 | [G-110](#g-110) | HIGH | 工人被死线打死时，它已经吐出来的东西（含用量）被整个扔了 |
| ✅ 已闭 | [G-111](#g-111) | HIGH | 明知工人死了，还花 19 分钟去验那半截产出 |
| ✅ 已闭 | [G-112](#g-112) | HIGH | 环境变量注入了，却从没告诉工人它叫什么 |
| ✅ 已闭 | [G-113](#g-113) | CRITICAL | ⛔ 上面那七条的**判据几乎全是代用品**，761 条全绿而 5 个洞一个没抓到 |
| ✅ 已闭 | [G-114](#g-114) | HIGH | 模板修好了，装到项目上的那份从来不跟，而且没人会告诉你 |

| ✅ 已闭 | [G-115](#g-115) | HIGH | 早报把「你现在该干什么」那半句剪掉了——⛔ **同一毛病第三次**，而 G-103 已点过名 |
| ✅ 已闭 | [G-116](#g-116) | HIGH | 墙钟掐死的闸被早报说成「闸自己坏了」——人被指去修一个没坏的环境 |
| ✅ 已闭 | [G-117](#g-117) | HIGH | `halt` 看不见正在跑的自动驾驶——⛔ 半夜只能守着窗口按 Ctrl-C |
| ✅ 已闭 | [G-118](#g-118) | HIGH | 撞额度就停在半夜不会自己接着跑——「挂一夜」实际是「挂到撞墙为止」 |
| ✅ 已闭 | [G-119](#g-119) | HIGH | **空文件夹拿到两个「通过」**——「未被改动」在文件不存在时恒真 |

**未闭 7 条**（🟠 5 · 🟡 2）· ⚖️ 取舍 4 条 · 📌/⭐ 记录 20 条 · ✅ 已闭 46 条 —— 合计 **77** 行

> ⚠️ **这行数字 2026-07-30 按索引逐行重数过**，2026-08-04 补 G-106~G-114 时又数了一遍。
> ⛔ **而 2026-08-04 当天它照样失守过**：那九条改完、文档写完、测试全绿，
> 索引**一条都没加**——是对抗复核翻出来的。⭐ 证明这条纪律要靠每次动手时想起来，
> ⚠️ 而「想起来」不是判据。
>
> ⚠️ 此前写的是「未闭 11 条
> （🔴 1 · 🟠 7 · 🟡 3）· 记录 7 条 · 已闭 21 条」，合计 39——而表里当时就有 57 行。
> ⛔ 汇总数不跟着表走，就变成一句**没人核对的话**：它看起来像读数，其实是遗迹。

---

## 全部条目（按编号，状态见上方索引）

### 📌 G-25 · 能力边界探测 · 第一批数据（2026-07-26）
**方法**：任务书由模型设计 → **独立审查实地复跑核对** → 通过审查的才派。审查要求：每条证伪须附自己跑的命令与真实输出；答案卷必须是可执行脚本，且**在未改动的代码上跑必须输出 FAIL**（否则它区分不了做没做）。

**审查环节的效果对比**（同样的四档设计，换了试验场与更严的证据要求）：

| 轮次 | 试验场 | 审查结果 |
|---|---|---|
| 第一轮 | eco-ob（486s 闸、脏工作区） | **4/4 NEEDS_FIX**，无一份答案卷可用 |
| 第二轮 | DevLoop 自身（0.24s 闸、干净） | **1 份 READY + 3 份 NEEDS_FIX**，四份答案卷**全部可执行且未改动时正确 FAIL** |

→ 换试验场的收益是实测的：**答案卷质量从 0/4 可用变成 4/4 可用**。上一轮的失败大半来自环境复杂度，不是设计水平。

**L3 实测结果（跨文件一致性档）**

| 项 | 值 |
|---|---|
| 成本 · 耗时 · 轮数 | **$0.4142 · 119 秒 · 26 轮** |
| 缓存命中 | 235776 tok |
| 闸 | **五道全绿** |
| 答案卷判分 | **PASS —— 全部判据通过** |
| 改动 | `telemetry.py` + `cli.py` + `pyproject.toml` + 新建 `tests_worker/`，4 处精确命中 |

**质量观察（比通过与否更有价值）**：工人的注释写清了三个「为什么」——为什么只列失败的分支（已过关的会随派单次数无限膨胀成噪声）、为什么用 `.get()` 而非下标（改动之前写的台账行没有该键）。**这正是 `rules-digest.md` 里要求的注释风格，它读懂并遵守了。**

**当前对核心问题的回答**：只读任务 6/6 · 写任务 2/2（L1 档的 gitignore + L3 档的跨文件贯通）。**便宜模型在 L3 及以下未出现失误。** 边界仍未探到——需要 L4（有陷阱档）才可能触及。


### ✅ G-24 · 闸可能测错代码（editable 安装下的静默失效）
**发现于**：DevLoop 接入自身时的主动排查（2026-07-26）——**不是等它出事才发现**
**事实**：本项目用 `pip install -e .` 安装，包链接指向**原目录**。若测试加载的是原目录而非 worktree，工人把源码改坏，闸照样全绿。
**实测结论**：当前**没有**失效——`python -m pytest` 会把 cwd 放进 `sys.path` 首位。实证：在 worktree 里故意改坏 `telemetry.py` 后跑 pytest，退出码 1，且 `devloop.__file__` 指向 worktree 内。
**但为什么仍要加固**：它能工作**只依赖调用形式**。改成裸 `pytest`（不带 `python -m`）就会加载已安装的原目录版本，**闸会静默地测错代码**。
**这比空守卫更危险**：空守卫给出的是「没验」，而测错代码给出的是**看起来正确的绿**——工人明明改坏了，闸却说通过。
**处置**：`gates.sh` 新增「被测代码来源」一闸，显式比对 `devloop.__file__` 的父目录与工作目录是否一致。已复验全绿。**已关闭。**
**射程**：管 Python 的 editable 安装；**不管**其它语言的等价陷阱（如 node_modules 软链、Go 的 replace 指令）——接入新语言项目时需各自检查。

### 📝 过程订正 · 一次自打脸的误判
排查 G-24 时我的判定脚本写错了——用「路径里是否含 AppData」来判断加载来源，而临时目录本身就在 AppData 下，于是得出了「闸测错代码」的**错误结论**。真实证据（`devloop.__file__` 的完整路径 + pytest 退出码 1）就摆在同一屏输出里。
**教训与 G-15 同源**：又一次用**间接特征**去推断状态，而直接证据已经在手边。这已是本项目第五次栽在同一个模式上（端点抖动 / 任务书判据 / 基线量错地方 / 凭时间戳判认证 / 本次）。
**记在这里是因为**：这个模式出现的频率高到不能当偶然——**凡是能直接读到答案的地方，一律不许用特征匹配去猜。**


### ✅ G-20 · 指纹校验实现了但没接进自动流程
**发现于**：用户追问「Phase 2 是不是收尾了」（2026-07-26）
**事实**：`gates.py` 的 `expect_fingerprint` 参数早已实现，但 `cli.py` 的写任务流程调用 `run_gates()` 时**没传它**——只有手动敲 `devloop gates --expect-fingerprint` 才生效。
**后果**：防篡改三道里的第 2 道在自动流程中**完全不起作用**。工人若在干活途中换掉闸文件，无任何机制发现。
**根因归纳**：**「实现了 ≠ 接上了」**。这与 G-17（空守卫）是同一类：代码存在、看起来完备，但在真实调用路径上没有效果。**验收时必须验的是「调用路径」，不是「代码存在」。**
**处置**：派单前记录指纹 → 跑闸时校验。已实测：传入伪造指纹返回退出码 2 并拒绝执行。**已关闭。**

### ✅ G-21 · 禁改守卫在两种项目状态下会走向两个极端
**发现于**：DevLoop 自身接入时（2026-07-26）
**事实**：守卫的第一版用 `git status -- .devloop/` → 在 `.devloop/` **未被跟踪**的项目（eco-ob）里恒为空 → **恒 PASS**。改成「存在即失败」后，在 `.devloop/` **已被跟踪**的项目（DevLoop 自己）里 → **恒 FAIL**。
**根因**：守卫写的是「它该不该存在」，而正确语义是「工人有没有改动它」。**判据选错了维度**，于是在两类项目上分别退化为恒真与恒假——两者都等于没守。
**处置**：先判断 `.devloop/` 是否被 git 跟踪，再分别用「查改动」或「查是否被创建」。两个项目的 `gates.sh` 均已同步。**已关闭。**

### 📌 G-22 · 试验场改用 DevLoop 自身（决策记录）
**决策于**：2026-07-26，用户批准
**为什么换**：eco-ob 的闸单次 **486 秒**，攒 6 个样本接近一小时，且其中大部分时间在跑与任务无关的回归。更麻烦的是它的复杂度**本身在制造噪声**——HEAD ≠ 工作区、17 个脏文件、180 个未跟踪项、墙钟断言。上一轮 4 份任务书全废，主要败因就是环境复杂度而非设计水平。**你在测模型能力，却一直在跟环境搏斗。**
**为什么是 DevLoop 自己**：① 它本来就该有测试（8 个模块零测试是个真缺口，尤其它要当作品集）；② 补完测试它立刻成为**闸跑 0.24 秒**的试验场，比 eco-ob 快约 2000 倍；③ 环境干净（git 已初始化、零未跟踪、无墙钟断言）；④ 任务书好写好验——答案卷就是 pytest 结果。
**⚠️ 已知局限（必须随数据一起标注）**：DevLoop 是刚写的代码，设计任务书的一方对它的熟悉度远高于工人，**这会让任务偏简单，样本代表性弱于在陌生项目上**。
~~**eco-ob 的定位**：留作第二轮验证——「真实复杂项目」的对照组，等其脏改动处理完再回来。~~
⚠️ **2026-08-11 订正：这句话过期了。** 2026-08-07 起 eco-ob 已是**唯一主战场**（跑了 6 轮：第0步 / 组1 / 组1b / 组1c / 组2 / 组3）。
⭐ ⛔ 但它始终是**手段**，不是产品：它存在的唯一理由是**把这个工具用出毛病来**。判据见 **G-128**。

### 📌 G-23 · `.devloop/` 应当被版本控制
**发现于**：G-17 排查（2026-07-26）
**事实**：eco-ob 的 `.devloop/` 未被 git 跟踪，直接导致禁改守卫失效。
**为什么应当跟踪**：`gates.sh` 与 `rules-digest.md` 是**项目配置**——它们定义了「这个项目怎么算通过」和「工人必须遵守什么」。这类东西和 CI 配置同级，理应版本化、可评审、可回溯。不跟踪则：守卫失效 · 无法追溯闸何时被改 · 换机器要重写。
**⛔ 例外**：`reports/` 与 `telemetry.jsonl` 是运行产物，应 gitignore（DevLoop 自己的 `.gitignore` 已如此处理）。
**触发条件**：eco-ob 下次动其 `.devloop/` 时一并提交——**属用户决策，工具不代劳**。


### ✅ G-17 · 「禁改清单」守卫是空的，从未守住任何东西
**发现于**：任务书设计评审（2026-07-26）——**在派单之前**，由独立审查实地核对发现
**事实**：`.devloop/` 未被 git 跟踪，而 worktree 基于 `git stash create`（**该命令不含未跟踪文件**），故干净检出里根本不存在 `.devloop/` → `git status --porcelain -- '.devloop/'` 恒为空 → 守卫**恒 PASS**。
**后果**：它守住的是「一个不存在的目录没被改」。此前所有「三道守卫全绿」的记录里，第三道实际上什么都没验——**提供的是虚假安全感**，这比没有守卫更危险。
**处置**：改为直接检查 worktree 内是否**出现**了 `.devloop/`（工人没有任何理由去创建它）。**已修并复验。**

### ✅ G-18 · 「确定性闸」内部含非确定性判据
**发现于**：同上（任务书设计评审）
**事实**：`test_m1.gd::_test_tick_budget` 用 `Time.get_ticks_usec()` 做**墙钟**断言——`sustained <= 7.5`（实测余量约 5%）、`burst < 1100.0`（实测余量约 3%）。
**后果**：**机器一有负载闸就红，与工人改动毫无关系**。一个号称「不会说谎」的确定性闸，内部藏着会因外部环境变红的判据——它会把「机器忙」误判成「活没干好」，正是本项目最想避免的那类错误。
**处置**：闸识别「仅性能断言未过」的情形，降级为 SKIP + 告警，不计入失败；同时明说「非工人问题，但也未证明代码正确」。**已修。**

⚠️ **但这个修复本身带着一个雷，2026-07-26 才被发现**：它引用了 `$perf_only` / `$perf_fails`
两个**从未被赋值**的变量，而脚本开头是 `set -u`——测试一旦真的失败就以**退出码 127** 中止。
绿路径完全正常，只在闸唯一有用的那一刻失效。详见 **G-36**（已重修 + 红绿四态测试）。
**教训：修复本身也要红测**——只在「本来就好」的路径上验证过，等于没验证。
**射程**：管「性能断言不误伤工人」；**不管**「性能退化的检测」——那需要单独的、有统计意义的性能闸，当前没有。

### 📌 G-19 · 任务书设计评审已证明必要，应固化为流程
**发现于**：本轮（2026-07-26）
**事实**：4 份任务书经独立审查，**全部裁定 NEEDS_FIX，无一份答案卷可用**。查出的问题包括：把脏工作区的数字当成 HEAD 的（71 vs 68）· 把未跟踪文件当成仓库文件（`probe_verify_config.gd` 在 HEAD 里不存在）· 引用了错误的提交号 · 描述的注释位置与实际相反 · 依赖 PATH 上并不存在的 `godot` 命令。
**更重要的是**：其中两条不是任务书的问题，而是**工具的缺陷**（G-17、G-18）——评审是在派单前发现它们的。
**结论**：**任务书在派出之前必须经独立审查实地核对每一个文件名、符号名、数字**。这不是可选步骤——四份里零份合格，说明凭记忆或凭推断写任务书的失败率接近 100%。
**触发条件**：**常驻流程**，写进 SPEC 的派单前置条件。


### ✅ G-16 · 桌面子代理通道不可用
**发现于**：第 3 轮审计（三个审查 agent 全数当场死亡）
**事实**：全局 env 的 `CLAUDE_CODE_SUBAGENT_MODEL` 把子代理钉在第三方模型名上，而子代理请求发往 Anthropic 端点 → 一律报「模型不存在」。**连续多轮无法取得独立审查视角。**
**处置**：Phase 0 移除 9 个污染键 + 用户重启桌面 App。**2026-07-26 实测返回 `SUBAGENT_CHANNEL_OK`，已关闭。**
**意义**：这是 Phase 0 唯一无法由 Python 侧自检的一项——**它必须在桌面会话内实测**，`doctor` 只能标注为「需人工验证」。工具能自检的边界到此为止。


### 🟠 G-10 · 闸验的是 worktree 工作区，不是提交

> ⚖️ **2026-07-27 重新归类：这不是待修缺陷，是一个已被理解的取舍。**
>
> **⛔ 不要"修"它。** 派单路径让闸验 worktree 的**工作区**而非提交，是**故意的**：
> 三道改动守卫靠 `git status --porcelain` 识别工人改了什么，**先提交会让工作区变干净、
> 三道守卫全部退化成空守卫**（与 G-20 同型，本项目已两次栽在空守卫上）。
>
> 代价如实记下：工人可能留下未暂存文件，提交能过所有本地检查却引用了没进提交的文件。
> 需要验提交时走 `devloop gates --commit <sha>`，那条路径本来就存在。
>
> **它留在"未闭"里的真实危害**：会诱导未来的维护者（包括未来的我）去"修"它，
> 然后砸掉整套验收闸。**归类错误本身就是一种缺陷。**
**发现于**：Phase 2 收尾设计（2026-07-26）
**事实**：写操作流程中，闸直接验 worktree 的工作区状态（等价于「HEAD + 工人改动」），**没有先提交再验提交**。原因：eco-ob 章程规定「只在用户明确要求时提交」，工具不得自动 commit。
**后果**：失去了 T1 那条「验证你要发的产物而非构建它的环境」的保护——一个提交可能引用未暂存的文件，本地能过、CI 过不了。当前流程下该风险转移到**用户合并时**。
**触发条件**：接入 CI 或自动合并时（Phase 5+）。届时需要一个不违反 commit 纪律的方案（例如让用户批准后再由工具提交并复验）。

### ✅ G-01 · 写操作路径完全未验证
**发现于**：Phase 1 验收（2026-07-26）
**事实**：首单是只读任务（`--tools readonly`）。工人的写操作质量、worktree 隔离、改动回滚——**一条都没跑过**。
**后果**：写操作才是真正的风险区。在此之前对「工人够不够用」的任何判断都只覆盖了简单场景。
**处置**：2026-07-26 首个写任务实测完成。worktree 隔离生效、工人只碰了授权文件、三道守卫全过、回归闸拦下真缺陷。**已关闭。**

### ✅ G-02 · 三条错误路径只写了代码没触发过
**发现于**：Phase 1 验收
**事实**：`dispatch.py` 里的超时、回执 JSON 解析失败、`claude` 不在 PATH 三个分支，均未实际触发验证。
**后果**：只读任务失败顶多浪费一单；**写任务时一个僵死的工人会占着 worktree**，后果重得多。
**处置**：2026-07-26 三条全部实测触发通过——超时（压到 3 秒）、可执行文件缺失、回执非 JSON（原文已留存供排查）。
**过程中另有一发现**：前两次测试**失败的是测试本身**——Windows 上 `subprocess` 用**当前进程**的 PATH 解析可执行文件，传进 env 的 PATH 不参与解析。故 `dispatch.py` 增加显式 `claude_bin` / `DEVLOOP_CLAUDE_BIN`。与 `gates.py::find_bash` 同源：**Windows 的可执行文件解析不能想当然。已关闭。**

### 🟠 G-03 · SPEC 承诺的接口只实现了一部分

> 📌 **2026-07-27 重新归类：这是路线图，不是缺陷台账。**
>
> 收缩后仍未实现的是 `--out` · `mcp_docs` · `mcp_fleet`。
> （`autopilot` 已于 2026-07-28 落地；`templates/` 四份已齐。）
> （`halt` / `--parallel` / `--model→--backend` 均已于 2026-07-27 实现，从名单移除。
> ⚠️ 用括号打补丁的写法留下过一个坑：名单的正文没改，索引也没改，
> 于是「仍然真缺 halt」在 halt 落地之后还挂了一天。**状态只在一处维护**。）
> 其中 `mcp_docs`/`mcp_fleet`/`autopilot` **本来就是 Phase 3/6/7 的产出**——
> 把「后面阶段还没做」记成「当前缺陷」，会让台账永远清不完，也让真正的缺陷被淹没。

> **2026-07-26 收缩**：`doctor` 与 `gates` 两个子命令**已实现**（`devloop/doctor.py`、`devloop/gates.py`，
> `cli.py` 两个子解析器均已注册），从缺失名单里移除。
> `--worktree` / `--gates|--no-gates` 也从名单里移除，但**理由不同**——它们不是「还没做」，
> 而是**从来就不该是开关**：真实语义由 `--tools` 决定（SPEC §5.1 已订正）。
> 仍然真缺的：`--out` · `mcp_docs` · `mcp_fleet` · `autopilot` · `templates/`。
**发现于**：Phase 1 验收后的机器比对（**人工回忆漏掉了这条**）
**事实**：

| 类别 | 已实现 | 缺失 |
|---|---|---|
| 子命令 | `dispatch` `stats` | `doctor` `gates` `halt` |
| dispatch 选项 | `--project` `--task` `--task-dir` `--tools` `--max-turns` | `--model` `--parallel` `--worktree` `--gates` `--out` |
| 模块 | `cli` `dispatch` `telemetry` `config` `models` | `doctor` `gates` `halt` `mcp_docs` `mcp_fleet` `autopilot` |
| 其它 | — | `templates/` |

**后果**：**SPEC 是对外契约**。它描述了一个比现实完整得多的工具——读者（含跨会话回来的自己）会照 SPEC 动手然后撞墙。这是「把设计写成既成事实」的同一类错误，第 9 轮审计刚在 PLAN 里抓过一次。
**触发条件**：**立即**——不是补实现，而是**在 SPEC 里标注实现状态**，让契约与现实对齐。缺失的实现各自跟随其阶段。

### 🟡 G-04 · `devloop` 未注册到 PATH
**事实**：`pip install -e .` 未生成 `devloop.exe`，只能用 `python -m devloop.cli`。
**后果**：仅影响手感；文档里的命令示例与实际用法不符会让新人困惑。
**触发条件**：做 QUICKSTART 文档时一并解决。

### ✅ G-05 · 配置仍从 `settings.json` 回退读取
**事实**：`worker-deepseek.json` 尚不存在，`config.py` 走的是显式标注的回退路径。
**后果**：无功能影响（来源会被打印出来）。
**处置**：Phase 0 完成——`~/.claude/worker-deepseek.json` 已建，全局 env 移除 9 个污染键，`devloop doctor` 确认来源为「独立文件」。**2026-07-26 已关闭。**

### ✅ G-06 · 单次样本
**事实**：全部结论建立在 1 单之上（$0.1252 / 29s / 17 轮 / 缓存命中 7424 tok）。
**后果**：一单不能代表分布，尤其成本与缓存命中率。
**处置**：已攒到 9 单（只读 6 + 写 3）。**已关闭**，后续由台账持续观测。

---

### 🟠 G-07 · 闸只在「干净检出」模式下有守卫能力

> ⚖️ **2026-07-27 重新归类：取舍，非缺陷。** 与 G-10 是同一件事的两面。
>
> 守卫只在干净检出里有意义——在本来就有未提交改动的工作区里，git 差异无法区分
> 「工人改的」与「你手头在途的」（Phase 2 实测：产生三条误伤）。
> **这正是每单必须开独立 worktree 的理由**，不是可修的缺陷。
**发现于**：Phase 2 绿测（2026-07-26，实测）
**事实**：三道改动守卫依赖 `git status` 判断「工人改了什么」。**只有在某个提交的干净 worktree 里，git 差异才等于工人的改动**；在本来就有未提交改动的工作区里二者无法区分，此时守卫降级为 SKIP。
**后果**：`devloop gates`（不带 `--commit`）**不具备守卫能力**，只能跑测试。若误以为它等同于完整验收，篡改可以完全不被发现。
**触发条件**：**立即生效的使用纪律**——真实验收一律用 `--commit`。派单流程接入闸时（Phase 2 收尾）必须默认走干净检出，不给「验工作区」留默认路径。

### 🟡 G-08 · M1 回归耗时 486 秒，闸跑一次就是 8 分钟
**发现于**：Phase 2 实测基线（2026-07-26）
**事实**：`test_m1.gd` 单次 486 秒 / 38 通过 0 失败。闸超时按 3 倍余量设为 1500 秒。
**后果**：并行派 N 个工人时若每单都跑全量闸，验收会成为瓶颈（3 单 = 24 分钟串行）。
**触发条件**：并行派单（`--parallel`）实装时——届时需要决定是每单跑闸还是批次末尾跑一次。

### ✅ G-11 · ~~任务书判据错误~~ → ~~基线量错了地方~~ → **闸的基准选错了**
**首次判断（2026-07-26，已推翻）**：首个写任务失败，我归因为「任务书判据写错（`.import` 可重建），工人忠实执行了错误前提」。
**⚠️ 该归因是错的。** 对照实验推翻了它：把判据改对后重跑，工人产出**完全正确**（只挡 `/_*.txt`，没碰 `.import`），**闸照样红**。

**真因（实测三步）**：
1. 闸在 worktree 里跑，而 worktree 是 **HEAD 的干净检出**
2. `.godot/` 导入缓存被 gitignore → 新 worktree 里没有它 → godot 加载资源失败
3. 补上 `.godot/` 后再跑：**HEAD = 23 通过 / 12 失败**；而主工作区 = **38 通过 / 0 失败**

**根本问题在我**：我在**主工作区**量出「38 通过」当作基线，而闸验的是 **HEAD**。**两者不是同一份代码**——主工作区有 7-25 那批未提交的改动，正是它们让测试转绿的。
**「先建答案卷」这条纪律我执行了，但建错了地方。** 答案卷必须在**闸实际验证的那个对象上**建立。

**后果**：闸的回归项在本仓库**从一开始就不可能通过**，与工人无关。此前两次写任务失败**全部记错了账**。
**处置进度**：① 台账归因已订正 ✅；② 闸已能同步构建缓存（见 G-12）✅；③ **剩余：HEAD 本身 23 通过 / 12 失败** —— 让测试转绿的修复仍在 7-25 那批未提交改动里。
③ **已解决，且不需要用户做任何仓库决策** —— `git stash create` 把当前工作区（含未提交改动）固化成一个提交对象，worktree 从它检出。该命令**只造对象，不动工作区、不进 stash 栈、不进任何分支历史**，因此不违反「只在用户明确要求时提交」。
**最终验证**：同一任务第四次跑，`通过 38 失败 0`，四道闸全绿，`完成 1/1`。
**回头看这条缺口的全过程**（三次归因，前两次都错）：任务书判据 → 构建缓存 → 闸的基准。**每一次「找到原因」都是靠一次新的实测推翻上一次，没有一次是靠想。** 已关闭。

### ✅ G-12 · 干净检出缺少被 gitignore 的构建缓存
**发现于**：G-11 排查（2026-07-26）
**事实**：`game/.godot/` 是引擎的导入缓存，被 gitignore。worktree 是干净检出，因此没有它，godot 无法加载资源。
**后果**：任何依赖构建缓存的项目，闸在 worktree 里都会假失败。这是**跨项目的通用问题**，不是 eco-ob 特有。
**处置**：2026-07-26 已修——项目在 `.devloop/config.toml` 用 `gates.sync_ignored_paths` 声明，`gates.py` 跑闸前从主工作区同步。
**验证**：修复前测试报「0 通过 0 失败」（资源加载崩溃），修复后报「23 通过 12 失败」——**测试真的跑起来了**。**已关闭。**

### ✅ G-15 · 自检用文件时间戳推断认证状态（错误判据）
**发现于**：用户实跑 `claude auth status`（2026-07-26）
**事实**：`doctor.py` 早先版本用 `.credentials.json` 的 mtime 判断令牌是否过期，报「已 61 天未更新」。实际 `claude auth status` 返回 `loggedIn: true`、Max 订阅——**认证完全正常**。
**根因**：令牌在内存中刷新，不回写文件；文件时间戳与令牌有效性**无因果关系**。
**意义**：这是**用可观察的间接指标推断状态**的典型错误——与本项目此前多次栽的坑同源（「端点抖动」「任务书判据错」「基线量错地方」，全是拿间接线索当结论）。**有直接接口就别推断。**
**处置**：改为调用 `claude auth status` 并解析 JSON。**已关闭。**


### ✅ G-09 · 裸 `bash` 在 Windows 上解析到 WSL
**发现于**：Phase 2 闸首次执行（真实触发）
**事实**：`subprocess.run(["bash", ...])` 被解析到 `System32ash.exe`（WSL 入口），报 `execvpe(/bin/bash) failed: No such file or directory`。
**意义**：这正是历次审计标记为「Windows 上 `.sh` 由谁执行」的那个未决问题的**具体形态**——**读文档读不出来，跑一次就撞到**。
**处置**：`gates.py::find_bash` 显式定位 Git Bash，排除 System32 路径，支持 `DEVLOOP_BASH` 覆盖。**2026-07-26 已修。**

### ✅ G-00 · Windows 控制台编码崩溃
**发现于**：Phase 1 首次派单（真实触发，非推演）
**事实**：Windows 控制台默认 GBK，打印 `✓` 抛 `UnicodeEncodeError`。
**关键观察**：**回执与台账已正常落盘，崩的只是显示层**——证明「先写文件再打印」的顺序是对的。
**处置**：`cli.py` 启动时强制 stdout/stderr 为 UTF-8。**2026-07-26 已修。**

### G-26 · 隔离分支上没有提交，产出只活在未提交工作区 —— **已修复**

**严重度**：CRITICAL（静默数据丢失）· **发现**：2026-07-26 · **状态**：✅ 已修

**症状**：`devloop dispatch --tools implement` 收尾时打印
`分支 devloop/<任务>-<时间戳>（保留供你审查；丢弃：git worktree remove --force <路径>）`。
但流程从不提交，分支自始至终指向基点提交，**上面一个改动都没有**。工人的产出只存在于
worktree 的未提交工作区，照着同一行给出的删除命令一执行，产出即刻永久消失。

**实际损失**：L3 任务的产出（telemetry 加 branch 字段，五闸全绿 + 答案卷全部判据通过，
成本 $0.41）已真实丢失，不可恢复。L4 的产出在发现时抢救成功。

**为什么会这样**：`worktree.py` 原 docstring 写着「⛔ 不自动 commit：章程规定只在用户
明确要求时提交」。这条推理错在把**用户主线**和**工具自造的一次性隔离分支**当成同一个东西。
纪律护的是前者；不提交后者的代价是静默丢产出，比多一个可随手删的分支严重得多。

**为什么没被早点发现**：闸全绿、答案卷全过、台账记录完整——所有信号都是绿的。丢失发生在
**这些信号之后**，属于「假绿的第五种形态：验收正确，但被验收的东西没被保存下来」。

**修法**：`Worktree.commit_result()`，在 `cli.py::cmd_dispatch` 中于跑闸**之后**调用。
顺序是硬约束——闸靠 `git status --porcelain` 识别工人改动，先提交会让三道守卫全部退化成
空守卫（与 G-20 同型）。闸没过也提交，提交信息里写明闸的结论。身份写死为
`devloop-worker[<任务>]`，`git log` 一眼可辨人机。5 个回归测试锁住：产出可从分支取回、
红的写明、无改动不造空提交、**主线 SHA 不动**、身份是机器。

**遗留**：合回主线仍须用户逐单批准（宪法未变）。分支会累积，清理策略见 G-27。

### G-27 · 隔离分支无清理策略

**严重度**：LOW · **触发条件**：分支数超过 20，或 `git branch --list 'devloop/*'` 影响可读性时再处理。
G-26 之后每单都会留一个分支。当前 3 个，不值得现在自动化。

### G-28 · 台账里的美元是合成价，不是真钱 —— **口径错误，已知未修**

**触发条件**：对外报出任何成本百分比之前。⚠️ 它是 G-38 的前置——没有真实分母，「省了 X%」不成立。

**严重度**：HIGH（一切成本主张的分母都建在它上面）· **发现**：2026-07-26 · **状态**：✅ 价目表已补（`prices.json`，2026-07-26 取自 DeepSeek 官方文档）；换算脚本待接入台账

**事实**：13/13 份回执的 `total_cost_usd` 精确等于
`5e-6·input + 25e-6·output + 0.5e-6·cache_read`，即 **Opus 的公开价目表**。
但产生这些 token 的是 `deepseek-v4-pro[1m]`。Claude Code 不认识第三方端点的价格，
于是套用了自己的价目表。

**后果**：此前所有对外汇报的成本数字（$0.41、$0.96、$5.68 …）**全部是合成价**。
真实花费远低于此（DeepSeek 定价低一个量级以上），但**低多少未知**。
「便宜模型省了 X%」这个主张的分母因此至今不成立。

**连带影响**：`cache_creation_input_tokens` 在 13/13 份回执里全为 0——从未观测到。
不确定是该端点不计费缓存写入，还是字段未上报。做成本模型时须标注「未观测」，不可当成 0 成本。

**修法**：外置 `prices.json`，按 `model` 查真实单价，token 数用回执原始值。
⚠️ **前提是拿到 `deepseek-v4-pro[1m]` 的真实价目表**（B12）——在那之前一切百分比都不许报。

**教训**：`total_cost_usd` 是一个**看起来权威的派生字段**。它有单位、有小数点、
出现在官方回执里——正因如此没人去问它是怎么算的。这与「假绿」同源：
**信任一个派生信号，而没去看它的原始输入**。

**2026-07-26 实测重算**：13 单累计 输入 182,119 · 输出 45,854 · 缓存读 642,560 token。
- 我此前汇报（Opus 价）：**$2.3782**
- 真实（DeepSeek v4-pro 官方价 $0.435/$0.87/$0.003625 每百万）：**$0.1214**
- **成本被夸大 20 倍。** 缓存读单价差 138 倍是主因——缓存读占了总 token 的 74%。
- ⚠️ 配置用的模型名是 `deepseek-v4-pro[1m]`（1M 上下文），价目表只列 `deepseek-v4-pro`。
  长上下文是否加价未确认，真钱仍以账单为准。

### G-29 · 判定必须看产物，不能看自评 —— **已在评分脚本落实**

**发现**：2026-07-26 u01 冒烟单 · **严重度**：HIGH（这是本项目存在的理由本身）

工人报告的 `self_check.no_line_anchors` 自报 `true`，而报告正文有 **19 处代码行号锚点**，
违反任务书禁令。**模型说自己遵守了，产物说没有。**

这是「done 由退出码裁决、不由模型自述」这条原则在**报告层**的实例：
自评字段和「工人说测试全绿」是同一类东西，一律不可信。

**修法**：评分脚本用正则直接扫报告正文（`[a-z_]+\.gd:\d+`），不读 `self_check`。
`self_check` 字段保留——它有诊断价值（自评与产物的**差值**本身是一个信号），但不参与判分。

### G-30 · A2 判据「可以」与「必须」之差 —— **任务书措辞缺陷，已记待改**

**触发条件**：下一批任务书起草时。

u01 任务书写「这类『行号指错了』**本身可以报成矛盾**」。工人 185401 观察到了行号漂移
（`checked` 字段里明写 `→ 236-241`），然后按「可以」的字面判为一致。**按任务书它没错。**

**教训**：判据里的情态动词（可以 / 应该 / 必须）会被逐字执行。
写「可以」就等于把它变成可选项，而答案卷却按必答题算分——**又一次「拿没写清的规矩扣工人分」**。
下一版改为「**必须**报，且**必须**给出正确的符号名」。

### G-31 · 首次捕获的模型自身错误（能力边界数据点）

14 次派单以来第一次：工人 184927 对一条八项射程的全称否定陈述，
**只查了两项的架构类名**（AP / Combat），没查到就判「一致」，而另外六项都实装了。

**边界形状**：不是「不会查」——它列了 14 组命名变体、写了 26 条 not_findings；
是**查错了粒度**（找类名而不是找行为）。而所有表面信号都是绿的。
→ 这类错误**只能靠答案卷或独立复核抓到**，抽查协议不可省。

### G-32 · 台账说了「失败」却不说「为什么失败」 —— **已修**

u11 单撞满 30 轮上限被截断（回执 `subtype=error_max_turns`、报告 0 字符）。
台账**正确**记了 `worker_ok=False`（没有假绿），但 `error` 与 `failure_class` 都是 `None`。
**「轮数不够」是我拆单太大（8 个文档塞进 30 轮），「模型不行」是能力问题——
成本实验里混为一谈，就会把编排方的失误算到便宜模型头上。**

**修法**：`Receipt` 新增 `subtype` / `terminal_reason` / `errors` 三个字段与 `why_failed` 属性；
台账新增 `truncated` 布尔（撞轮数上限单独可筛）与 `input_tokens` / `output_tokens`
（算真实成本要原始 token，回执的 `total_cost_usd` 是 Opus 合成价）。5 个回归测试。

### G-33 · 手写 JSON + 自由文本 = 转义地雷 —— **格式设计缺陷，待改**

**触发条件**：下一批任务书起草时（与 G-30 同批处理）。

13 单里 **3 单**的末尾 JSON 块解析失败，三种不同的转义撞车，**全部是任务书挖的坑**：

| 单 | 撞什么 | 例 |
|---|---|---|
| u03 / u07 | grep 交替语法进 JSON 字符串 | `"checked": "grep '双层\|double.layer'"` —— `\|` 在 JSON 里是非法转义 |
| u11 | 中文里夹 ASCII 双引号 | `"doc_anchor": "…与"卡在哪（现行）"段"` |

**根因**：我要求工人**手写**含自由文本与 shell 语法的 JSON。这不是模型能力问题——
内容全部完好，补上转义即可复原（`_review/p4/repair_json.py`，三份全部救回，共补 8 处）。

**下一版改法**（三选一，倾向第 2）：
1. 禁止在值里出现反斜杠与引号 —— 治标，且会限制表达
2. **改用无转义规则的分隔格式**（每条一个块，`键: 值` 逐行）—— 从根上消除
3. 派单侧自动修复 —— 已实现，但那是补丁不是设计

⚠️ 派单前我判断「最脆的一环是 JSON 遵从率」，方向对了但**原因猜错**：
不是模型写不出 JSON（结构、字段、嵌套全部正确），而是**我给的格式本身有雷**。

### G-34 · 抽查实测：6/6 属实

自查 6 条高影响矛盾，全部属实。一条有 off-by-one（工人报 221 行、实际 220），实质判断成立。
⚠️ **我自己第一次抽查①时查错了范围**（全文件 grep `capacity_cost` 而非查那个函数体），
差点判工人错。**复核者也会犯工人犯的错——抽查协议必须能自查。**

### G-35 · `num_turns` 与 `--max-turns` 不是同一个计量对象 —— **认知错误，已查明**

**触发条件**：下一次决定 `--max-turns` 或拆单尺寸时。

**严重度**：MEDIUM（会导致拆单尺寸判断错误）· **发现**：2026-07-26

派单时给全部 10 单传了 `--max-turns 30`，但回执显示 u10 跑了 **71** 轮 `subtype=success`，
而 u11 在 **31** 轮报「Reached maximum number of turns (30)」。同一个参数两种行为，说不通。

**实地数会话记录得出的真相**（u10，session `2d43cb22`，184 行）：

| 计数 | 值 |
|---|---|
| `user` 角色行（工具结果回传） | **71** ← 回执的 `num_turns` 数的是这个 |
| `assistant` 角色行 | 100（含 70 个 tool_use、25 个 thinking、5 个 text） |
| **assistant 消息去重后** | **25** ← `--max-turns 30` 限的是这个，25 < 30 故成功 |

两者比值 **1.0–2.8 倍，不固定**，取决于每轮塞了几个并发工具调用。

**为什么 u11 会饿死**：它要读 8 份整文档，一轮一个 `Read` 无法批量，比值接近 1:1，
30 轮全烧在读文件上，没轮次写报告。而 u10 把 grep 批量化（25 轮塞了 70 个工具调用），
同样的预算干了 2.8 倍的活。

**所以拿 `num_turns` 判断「离上限还有多远」是错的**——它不是那个量。
⚠️ **这又是第六条纪律**：拿一个看起来相关的数当直接证据。真答案在会话记录里，数一下就有。

**处置**：
1. 判断轮数余量：数会话记录里去重后的 assistant 消息数，不看 `num_turns`
2. 任务书里「尽量一次 Grep 查多个符号」这条不是风格建议，**是预算杠杆**，应提到显眼位置
3. 读多写少的单（要读整份大文档的）天然无法批量，拆单时要单独给更高的轮数上限

### G-36 · eco-ob 的闸在「测试失败」时会以退出码 127 崩掉 —— **已修**

**严重度**：CRITICAL（闸唯一有用的那一刻正好是它崩的那一刻）· **发现并修复**：2026-07-26

`C:\pg\eco-ob\.devloop\gates.sh` 开头是 `set -uo pipefail`，而回归闸的分支里
引用了 `$perf_only` 与 `$perf_fails` —— **这两个变量从头到尾没有被赋值过**。

**这个缺陷的形状特别恶劣**：
- 测试全绿时 `rc=0`，那个 `elif` 根本走不到，闸表现完全正常
- **只有测试真的失败时才会走到它** —— 也就是闸唯一有用的那一刻
- 后果是 `unbound variable` → **退出码 127**，而 127 不在闸协议的 0/1/2 里

红测实证：`bash -c 'set -u; rc=1; n_fail=2; [ "$perf_only" -eq 0 ]'` → `unbound variable`，127。

**它是怎么混进来的**：G-18 的修复引入的。BACKLOG 当时把 G-18 标成「✅ 已修」，
**但没有对修复本身做红测** —— 只验证了绿路径。这正是本项目自己反复强调的
「不要问闸有没有跑，要问如果我故意搞破坏它会不会红」，而我们在自己的修复上没做这一步。

**修法**：从测试输出实际统计（性能断言靠描述里的 `O-PERF-01` 标记识别，
见 `game/tests/test_m1.gd::_test_tick_budget`）：
```bash
fail_lines=$(printf '%s' "$out" | grep 'FAIL' || true)
perf_fails=$(printf '%s
' "$fail_lines" | grep -c 'O-PERF-01' || true)
other_fails=$(( n_fail - perf_fails )); [ "$other_fails" -lt 0 ] && other_fails=0
```
判据改为 `other_fails == 0 && perf_fails > 0` 才降级 SKIP。
**红绿四态测试全部通过**：全绿→PASS · 仅性能失败→SKIP · 真失败→FAIL · 混合失败→FAIL。

**⭐ 由此得出一条纪律**：**修复本身也要红测。** 一个修复如果只在「本来就好」的路径上
验证过，等于没验证——缺陷恰恰藏在它要处理的那条异常路径上。

### G-37 · 编排侧的三个操作失误（不是 DevLoop 的缺陷，但会浪费真钱）

**触发条件**：常驻纪律，不适用「触发条件」这条规则——它是操作习惯，不是待办项。

**严重度**：LOW · **状态**：已知，靠纪律规避

1. **后台命令里再套 `&`** —— 外层立刻返回、内层子壳被回收，任务实际没跑。
   我据此误判「后台派单没落地」，又前台跑了一遍。
2. **命令末尾加 `| tail -N`** —— 管道缓冲，后台任务的进度在进程结束前完全不可见。
   批处理跑了 50 分钟，中途一行输出都看不到，用户以为卡死。
3. **用长 `sleep` 占着终端等** —— 从用户视角就是任务一直转圈没反应。应短轮询。

**共同处置**：判断后台派单的状态，**一律读 `.devloop/telemetry.jsonl` 与 `reports/`**
（每单跑完立刻追加一行，是最直接的证据），不看输出文件大小、不猜进程名、不长 sleep。

### G-38 · 贵模型对照臂 —— **✅ 已跑（2026-07-26），结论为负**

**严重度**：HIGH（这是整个项目省钱主张的分母）· **状态**：✅ 已完成

**结论（详见 `_review/p4/RESULT.md`）**：

| 口径 | A 便宜路线 | C 贵模型 |
|---|---|---|
| 窄（只算执行腿） | **$0.56** | $26.82 |
| 中（+ 拆单设计 $27.67） | **$28.23** | $26.82 |
| 宽（+ 主循环编排/评审/抽查） | 未统计，只会更高 | $26.82 |

**摊薄点 = 第 12 单，而本轮只跑了 11 单：连打平都没到。**
（⚠️ 初版数字全部偏低，见 G-42；订正后结论**更糟**，不是更好）
质量上 C 报 98 条 vs A 报 32 条，池化覆盖 84% vs 27%；
抽查 10 条仅 C 找到的，7 条可机械验证，**7/7 全部成立**——不是幻觉，是 A 漏了。

**D6 自曝的偏差方向已明确**：子代理携带主会话上下文（单个 cache_read 164 万，
A 臂单个仅 71 万），**抬高了 C 臂成本**——真实差距对 A 更不利，不是更有利。

**这条不关闭为「已解决」，而是转为持续测量项**：见 RESULT.md §5 的四项待测，
其中「拆单设计里一次性基础设施 vs 每批必付的比例」是摊薄点的分母，也是本结论最大的未知数。

只读工作包 A 臂（便宜模型）已完成：13 单、真实成本 $0.5626、33 条矛盾。
**C 臂（贵模型跑同样 11 份任务书）一单未跑。** 没有它，「省了多少」没有分母。

**载体已定**（决策记录见本条下方，PLAN §10 载体对比表同步）：用 **Claude 子代理**，不用 Anthropic API key。
理由：用户只有订阅、无 API key；而 `claude --bare` 只认 `ANTHROPIC_API_KEY`，
不带 `--bare` 又会加载 eco-ob 的 `CLAUDE.md`——**而 CLAUDE.md 本身就是对账靶子，等于开卷**。
子代理跑在订阅上、上下文全新（不会被 A 臂结果锚定）、任务书一字不差。

**已知缺陷（必须写进结论，不许藏）**：子代理的系统提示词与 `claude --bare` 不同，
**不是逐字节严格的对照**。这会让「省了 X%」带一个系统性偏差，方向未知。

### G-39 · 契约在为一个从未实现的安全属性背书 —— **已订正**

**严重度**：HIGH（SPEC 是契约文档，它的错误会被下游当成前提）· **发现**：2026-07-26 独立文档审查

`SPEC.md` §5.2 把「**cwd：工具本体目录（⛔ 不是被检查目录——防篡改）**」写进闸协议，
读起来像是一道防篡改设计。实际上 `devloop/gates.py::run_gates` 调 `subprocess.run` 时
**根本没有传 `cwd` 参数** —— 闸继承的是调用者当时的工作目录，可以是任何地方。

**为什么危险**：写 gates.sh 的人会以为 cwd 有保证，从而依赖它定位项目；
而真实行为取决于你在哪个目录敲的命令。本项目自己的 `.devloop/gates.sh` 是靠每处
`cd "$WORK"` 自救的——**代码没依赖这条契约，恰恰说明写代码的人知道它不成立，但没回来改文档。**

**订正**：SPEC 改为明写「cwd 不作任何保证，闸脚本不得依赖它，一切路径从 `$1` 推」。

**教训**：**契约文档最危险的错误不是漏写，是写了一条不存在的保证。** 漏写会让人去查代码；
写错会让人不去查。

### G-40 · 闸的协议外退出码被当成「活没干好」 —— **已修**

**严重度**：CRITICAL（这是 G-36 的代码面病根，且对所有接入项目都成立）· **发现修复**：2026-07-26

`gates.py::run_gates` 此前直接 `return GateResult(proc.returncode, ...)`，
而 `GateResult.gate_broken` 的判据是 `code == 2` **精确匹配**。于是任何协议外的退出码——
脚本语法错、`set -u` 撞未赋值变量的 **127**、被信号杀死的 **128+N**——都会落进
`passed=False` 且 `gate_broken=False`，报出来是**「质量不合格，但一道失败的闸都没有」**。

**这正是 SPEC §5.2 自己写着必须区分的两件事，被代码违反了。**
G-36（eco-ob 的闸在测试失败路径上以 127 退出）只是这个病根的一个实例——
修了那个脚本，别的接入项目照样会撞。

**修法**：`run_gates` 归一化——非 0/1/2 一律按 2（闸自身故障）处理，
把原始退出码与常见来源写进 stderr，且**保留崩溃前已解析出的闸行**（那是排查线索）。
3 个红绿测试锁住：127/3/126/137 全判 gate_broken · 0/1/2 原样保留 · 崩溃前的结论不丢。

### G-41 · 并行派单的竞态实测（做 `--parallel` 之前的前置测量）

**触发条件**：实现 `--parallel N` 之前必须先修下表的 ⚠️ 两条。· **测于**：2026-07-27

| 场景 | 实测结果 | 判定 |
|---|---|---|
| 8 个**不同**任务名并发建 worktree | **8/8 成功** | ✅ 真实并行场景安全 |
| 16 线程并发写台账（`path.open("a")` 追加短行） | 16 行齐全、0 行无法解析 | ~~✅ 未损坏~~ ⛔ **结论已被推翻，见下** |
| **同名**任务同秒并发建 worktree | 4 路只成 1 路，其余报 `fatal: cannot lock ref 'refs/heads/devloop/same-<戳>'` | ⚠️ **会撞** |
| 同名任务同秒的回执文件名 | `<戳>_<任务名>.json` 完全相同 | ⚠️ **会互相覆盖** |

**根因**：`worktree.py::create` 与 `dispatch.py` 的回执命名都只用 `time.strftime("%Y%m%d-%H%M%S")`，
**秒级粒度**。不同任务名不会撞（名字本身区分开了），**同名任务并发就撞**——
重试同一单、或两份任务书文件名 stem 相同时会触发。

**修法**：时间戳后加短唯一后缀（进程内计数器或 4 位随机），分支名与回执名同步改。
⚠️ 改回执名会影响 `_review/p4/aggregate.py` 的 `f.stem.split("_", 1)` 解析，一并改。

**⚠️ 顺带记一次我自己的误判**：第一版竞态测试跑在 `/tmp/racetest`，
而 `pathlib.Path("/tmp/...")` 在 Windows 上解析成 `C:	mp\...`，与 Git Bash 的 `/tmp` 不是一个地方。
4 路全失败报 `NotADirectoryError`，**我据此直接写了「会撞名」的结论**——
那个报错跟撞名毫无关系。用真实 Windows 临时目录重测才拿到上表。
**又一次从不相干的信号推结论**（第六条纪律）。

#### ⛔ 订正（2026-07-30）：上表第 2 行的结论**不成立**，且是危险的那一类不成立

⚠️ **原记录保留在上表，⛔ 不删。** 值钱的不是「未损坏」这个结论——
值钱的是**「一次试验就敢给一个并发性质下定论」这个错误本身**，
删掉它等于把教训一起删掉。

**复现脚本**：`tools/probe_ledger_concurrency.py`（0e2518e 新增，三组配置**各 40 次**）

| 配置 | 40 次里丢行 | 40 次里出坏行 |
|---|---|---|
| G-41 原样：16 线程 × 1 行 · **等长** | ⚠️ 未记录 | ⚠️ 未记录 |
| 8 线程 × 80 行 · **不等长** | **40 次** | **40 次** |
| 16 线程 × 40 行 · **不等长** | **40 次** | **40 次** |

⚠️ 第一行写「未记录」是**如实留白**：提交正文只报了不等长那两组的读数，
⛔ 不许替它编一个（脚本本身三组都跑，要数字自己跑一遍就有）。

其中一次直接写出**非法 UTF-8 字节**，连 `read_text` 都抛 `UnicodeDecodeError`。
中间读数（`1659c49` 正文）：8 线程 × 80 行**无声丢 18 行**。

**原试验为什么会绿**：① 只跑了**一次**；② 用的是**等长短行**——
而真实台账的行长差好几倍（带 `error` 文本的行比没有的长得多）。
⚠️ 等长短行把竞态窗口缩到几乎撞不上，**试验输入的形状不对，结论就是运气**。

**⛔ 代价不是「少了几行日志」**：自动驾驶的每一条防线（花了多少 / 派了几次 /
哪些绿了）**全都只从台账读**。
· 丢行 → 防线读到偏小的数 → 预算永不到顶、已绿的单被重派（**重复花钱**）
· 坏行 → 整个台账读不出来 → **防线全部归零**

**修法（0e2518e）**：
· 写侧 `_append()`：进程内 `threading.Lock`（盖 `--parallel`）＋文件锁
  （盖 `--detach` 与前台并存的**跨进程**情形）＋ fsync。
  ⛔ 拿不到文件锁也照写——丢一行日志远好过丢一单活。
· 读侧 `load()`：逐行 try，⛔ **坏行必须抛**（`LedgerCorrupted`），
  不许静默跳过——静默跳过等于让「已花多少」偷偷变小。并用 `errors="replace"` 读。
· 修后复跑：三组各 40 次，**零丢行零坏行**。

**⭐ 教训（比结论贵）**：一次试验只能**证伪**，⛔ 不能给并发下「安全」的定性。
要下定性，最低限度是「重复多次 ＋ 输入形状贴近真实」，
而且要把**试验用的形状写进记录里**——上表当年没写「短行、等长」，
所以后来读的人（包括我自己）无从判断那个绿有多脆。

### G-42 · token 去重取错了帧，所有子代理成本被低估 —— **已修，且订正后结论更糟**

**严重度**：CRITICAL（它错的是**已经对外报出去的结论数字**）· **发现**：2026-07-27 独立审查

**事实**：会话记录里同一条 `message.id` 会出现多帧。我的去重保留了**第一帧**，
而子代理会话是**流式**落盘的——`output_tokens` 逐帧增长，第一帧接近 0。

| | 首帧（错） | 末帧（对） | 低估 |
|---|---|---|---|
| C 臂输出 token | 33,610 | **310,749** | 9.2× |
| C 臂成本 | $19.89 | **$26.82** | 1.35× |
| 拆单设计输出 token | 12,316 | **377,204** | 30.6× |
| 拆单设计成本 | $18.54 | **$27.67** | 1.49× |

**订正让结论更糟，不是更好**：初版报「中口径 A 便宜 4%」，实际是 **A 贵 5%**；
摊薄点从第 11 单变成第 **12** 单——而本轮跑了 11 单，**连打平都没到**。

**⭐ 我的自检为什么没抓到（这条比缺陷本身更值钱）**

我确实写了自检，判据是「去重后的结果必须与回执 `usage` 逐字段相等」，而且**它通过了**——
在**工人会话**上。工人会话由 `claude -p` 子进程产生，那种记录里第一帧就等于最后一帧
（实测：同 id 多帧且 output 不同的有 **0** 个），首帧/末帧无差别。

**但同一个函数还被用在子代理会话上，而那种会话从未被自检覆盖**
（C 臂 121 条多帧消息、设计工作流 144 条）。

→ **自检通过 ≠ 所有输入都验过。「自检的覆盖面本身也要被检查」。**
这是「假绿」的**第六种形态**：**自检存在、判据正确、结论正确——但只覆盖了一部分输入**。
它比前五种更隐蔽，因为你手里真的有一份通过的自检报告。

**修法**：`compare_arms.py::session_tokens` 改为后写覆盖（保留末帧），
docstring 里写明两个坑与自检的覆盖面局限。所有已发布数字已全部订正。

### G-44 · 拆单成本的 F/V 拆分 —— **已完成，且推翻了初版的摊薄点**

**触发条件**：已完成（2026-07-27）· 明细见 `_review/p4/COST-SPLIT.md`

| 桶 | 金额 | 换项目要重做吗 |
|---|---|---|
| P 每项目一次性（勘察该项目的文档/代码/就绪度） | $8.65 | ✅ 要 |
| F 跨项目一次性（任务书模板 + 抽查协议 + 汇总） | $13.07 | ❌ 不用 |
| M 研究性开销（成本实验的设计） | $5.94 | — 不服务于工作包 |
| **V 每批必付** | **$0.00** | — **机械替换，零模型调用** |

**V ≈ 0 是最重要的发现**：11 份任务书由脚本从模板机械生成，
`u02` 与 `u03` **相似度 97.2%**、140 行里只差 8 行变量。

**因此初版「摊薄点 = 第 12 单」是错的** —— 那个算法把 $27.67 全记在一批账上。
订正：首批回本点第 **10–16** 单（取决于贵模型口径），**稳态下预付为 0、第 1 单即回本**。

⚠️ **但仍不能宣布划算**：稳态从未实测（是推算）· 质量差距未改善（覆盖 27% vs 84%）·
宽口径（主循环开销）仍缺席。

**上一版错在哪**：它假设那 10 份任务书是模型写的，据此论证「94–96% 花在重复吐骨架」。
**给一笔钱做归因之前，先去看那笔钱实际买到了什么产物，不要从「它理应怎么产生」倒推。**

### G-45 · 「稳态预付为 0」已实测成立 —— **但要加一条限定，否则会误导**

**测于**：2026-07-27 第二批（同项目、新表面、7 单）· 明细见 `_review/p5/RESULT.md`

**成立的部分**：模板直接复用、勘察用脚本机械完成、任务书机械替换 → **模型侧预付 $0**
（第一批是 $21.72）。生成脚本设了硬失败：占位符挖不出来就报错退出，**它没有触发**。

**必须加的限定**：**7 单里 3 单撞轮数上限**（`truncated=True`，report 长度 0，白跑），
补跑用 `--max-turns 80` 才过。三单全是读多写少型（BUILD_LOG 1891 行、15 个文件逐个读头注释）。

**拆单尺寸是模板抽象不掉的那部分。** 机械替换省掉了「设计」的钱，
省不掉「判断这一单多大合适」的脑力，而判错的代价是重跑——本批浪费执行成本约 $0.13（占 29%）。

→ **正确说法**：稳态下**模型侧**预付为 0；**判断力侧**不为 0。
不加这条限定，「预付为 0」是一句会误导人的话。

### G-46 · 拆单尺寸缺机械判据

**严重度**：MEDIUM · **触发条件**：下一批派单之前

本批凭第一批的经验给 `--max-turns 30`，而这批要读的东西大一个量级，3/7 撞顶。
我**知道** `num_turns` 与 `--max-turns` 不是同一个量（G-35），但拆单时没做换算。

**要的东西**：目标总行数 ÷ 每轮可读行数 → 建议轮数。
派单前算一次，比事后看 `truncated` 便宜得多。

### G-33 复发计数

两批共 **4 次**触发（第一批 3/13，第二批 1/7）。条目一直标着「待改」。
**够了——下一批之前改成无转义规则的分隔格式。**

### G-46 的解法记录 —— **我的第一个方案被自己的数据否掉了**

**原方案**：写一个估算器，按「取证面文件行数 ÷ 每轮可读行数」推荐轮数。

**用 7 个真实数据点一标定就垮了**：它在 7 单上**全部报警**，包括本来就够用的 4 单。
全报警 = 零信号，比没有更糟——它会训练人忽略警告。

再换「只数对账目标行数」的模型，仍不成立：

| 单元 | 目标行数 | 实际轮数 | 行/轮 |
|---|---|---|---|
| u17 project-context | 214 | 72 | **3.0** |
| u18 prototypes | 59 | 7 | 8.4 |
| u12 BUILD_LOG 前半 | 1,891 | 95 | 19.9 |
| u15 sim 头注释 | 3,175 | 70 | **45.4** |

**每轮读的行数差 15 倍。** 真正的驱动是「有多少条断言要逐个去核」——
而那个数**不读完就数不出来**。预测这条路走不通。

**⭐ 正解简单得多**：`--max-turns` 是**天花板不是预算**。按 token 计费，
**没跑的轮次产生零 token**——给高不花钱，给低了撞顶那单 report 为 0、**整单白跑、钱照花**。
默认从 30 改成 120（实测最大单用到 95），估算器删掉。

**教训**：我花力气去做「更聪明的预测」，而问题的实际形状是**失败的代价不对称**——
给高的代价是 0，给低的代价是白跑一单。**先看代价对称不对称，再决定要不要预测。**

### G-28 收尾 · 旧台账迁移（2026-07-27）

价目表接入解决了「以后」，旧行还是错的。`_review/migrate_telemetry.py` 迁移了 **26 行**
（DevLoop 2 · eco-ob 9 · eco-ob-p4 15），**26/26 都算得出真实价，零需要猜**。

三条纪律写进脚本：
1. **原始 token 是唯一可信输入** —— 旧行缺 token 的从回执回查（含已归档到 `_review/p*/reports/` 的）；
   都查不到就标 `null`，**⛔ 不从合成价反推**——反推要假设当时用的哪张表，那是在编。
2. **改前备份**（`*.jsonl.pre-g28.bak`），台账是唯一的历史记录。
3. **合成价保留**，改名为 `cost_usd_synthetic` 不删——它是错的，但它是**当时真实发生过的记录**，
   删掉就没法解释「为什么早期文档里写着那些数字」。

脚本**幂等**，已迁移的行原样跳过，可反复跑。

**迁移后的真实总账**：DevLoop $0.87 · eco-ob $0.05 · eco-ob-p4 $1.01，合计约 **$1.93**。

### G-33 收尾 · 换成无转义规则的分隔块（2026-07-27）

**修法**：报告末尾的结构化块从**手写 JSON** 换成 `@@DEVLOOP` 分隔块。

```
@@DEVLOOP v1
@@ITEM finding
verify: grep '双层\|double.layer' game/src/     ← 旧格式在这里死
doc_anchor: …与"卡在哪（现行）"段                  ← 旧格式在这里死
@@END
```

**核心保证不是「分隔符够罕见」，是「值和分隔符处在互斥的列位置」**：
结构行 ≡ 行首两字符为 `@@` 的行；而值只出现在 `键: ` 之后或 `+ ` 之后——
**行首必然不是 `@@`**。所以值里写什么都生不出结构行，分隔符短到 `@@` 也不用转义。
实测：值里塞进 `@@END` / `@@ITEM finding` / 一行代码围栏，仍只解析出 1 条，五行值一字不差。

**为什么不选另两个候选**（三份证伪全是 NEEDS_FIX，但性质不同）：
- **受限 JSON**：要求工人**改写内容**。实测 5.5% 既有条目（28/505）必须改写或降级；
  它给的替换示例 `grep '"id"'` → `Grep pattern=id[:]` 实测 **6 命中 → 0 命中**，
  而那条错误示例会原样进工人的提示词。
- **两段式**：砍掉 5 个字段，339 条审计痕迹降级成不可机检的自然语言。**明确倒退。**
- 分隔块只要求工人**换个排版，内容一个字符不动**——本项目纪律是「判定看产物」，产物保真优先。

**「不许砸掉现有」的证据**（每次改动后都跑）：
- 23 份历史报告 JSON→块→JSON **往返逐字段无损**（20 份可解析的全过，3 份是撞轮数上限本就没正文）
- `aggregate.py` 输出**逐字节不变**：`13 单 · 契约合规 10/13 · 矛盾合计 33 · $0.5626`
- `compare_arms.py` 输出不变：`池化 117 条 · A 27% · C 84%`
- 旧 JSON 仍能读（`load_report` 三条路：`block` / `repaired` / `json`）
- `repair_json.py` 与 `reports_repaired/` **保留不动**——4 份历史修复的来源，删了没法复现

**⭐ 顺带删掉一条无效的自查**：旧任务书要求工人「自己从头读一遍检查引号、逗号、括号配对」。
**4 份撞车的报告全部执行了它，并自评 `json_valid: true`（4/4）。**
工人手里没有解析器，这条自查**在架构上就是无效的**——它只能让人以为验过了。

**整合时自己逮到的**：`blockfmt` 的 docstring 写着「缺字段只告警」，而实现是
「缺一半以上判死」（理由正确：那不是笔误，是结构塌了）。**文档没跟上实现**，已对齐。

### G-47 · Phase 5 评测集 · 首跑 19/20（2026-07-27）

**它要回答的问题**（这是它存在的唯一理由）：换后端、改任务书模板之后，**判断力掉没掉**。
本项目已经改过两次模板（G-30 情态动词、G-33 报告格式），改完只验了「格式还能不能解析」，
**从没验过「判断还准不准」**。

**答案从哪来**：20 个样本全部由编排方在 2026-07-26/27 **亲手核实过**，
每条附判据 · 证据位置 · 谁怎么核的。不是模型自称的，不是从工人报告里抄的。
⛔ 全部锚在 `eco-ob-p4` @ `6f4b849`；跑分前核对基准，漂了就**拒跑**——
基准一变，分数就不再是判断力的度量，**而它看起来仍像个正常分数**。

**首跑结果**（deepseek-v4-pro · $0.33 · 单题 $0.017）：

| 类别 | 得分 | 测的是什么 |
|---|---|---|
| `broken`（11 题） | **11/11** | 能不能找出腐烂 |
| `holds`（6 题） | **6/6** | 会不会乱报（假阳性） |
| `undecidable`（3 题） | **2/3** | 知不知道自己不知道 |

**⭐ 唯一错的那题最有诊断价值**。c20 的原文是
「M1 参考数据重生成 **+ 38 断言全适配**」——前半句（文件在不在）能验，
后半句（断言全过）**必须跑测试**。工人验了前半句就答 `holds`，
**用可验证的半句去为整句背书**。这正是设这类题的原因。

**判分口径三条**（写进 `runner.py`，不是随口约定）：
1. **总分不是主指标** —— 11/20 的答案是 broken，「一律答 broken」也能拿 55%。分类看。
2. **答错的性质要分开** —— `broken` 答成 `undecidable` 是**保守**（漏了但没说错）；
   `undecidable` 答成 `holds` 是**硬判**；`broken` 答成 `holds` 是**说反了**。三者危害不同。
3. **n=20 只够区分数量级** —— ⛔ 不许报到小数点后一位，不许拿 60% vs 65% 说变好了。

**退出码只反映「跑完了没有」，不反映分数** —— 把分数变成退出码会诱导人去凑分。

**未做**：Langfuse 自托管、GitHub Actions 定期重跑。前者是给团队看趋势用的，
单人项目 `runs.jsonl` + `--compare` 够用；后者要等有稳定的重跑节奏再说。
**不做的理由要写下来，否则下次会当成「忘了做」。**

### G-48 · 评测集的三个缺口已补 · 换后端实测（2026-07-27）

首建时有三个缺口，**每一个都是「验了绿路径、没验红路径」**：

| 缺口 | 为什么是真缺口 | 怎么补的 |
|---|---|---|
| 只跑过一个后端 | 而「换后端能对比」正是它存在的**头号理由**——核心用途从没验过 | 加 `deepseek-flash` 跑同一批题 |
| `--compare` 只走过「没有历史」那条分支 | 真正的比对逻辑一次没执行过 | 有两次跑分后实测 |
| **没做红测** | **一个测不出质量下降的评测集，等于没有** | 用更弱的模型跑，看它掉不掉分 |

**红测成立，且掉分掉在正确的地方**：

| | v4-pro | v4-flash |
|---|---|---|
| `broken`（找腐烂） | 11/11 | **11/11** |
| `holds`（不乱报） | 6/6 | **6/6** |
| `undecidable`（知道自己不知道） | **2/3** | **1/3** |
| 成本 | $0.3311 | **$0.0929** |

**便宜 3.6 倍，掉的分全部集中在 `undecidable`。** 两道错题都是**硬判**——
`c18`（测试通过数，必须跑才知道）与 `c20`（「文件在」+「断言全过」的复合句）。
**弱模型都倾向于用能查的那半句为整句背书。**

→ 可直接用的结论：**任务不涉及「我判不了」的边界时，flash 够用且省 3.6 倍**；
一旦要它诚实地说「不知道」，就得用 pro。

`--compare` 实测输出：
```
c18  ✓→✗ **回归**  应答 undecidable，上次答 undecidable，本次答 holds
```

**换后端确实只是改配置**：`deepseek-flash` 与 `deepseek` 同端点、同钥匙、只换模型名，
代码一个字没动——这就是「换一个 api 的事情」的最小形态。

### G-49 · Phase 6 · 非阻塞派单已落地（2026-07-27）

**触发条件早被实测远超**。PLAN 写的是「当前同步派 3 单需干等约 2 分钟」，实际：

| 批次 | 干等 |
|---|---|
| Phase 4 首批 14 单 | 74 分钟 |
| Phase 4 第二批 10 单（p4-u02…u11 串行） | 54.6 分钟 |
| Phase 5 那 7 单前台（`--parallel 3`） | 18.1 分钟（其中 **u17 首批一行台账都没有，补跑于 20:19**）|
| 评测集 20 题 ×2（`--parallel 4`） | 9 分 05 秒 / 7 分 20 秒 |

**实测效果**：同样 7 单，前台干等 18.1 分钟 → `--detach` **1 秒返回**，
活在后台照跑 8 分 47 秒（`job.json`/`console.log` 的 mtime 与台账 7 行区间两条独立证据）。

> ⚠️ **2026-07-27 订正四个数字**（三路审查逐条实地核对，我原来写的是错的）：
> - 「7 单前台 50 分钟」→ **张冠李戴**。50 分钟那批是 Phase 4 的 p4-u02…u11 串行 10 单
>   （54.6 分钟），不是这 7 单；「10 分钟超时被切丢失的 u17」是 `p5-u17`，也不在那批里。
>   两批被我混成了一条。
> - 「后台跑完 12 分钟」→ **8 分 47 秒**，无任何证据支持 12。
> - 「评测集 29 / 32 分钟」→ 真墙钟 **9 分 05 秒 / 7 分 20 秒**。「29」的来源已闭合：
>   `format_run` 把**各题耗时之和**印成了「墙钟」（并发 4 下差 3 倍以上），代码已改；
>   「32」**没有任何可追溯来源**——不许顺手记成同一个 bug 的第二个受害者。
> - ⛔ 并且：`--detach` **不缩短执行时间**，只把等待从前台挪走。
>   把「50 分钟」对「12 分钟」并排写，读者会理解成派单变快了 4 倍。**没有这回事。**

**⚠️ 我把 PLAN 里捆在一起的两件事拆开了**：

| | 是什么 | 现在做吗 |
|---|---|---|
| **非阻塞派单** | 派出去就走 | ✅ 真痛点，已做 |
| **MCP 服务** | 让编排方不经 Bash 就能调用 | ⏸ 是**交付方式**，不是痛点。我有 Bash；Phase 7 的自动驾驶是 Python 进程、直接调用即可。它的理由是想有一个可对外展示的自写 MCP Server」），成立，但**不该和「非阻塞」绑成一个阶段** |

**三条设计要害**：

1. **进度读台账，不读输出文件、不猜进程名**（G-37 的教训：曾用「输出文件 0 字节 +
   `ps` 没匹配到」断定任务没跑，实际三次全跑了，多花两单）。
2. **Windows 上必须真脱离父进程**（`DETACHED_PROCESS | CREATE_NEW_PROCESS_GROUP`）——
   否则父进程一退子进程跟着死，「非阻塞」变成「静默丢活」。
3. **⛔ 作业记录只登记「派了什么」，不登记「结果如何」。** 结果的唯一真源是台账与回执。
   两处都记会分叉，而**分叉的账本比没有账本更坏——它看起来是权威的**。

**退出码**：`done` 且全过 = 0 · `done` 有失败 = 1 · **`running` 或 `died` = 3**。
⚠️ 「还在跑」绝不能返回 0——脚本会当成功往下走，那是自动驾驶最危险的失效模式。

**`--detach` 放在后端解析、假绿拦截、任务书预校验之后**：能当场失败的错误在前台就失败，
不会变成一个后台的哑巴作业。

**顺带记一次自己的误判**：验退出码时用了 `cmd | head -14; echo $?`，
拿到的是 `head` 的退出码，据此以为 `running` 返回了 0。
**用管道后的 `$?` 判断上游命令**——纪律与累计次数只在
[TROUBLESHOOTING 排查通用纪律第 6 条](TROUBLESHOOTING.md) 一处维护
（同一份文档第 7 条自己写着「可数的计数器必然分叉」，这里不再记次数）。

---

### G-50 · 急停落地 + 四条自查裁决 + 一条取舍（2026-07-27）

**为什么记**：这四条裁决原本只活在 `f974cb2` 的 commit message 里。
BACKLOG 自己的开场白是「口头清单会蒸发」——commit message 比对话稍好，
但**同样不是可检索的台账**。

#### 急停（`devloop/halt.py`）

Phase 6 之前，一批活跑在前台，Ctrl-C 就停了。有了 `--detach` 之后作业脱离父进程活着
——**没有急停就没有任何办法叫停它**。SPEC 把它标成「Phase 7 前置」，位置对，
但它已经从「将来要做」变成「现在就缺」。四条设计纪律：

1. **默认只列不杀**（与 `prune` 同源：跑着的作业里可能有已花钱、快产出的单）
2. **⛔ 必须说清「已经花掉的钱不会退」**（不说，人会以为「停了 = 没花钱」）
3. **杀进程树不是杀单个 pid**（作业进程只是调度器，真正烧钱的是它派生的 `claude`
   子进程；只杀父进程会留下**一地还在烧钱的孤儿，而你以为已经停了**）
4. **⚖️ running 与 died 分开列**（死作业是要清理的**残骸**，不是要杀的目标；
   混在一起会让一个陈旧的死作业使 `halt` 永远返回非 0 且永远清不掉）

#### 四条自查的裁决

| # | 自查项 | 裁决 |
|---|---|---|
| ① | `--detach` 遇上 subagent 后端 | **是缺陷，已修**。subagent 分支在 detach 之前返回，flag 被静默吞掉。**静默吞掉用户明确给出的参数**是本项目最忌讳的那类行为 |
| ② | 起进程失败会不会被报成成功 | **不是缺陷**。故意用不存在的子命令起，`status` 明确报 `died` 并点名未完成单元，console.log 里有完整 traceback（Python 的 stderr 即使重定向也是行缓冲）。<br>⚠️ 后来有一份反证材料说「console.log 是 0 字节」，**那份材料举证错了**：那个作业是被 `halt --kill` 强杀的，被 `taskkill /F` 打掉的进程当然不 flush。裁决②原文成立 |
| ③ | 停不掉跑飞的作业 | **是缺陷，已修**（即上面的 `halt`） |
| ④ | 两作业串台账 | **⚖️ 已知取舍**，见下 |

#### ⚖️ 取舍 · 两个作业的台账互串

**成立条件**（三个必须同时满足）：同一项目 + 同时在跑 + **单元名重叠**。
此时旧作业的时间窗是开口的，会把新作业写的行算进自己的进度。

**为什么不现在修**：单元名重叠就意味着在重跑同一批，那时本来就该看最新的结果。
而根治要动台账格式（`telemetry.record` 写入 `job_id`、`done_units` 按它过滤），
属于 schema 变更，代价大于当前收益。

**⚠️ 但要说清**：G-51 的「按去重单元名计进度」**不解决这一条**——那条治的是
单作业内同名两行，这条是跨作业。两者同源不同刀。

**根治方案已想好，记在这里备用**：台账行加 `job_id`，`done_units` 按 `job_id` 过滤。

---

### G-51 · Phase 6 三路独立审查：七条真缺陷（2026-07-27，已修）

**怎么来的**：三路并行审查（jobs 代码 · 评测集 · 文档落盘）+ 每路配证伪。
⚠️ **证伪那一轮推翻了审查自己的 4 条**（2 条凑数、1 条举证张冠李戴、1 条把
「题目歧义」误报成「答案标错」）——审查方的结论同样要验，这是本项目的老规矩。

#### 一处根因，三个后果 —— `--detach` 的 argv 从进程 argv 反推

```python
argv = [a for a in sys.argv[1:] if a != "--detach"]     # 旧
```

| 后果 | 实测复现 |
|---|---|
| **a. 无限自我重生** | argparse 默认开前缀缩写，`--det` 被当成 `--detach` 却不等于字面量，原样传给子进程 → 子进程又走一遍 detach 分支。3 秒内 16 个作业目录，约 5 个/秒，**一单活都不干** |
| **b. 派出去的是别的命令** | 程序化调用 `main(argv)` 时取的是 `sys.argv[1:]`。把 `sys.argv` 设成 `["pytest","-q","tests/"]`，抓到的子进程真实命令行就是 `python -m devloop.cli -q tests/`，而 stdout 照样打印「已在后台启动」、rc=0。**Phase 7 自动驾驶正是程序化驱动 `main()`——这条会炸在自动驾驶第一天** |
| **c. 静默改派到别的仓库** | 子进程 cwd 被硬设成工具包目录，而**那个目录自己就有一份合法 `.devloop/`**。相对 `--project` 于是解析成 devloop 仓库：不报错，只是拿着钱去干了另一个仓库的活 |

**修法**：`allow_abbrev=False` + `_rebuild_argv(args)` 从**解析结果**重建（路径一律
`.resolve()`）+ cwd 留在用户所在目录（包路径改由 `PYTHONPATH` 提供）。

> ⚠️ `jobs.py` 原有一句注释：「⛔ 不在这里重新拼参数：拼两遍必然分叉」。
> **那个顾虑是对的**，而新修法正是它警告的做法。所以配了一条不变量测试把分叉钉死：
> **重建的命令行喂回解析器，必须得到同一个请求**（5 组参数组合）。
> 改那个函数时，这条测试是唯一的守卫。

#### 其余六条

| 条 | 是什么 | 为什么危险 |
|---|---|---|
| **进度按台账行数计** | 同一单元两行 → 报「2 单跑完，全部通过」rc=0，而另一单一次没跑 | **假绿**。单作业内手工重跑一单即可触发 |
| **`status --all` 恒返回 0** | 真项目实测 died 0/7 仍 rc=0；单作业路径 rc=3 | 直接违反刚写进 SPEC 的退出码契约。**它是从没被测过的那条路径**——97 条测试没有一条覆盖 `cmd_status` |
| **评测把答案写进被考仓库** | 20 份含 `VERDICT` 的报告落进 `eco-ob-p4/.devloop/reports/`，工人 cwd 就在那里、readonly 没有路径白名单 | 第二次跑分是在第一次的 20 份答案已在盘上的情况下跑的。⛔ **不能靠禁令挡**——禁令本身还会把答案位置说出来 |
| **`_extract` 漏一半写法** | 实测 20 种写法漏 10 种，Markdown 加粗恰好是模型最常见的那种 | 把**格式问题**计成**判断错误**，进同一个分母 |
| **`run_one` 丢弃失败信号** | 超时、撞满轮数、跑在别的模型上，一律降级成「这题答错了」 | 正是 `models.py` 自己写下的教训：「轮数不够是我的拆单错误，不是模型能力问题」 |
| **`compare()` 吞掉回归** | 历史里夹一次 `--only`，下一次全量比对只跟那几题比。实测 19 题回归被印成「完全一致——没有回归」 | **这套东西存在的唯一理由就是那句话，而它当时是错的** |

#### 附带修掉的

`console.log` 缺 `-u`（作业跑完之前恒 0 字节，而 CLI 把它当看进度的手段递给用户
——G-37 第 2 条在工具内部原样复发）· `pid_alive` 只看 pid 不看映像名（pid 复用会让
死作业永远 running，且 `halt --kill` 会杀无辜的进程树）· `load()` 静默跳过缺 `job.json`
的目录并回退报更早作业「全部通过 rc=0」· `halt --kill` 杀完不留痕 · POSIX 下缺
`start_new_session`（`killpg` 会带走调用者自己的 shell）· `severity` 三分有一格落在
分类外 · `--only` 传未知题号静默跑 0 题并写进历史 · `.devloop/jobs|handoff` 未进
`.gitignore`（**已经出过事**：一次探针残留被 `git add -A` 扫进了提交 f974cb2）。

#### ⚠️ 数字订正（我自己写错的）

G-49 里四个数字全错：「7 单前台 50 分钟」是张冠李戴（那是 Phase 4 另一批的 54.6 分钟）·
「后台 12 分钟」实为 8 分 47 秒 ·「评测集 29 / 32 分钟」实为 9 分 05 秒 / 7 分 20 秒。
「29」的根因已闭合（`format_run` 把**各题耗时之和**印成了「墙钟」，并发 4 下差 3 倍以上）；
**「32」没有任何可追溯来源，直接删掉**——不许顺手记成同一个 bug 的第二个受害者。

#### 这次没做的（有理由）

- **`prune` 接管 `.devloop/jobs/`**：⚖️ 不做。正在跑的作业目录持有 `console.log` 句柄，
  `unlink`/`rename` 实测抛 `WinError 32`；且**作业目录是尸检材料**，自动删它与
  「进度读直接证据」的纪律相斥。代价写进 SPEC §4。
- **`halt.kill` 的真 `taskkill` 自动化测试**：不做（要先起真进程，平台强绑定），
  改为在 SPEC 里如实标注「手工实测，无自动化红测」。
- **c20 换一道题 / undecidable 扩到 8–10 题 / run-to-run 方差测量**：下次动评测集时做。
  现有 3 道 undecidable 全部出自同一份文档同一节同一种句式，**有效样本远小于 3**。
- **对外撤回一句结论**：「掉的分全在 undecidable，2/3 → 1/3」**不成立**——
  Fisher 精确检验 p=1.0，Clopper-Pearson 区间几乎完全重叠。
  正确说法是「两个后端在这 20 题上**没测出差别**」。

---

### G-52 · 评测集清洗后的干净基线（2026-07-28）

**前提**：G-51 修完六条缺陷之后重跑。此前的三次跑分**全部作废**，理由各不相同：

| 跑分 | 作废理由 |
|---|---|
| 07-27 21:45 pro · 22:00 flash | 回执落在**被考仓库**里，第二次跑分时第一次的 20 份答案已在盘上 |
| 07-28 00:02 flash | 跑在 `--max-turns 30` 下，**三题撞顶被截断** |

#### ⭐ 新发现：评测的轮数上限漏了 G-46 的订正

派单侧早在 G-46 就把 `--max-turns` 从 30 提到 120（理由：**没用掉的轮数不花钱**），
**评测侧漏了**，一直是 30。实测后果：

- c13 / c19 / c20 三题全部停在 `num_turns=31` —— 即**三题都是撞上限**，不是答错
- c01 用了 29、c05 用了 30 —— **全在边缘**，随时会掉下去

⛔ 旧代码会把这三题记成「模型答错」，让 flash 白背三口锅。
这正是 `models.py` 自己写下的那条教训：**「轮数不够是我的拆单错误，不是模型能力问题」**。

**反直觉的实测**：上限提到 120 之后，flash **更便宜也更快**
（$0.1020 / 5.0 分钟 ← $0.1191 / 8.8 分钟）。
原因：撞顶的单会把 30 轮全烧光然后失败；给足余量反而早早收工。
「给高不给低」不只是保险，在这里是**净赚**。

#### 干净读数

| | pro（deepseek-v4-pro） | flash（deepseek-v4-flash） |
|---|---|---|
| 总分 | **20/20** | **19/20** |
| broken（能不能找出腐烂） | 11/11 | 11/11 |
| holds（会不会乱报） | 6/6 | 6/6 |
| undecidable（知不知道自己不知道） | 3/3 | **2/3** |
| 成本 | $0.2959（单题 $0.0148） | **$0.1020**（单题 $0.0051） |
| 真墙钟 | 7.6 分钟 | 5.0 分钟 |
| 轮数中位 | 13 | 15 |

**flash 唯一错的是 c18，性质「硬判」**：它能 grep 到四个 `EXPECTED_ASSERTIONS` 常量
（31/45/38/23），就以为整句成立——**漏掉了「失败数 = 0」那半句必须运行才知道**。
⭐ 这道题的判据刚在 G-51 里被订正过，现在量的正是该量的东西。

#### ⛔ 结论要怎么说

**「20 题上没测出质量差别（差 1 题，n=20，Fisher 精确检验 p=1.0），而 flash 便宜 2.9 倍。」**

⛔ 不许说「flash 质量掉了」——1 题之差在 n=20 上什么都说明不了。
⛔ 也不许说「两者一样好」——没测出差别 ≠ 没有差别，n=20 只够区分数量级。

#### 顺带验证了三条修法

1. **回执隔离**：两次跑分期间，基准仓库 `.devloop/reports/` 恒为 7 个文件
   （只有真实工作回执，零评测答案）。
2. **派单失败不计入分母**：截断那次报「17/17 + 3 题没取到读数」，
   而不是旧口径的「17/20」。**信息量完全不同**。
3. **作废的跑分不参与趋势比对**：`--compare` 明说「跳过 3 次已作废的跑分」，
   ⛔ 不静默——静默跳过会读成「历史就这么长」。


---

### 🔴 G-53 · 闸可以「一条都没验」却报「全过」（2026-07-28，已修）

> ⚠️ **这条正文欠了一天。** 2026-07-28 写它的时候 shell 的工作目录被重置，
> 内容落到了 `C:\pg` 下一个野文件里；我发现后只搬回了 G-54，**漏了 G-53**。
> 于是索引写着「✅ 已闭」并给了链接，点过去什么都没有——
> 53 条索引里唯一缺正文的一条，而它恰恰是那一轮最危险的缺陷。
> 是第三轮独立审查把这笔账翻出来的。

#### 实测复现（两种形态，都返回 `passed=True`）

```
A 全 SKIP  → code 0 · passed True · gate_broken False · summary「0 过 / 0 未过」
B 零输出   → code 0 · passed True · lines 0
```

根因：`GateResult.passed` 只看 `code == 0`，而 `_parse` 不要求任何行存在。
**于是 `gate_ok == True` 不代表任何东西被验过。**

这是六种假绿里的第一种（守卫的目标不存在），只是更彻底：
**一个目标都没有，还报全过。**

#### 现实触发路径不是假想

`C:\pg\eco-ob\.devloop\gates.sh:81-82` 真有这个分支：

```bash
if [ "${DEVLOOP_SKIP_TESTS:-0}" = "1" ]; then
  skip "M1 回归" "DEVLOOP_SKIP_TESTS=1（仅用于闸自身的快速自测，不得用于真实验收）"
```

那句「不得用于真实验收」是写给人看的，**对机器没有任何约束力**；
而 `gates.py` 把 `os.environ` 原样透传给闸。

⛔ **操作者 shell 里一个环境变量，能把真实验收闸变成空转，而工具报「全过」。**

真实项目实测对照（`devloop gates --project C:/pg/eco-ob`）：

| | 旧行为 | 新行为 |
|---|---|---|
| 正常 | 1 过 / 0 未过 ✓ | 1 过 / 0 未过 ✓（不变） |
| 带 `DEVLOOP_SKIP_TESTS=1` | **「0 过 / 0 未过」，passed=True** | **闸自身故障**，且点名生效中的开关 |

#### 三条修法

1. **退出 0 但一道 PASS 都没有 → 归为 2（闸自身故障）。**
   ⛔ 与「活没干好」必须分开，否则环境故障会被当成质量问题。
2. **`require_pass` 点名验收。** 修掉「空转即绿」之后，5 道闸里 4 道 SKIP、
   1 道 PASS 仍然是「绿」——那正是 **G-42 那种假绿**（自检存在、判据正确，
   **但只覆盖了一部分输入**）。
   点名的闸**不存在** → 2（守卫的目标不存在）；**是 SKIP** → 2（没真正执行）。
3. **生效中的跳过开关必须报出来。** ⚠️ 不粗暴剥离（闸的快速自测要用它），
   但人有权在看结果之前知道它开着。

#### ⚠️ 说辞的订正（第三轮审查补）

早先这条的错误消息印「一道都没验」并附三条**猜**的原因。
而 eco-ob 有一条**合法**分支会长成同样的形状：测试真跑了 486 秒、
38 条断言全过，只有墙钟性能断言没达标 → 闸 `skip "M1 回归"` → 0 PASS + exit 0。
那时「一道都没验」是假话，三条猜测也全不成立。

**⛔ 判定不放宽**——闸自己那行 SKIP 的原话就是「非工人问题，
**但也未证明代码正确**」，没证明就不能放行。
要改的是解释：**把闸自己说的话摆出来，不要猜**。

#### 一条更一般的教训

新增的 `GateResult.verified` 是**真正验过并通过的道数**。
「验收有没有发生」的度量是它，**不是退出码**——
退出码只说明脚本自认为没问题，它可能一条都没验。

红测 11 条，其中 4 条防回归。真实闸双向验过。

---

### 🔴 G-54 · 分支劫持：产出会落到用户主线上，而工具报「已固化到隔离分支」（2026-07-28，已修）

**怎么发现的**：宪法设计审查的「逃逸路径」那一路。与 G-53 同级，**今天就活着**。

#### 根因

`commit_result` 用 `git commit` 提交，而 **`git commit` 落到哪个分支完全取决于
此刻的 HEAD**；`self.branch` 只是建 worktree 时记下的一个字符串，
提交时**没有任何一处拿它和 HEAD 对过**。

而那句提交信息里写着「仅落在隔离分支 …，未触碰主线」——
**在加断言之前，那句话是工具在替自己作证，没有任何东西保证它为真。**

#### 实测复现（临时仓库 + 真 `commit_result`）

工人在 worktree 里跑一句：

```bash
git symbolic-ref HEAD refs/heads/master
```

> ⚠️ 这句比 `git checkout master` 强得多：**主 worktree 占着 master 时它照样成功，
> 且一个文件都不动**，所以既不会报错也不会留下痕迹。

读数：

```
master 初始         1b17820
commit_result 返回   8a56130
master 现在         8a56130   ← 工人的提交落在主线上了
隔离分支现在         1b17820   ← 一个提交都没有
```

CLI 随后会打印「分支 devloop/… @ 8a56130（产出已固化，删 worktree 不会丢）」
——**一句假话**。而且 `prune` 会把那个空分支判成可删。

同时这直接违反宪法 **C-4**：任何触及用户主线或集成分支的 `git commit` 都要人批准。

#### 修法

`commit_result` 函数体第一件事：断言 `HEAD == self.branch`，不等就 `raise BranchHijack`。

三条要点：

1. **⛔ 断言放在函数里，不放调用方。** 放 `cli.py` 只护得住一条调用路径——
   测试、`gates --commit`、将来的自动驾驶全都绕得过去。
2. **⛔ 绝不 `return None`。** 那会让 CLI 打印「工人没有产出，无可固化」，
   而工人明明有产出，且整批还会返回 0 ——
   **G-26（产出静默丢失）+ 静默降级，双重复发**。
3. **异常消息必须说清产出还在哪。** 拒绝提交 ≠ 丢掉工人的活。
   `raise` 走 `cli.py` 已有的异常路径：本单判失败、打印原因、
   打印「worktree 保留在 … 供排查」，**产出留在磁盘上**。

单独一个 `BranchHijack` 异常类：让调用方能把「产出落错地方」与
「工人没产出」「闸没过」三者分开——它们的正确处置完全不同。

#### 判据维度的实测确认

- 带斜杠的分支名：`git rev-parse --abbrev-ref HEAD` 返回完整的 `devloop/x-123`，可等值比较
- detached 状态：返回字面量 `HEAD`，与分支名不等 → **同样被拦下**
  （别让它掉进「碰巧不等于主线所以放行」）

红测 5 条，其中 2 条防回归（正常路径照常固化到隔离分支且主线不动 · 无改动仍返回 None）。
修后在同一个复现场景上验过：`BranchHijack` 抛出，**master 一个字节没动**。

测试 172 → 177。

---

### G-55 · Phase 7 自动驾驶：宪法 + 阶段计划 + 失控防线（2026-07-28）

**顺序是硬的**：宪法必须先落地。没有它，「自动驾驶」就是「让模型自己决定
什么时候该问人」——那等于没有。

#### 宪法：从散文变成可机械执行的判据

此前宪法只是 PLAN 里的一段散文。而这个项目在别处的铁律是
**「完成由退出码裁决，不由模型自述」**——一段模型读完承诺遵守的散文，
正是它到处在否定的东西。

**三文件分离**：`constitution.md`（给人读，⛔ 任何脚本不得解析）·
`constitution.toml`（给脚本读）· `../.devloop-anchors/<项目>.json`（指纹，**在项目外**）。

理由是自己踩出来的：`rules-digest.md` 里白纸黑字写着「`.devloop/` 任何文件禁改」，
而**真正执行它的是 `gates.sh` 里那段脚本**。宪法若只写成散文，
就是第二个 rules-digest：守卫的目标不存在。

**三条反测**（比「命中能不能抓到」重要得多）：

1. **守卫的目标不存在** —— 清单写错路径 → `load()` 当场炸。
   清单写错**比没有清单更坏**：它会每次都报「无命中」。
2. **只覆盖了一部分输入**（G-42 形态）—— 判不了的条款必须显式登记，
   ⛔ 且 `summary()` **每次**都印「未覆盖 N 条：…」。
   **「零命中」不带这条尾巴，本身就是假绿。** 一条都没登记 → 拒绝加载。
3. **宪法自己被改** —— 宪法在自己的清单里，锚在项目外，
   ⛔ 绝不以现值为新基准。**能自愈的锚不是锚。**

#### ⚠️ 落地时两次「差点在自己的守卫上犯同一个错」

都是被自己的红测抓住的，都属第一种假绿：

| 差点犯的错 | 实测 |
|---|---|
| `git ls-tree -- 'ref/*.json'` | **一行都匹配不到**；`:(glob)` 魔法 ls-tree 也不支持。按直觉写的守卫会永远报「无命中」 |
| git 默认转义非 ASCII 路径 | `"a/\345\201\267.json"`。守卫会**静默漏掉每一个中文名文件**——而 eco-ob 的 `01_设计文档/` 正是中文名，**正是宪法 C-1 要保护的那批**。守卫会恰好在它最该保护的路径上变成空的 |

修法：从 git 取全量清单、在 Python 里匹配（两侧同一套逻辑）；
全部 git 调用加 `-c core.quotepath=off`。

#### 阶段计划（此前完全不存在）

`TaskSpec` 只有 `path` + `body`。新增 `devloop/plan.py`，TOML 载体——
**主论据是「计划是人写的，必须能写注释」**，`eco-ob/.devloop/config.toml` 里
那 5 行解释「为什么必须同步 `game/.godot`」的注释就是活证据：
没有它，下一个人会把那行配置当成无用的删掉。JSON 做不到。

⛔ **没有机器判据的单必须显式写明理由。** 不强制这一条的话，
「忘了写验收标准」和「这单确实没法机检」在文件里**长得一模一样**——
而前者是漏洞，后者是已知代价。

#### 四条失控防线

都在花钱**之前**检查，顺序是「先查不依赖成本的」：

| # | 防线 | 为什么这样定 |
|---|---|---|
| 1 | **派单次数绝对上限** | ⭐ 不依赖任何成本计算。价目表缺某模型时 `cost_usd_real` 是 `None`，把 None 当 0 的话**预算永远不会耗尽** |
| 2 | 算不出成本 → 停 | ⛔ 「算不出」≠「花了 0 元」 |
| 3 | 已花 + **预留** > 上限 | 预留必须是单单成本的**高分位不是平均**——按平均留，一单贵的就冲过头 |
| 4 | 墙钟 + 空转看门狗 | ⚠️ **K 没有数据支撑，我说没有**：真实台账里「写路径 + 闸」只有 3 行。默认 3 是保守的可解释初值（够一次「失败→重试→换法」），**不是标定出来的**——这句话印在停机结论里 |

#### 崩溃恢复：进度只从台账重算

⛔ 与 `jobs.py` 同一条纪律：自己的记录只登记「派了什么」，不登记「结果如何」。
**分叉的账本比没有账本更坏——它看起来是权威的。**
checkpoint 只存台账里没有的东西；恢复时每轮从台账重算，按任务名去重、后写覆盖先写。

#### 其他裁决

- 失败的单**可以重试，但不解锁下游**——依赖一个失败的产物往下干只会把错误铺开
- 无人值守**硬性要求**宪法在位且锚对得上。前台派单没宪法只是警告（人在看着）
- 退出码 3 = 「停下来了，要人看」。⛔ 除了「跑完了」，一切停机都要找人，
  **包括「不知道为什么停的」**

#### ⏸ LangGraph 尚未接入 —— 触发条件

PLAN §14 的定案（JD 依据成立），但本轮没接，两条实测理由：

- **34 个传递依赖 / 6.6 MB**（含 langsmith、httpx、requests、websockets），
  而本包现在只有 **1 个**依赖
- `SqliteSaver` **不在其中**——要另装 `langgraph-checkpoint-sqlite`（实测 3.1.0）

装它会改变用户的 Python 环境，那是用户的决定。
**触发条件**：用户明确要装、或需要一个可对外展示的自写 MCP Server 时。
本轮先把自研状态机做成能跑的，LangGraph 作为**可选层**后接
（`pyproject.toml` 的 `[autopilot]` extra，核心与全部测试不依赖它）。

#### 真实验证

devloop 自己身上端到端演练：读计划 → 宪法前置（锚对得上，报「未覆盖 7 条」）
→ 从台账重算进度 → 依赖调度只放 L3 → 诚实报「无机器判据（已认领：…）」→ rc=3。
把预算调到预留额之上：**零花费时就拦住**——正是「派完才发现超了」要防的。

测试 177 → 228。


---

### G-56 · 第三轮独立审查：39 条，分三批修完（2026-07-28）

**审查范围**：`constitution.py` / `plan.py` / `autopilot.py` 三个**从没被独立查过**
的新模块，加上本轮全部修法。四路并行 + 证伪 + 合成，22 个代理。

> ⚠️ **合成那一步我自己搞砸了**：脚本里写了 `.slice(0, 70000)`，把四路结果
> 截断成两路，合成方只拿到一半（它自己在开头就声明了这件事）。
> 数据一直在，从 journal 全部捞回。**教训：给合成方喂输入时，
> 截断必须是显式的取舍，不能是一个随手写的常数。**

#### 最急的一条：我自己上一批引入的回归

`snapshot_base` 在工作区干净时返回**字面量 `"HEAD"`**，而宪法树内判据拒绝 HEAD。
于是「干净工作区 + 已启用宪法」的项目上，**每一单写任务都被判成宪法故障**
——干得好的活被判失败。devloop 自己当时就满足这两个条件。

⚠️ 根因不是「漏改一处」，是**同一段解析逻辑写了两处**：autopilot 里我写了一遍
并修对了，dispatch 那处原样留着。修法是收敛成 `wt_mod.resolve_base`，
并由测试钉死后置条件。

#### 第一批 · 会给错答案 / 丢钱（5 条，3 CRITICAL）

| 条 | 是什么 |
|---|---|
| `git add` 绕过新增判据 | 判据只看「未跟踪」，工人 `git add` 一下——**连提交都不用**——文件就同时不在 base、也不在未跟踪名单里。⚠️ 而 `git add` 不是攻击，是干完活再自然不过的动作。正确定义是「**base 里没有的文件**」 |
| 只读单点名闸 = 空守卫 | `tools` 默认 readonly → 不跑闸 → `gate_ok=None` → 判绿那行写「不等于失败」→ **空 ≠ 失败 → 算绿**。而 `--dry-run` 还把闸名念给人听。⚠️ **本项目自己的测试夹具就是这个坏形状**，所以 24 条测试一条都抓不到 |
| 派了不留账 | 钱在 `dispatch_one` 那刻就花了，账在最后一步才记，中间四个环节任一抛异常就一行不写。而失控三防线**全都只从台账读**——实测连跑四次真派 12 单、账本 0 行，四次都印「花了 $0.0000」 |
| T5 三个函数零调用 | `check_files`/`check_refs`/`snapshot` 写了、测了、生产代码一次没调。宪法 B-2 完全不设防，**而因为有绿测，它在测试报告里长得像「已覆盖」** |
| 硬拒绝是死代码 | `--detach` 的提前 return 排在宪法块之前 → 那条规则在**它唯一该生效的场景**里从不执行；而重建给子进程的命令行又去掉了 `--detach`，**两条路径同时失效** |

#### 第二批 · 守卫在跑，但视野是空的（5 条）

一句话：**「我检查了，没问题」和「我根本没检查」，输出上一模一样。**

- `load()` 只校验 `protected_file`，`protected_tree` 一条不查 → glob 写错永远报无命中
- `trees` 为空直接报绿 —— **而出厂模板把它们全注释掉了**，即 `constitution init`
  之后的默认状态就是「覆盖 0 条路径却每单报绿」。与 G-53 完全同形
- `protected_file` 指向**目录** → 指纹恒为空串 → **删掉整个目录也报绿**
- `protected_file` 写 `glob` → 整条链路静默忽略，锚里一条不记
- `_match` 的目录分支走纯字面 `startswith` → `ref*/` 永远匹配不到任何东西

修法统一成一句：**要么判它，要么显式登记你判不了，不许沉默地报绿。**
选「没有」之后，那句理由**会被印在每一次结论里**——不是关掉守卫，是把缺口摆到明面上。

#### 第三批 · 口径与欠账（3 条）

- **G-53 的说辞误伤了一个合法场景**：eco-ob 有一条分支是「测试真跑了 486 秒、
  38 断言全过，只有墙钟性能断言没达标」→ 0 PASS + exit 0。
  我印的是「一道都没验」加三条**猜**的原因，全不成立。
  ⛔ **判定不放宽**（闸自己说「未证明代码正确」），改的是解释：
  **把闸自己说的话摆出来，不要猜。**
- **G-53 正文欠了一天**（见该条目开头的说明）
- **宪法两份模板 9 条标注对不上**：人读版说「有机械判据」，判据表里没有。
  ⚠️ 真修法不是手工对一遍，是**加一条 parity 测试**（`tests/test_templates.py`）——
  审查同时指出 `templates/` **零测试覆盖**，这几条就是这么活下来的。
  标注现在必须写清**由谁判**：`toml` / `代码` / `待配置`，
  而标「待配置」的必须说清**不配会怎样**（= 空守卫）。

#### 顺手修的一条（本属第三批，但它当场咬了测试）

**宪法存在但坏了 → 被降级成「未启用宪法」照常派单。**
TOML 语法错 / schema 不认 / 路径写错 / 没登记 unjudged —— 四种本该退出码 2 的硬错
全变成一行警告，之后 T0、T2、硬拒绝一个都不跑。而那句提示还是假话：**宪法在，只是坏了。**
新增 `NotEnabled` 异常，只在文件根本不存在时抛。

#### 这一轮暴露的一条元教训

三批里有 **6 条**是「实现了、测了、但没接线」或「守卫的目标不存在」。
它们都有绿测，**在测试报告里长得像已覆盖的机制**。
所以新开了 `tests/test_wiring.py`：**只测「谁在什么时刻调了谁」**，
不测函数自身逻辑——因为函数写得再对，没被调用就等于不存在。

测试 228 → 260。


---

### ⚖️ G-57 · 便宜路线停用为默认（2026-07-28 用户裁决）

**裁决原话**：「既然目前来说走便宜路线不行，那我们就先把便宜路线给停了，
只留一个框架，先不要那么跑了，又花钱质量又差。」

#### 依据（实测，不是感觉）

| 口径 | 便宜路线 | 贵模型 | 结论 |
|---|---|---|---|
| 只算执行 | $0.56 | $26.82 | 便宜 **48 倍** |
| **加上产生任务书的拆单成本** | $28.23 | $26.82 | **贵 5%** |
| 再加编排方主循环、评审、抽查 | 没统计，只会更高 | $26.82 | 输得更多 |
| 质量（矛盾覆盖率） | 27% | 84% | 差 **3 倍** |

回本点是第 12 单，那次只跑了 11 单——**连回本都没到**。
且 7/7 抽查确认：贵模型多找出来的矛盾**都是真的**，不是幻觉。

#### 停的是什么，留的是什么

| | 状态 |
|---|---|
| 注册表默认后端 | `deepseek` → **`subagent-opus`** |
| api 后端的能力 | **全留**。`--backend deepseek` 照常可用 |
| 派单 / 隔离 / 闸 / 台账 / 宪法 / 自动驾驶 | **一行不动** |
| 显式选 api 后端时 | 当场打印实测代价（`💰` 那行） |

⛔ **停的是「默认」，不是「能力」。** 随时可以重新开测——
而这正是为什么不删代码：删了就再也没有分母了。

#### ⚠️ 直接后果：写操作任务暂时没有执行者

`deepseek` / `deepseek-flash` 是**仅有的两个能起子进程的执行者**。
而 `subagent-opus` 只支持只读——那道拦截有硬理由：
子代理跑在**编排方的工作目录**里，放不进隔离 worktree，
允许它写就是一条静默假绿通道（改动落进用户活工作区，而 worktree 空转、
闸全绿、台账记 ok=true）。

所以停掉之后：

- **只读审计** —— 照常，走 subagent 交接协议
- **写操作自动驾驶** —— **没有执行者**

⚖️ 这是这次裁决的**已知代价**，不是缺陷。两条出路，触发条件各不相同：
1. 需要写操作时，显式 `--backend deepseek`（代价会当场打印）
2. 做一个「编排方在 worktree 里跑子代理」的新交接模式 ——
   触发条件：**当写操作自动驾驶重新成为真需求时**。⛔ 现在不做。

#### 顺带修的一条

改默认后端时暴露出：`eval` 的入参校验（题号写错）排在**基准检查与后端解析之后**，
于是「题号写错」被「后端 kind 不对」挡在前面，**报错答非所问**。
已把纯入参校验挪到最前——与 `_load_specs` 预校验任务书同一条纪律：
**能在零成本时刻失败的，就不要留到有成本的时刻。**

#### ⚠️ 一条自省

我在演示「花钱必须是显式选择」的时候，自己手滑真派了一单出去
（写的是 `dispatch --backend deepseek` 而不是加保护的形式），跑了约一分钟被掐掉。
**没有台账行**（在 `dispatch_one` 返回前就被杀），没有 worktree 残骸。
⚠️ 这恰好说明那条「当场打印代价」的改动是必要的——而我自己是第一个反例。


---

### G-58 · 两个真实项目接入宪法；p4 的「没有闸」是取舍不是缺口（2026-07-28）

#### eco-ob：宪法上线，当场补了两个真实的洞

**① `game/data/terrain.json` 是一条纯散文守卫。**
它写在 `rules-digest.md` 的禁改清单里，而 **`gates.sh` 里一个字都没提**
（实测 `grep -c terrain .devloop/gates.sh` → **0**）。
在宪法之前，这条禁令**没有任何执行者**——正是本项目开篇否定的那种东西
（`rules-digest` 写着禁改，真正执行的却是闸里的脚本）。

**② 闸的三道守卫，工人自提交就绕过去了。**
基线守卫（`git status -- 'game/tests/ref_*.json'`）· 哈希常量守卫
（`git diff -- 'game/tests/*.gd'`）· 禁改清单（`git status -- '.devloop/'`）
——**全用工作区差异**。工人把改动提交掉，工作区就干净了，三道全部判 PASS。
宪法的 **base 锚定**判据补的正是这一半：拿**派单前那个提交**比对，自提交也躲不掉。

配置：5 个受保护文件 + 4 条受保护路径（`ref_*.json` · `tests/*.gd` ·
`terrain.json` · `01_设计文档/**`），9 条判不了的显式登记，已锚定。
⚠️ 普通源码（如 `sim_world.gd`）**不受保护**——工人本来就该能改它。

#### eco-ob-p4：⚖️ 它**故意没有闸**

**我此前把「p4 补 gates.sh」列成待办，那是错的。**

p4 是评测集的**冻结基准**（`BASE_REPO`，提交 `6f4b849`，tag `devloop-p4-base`），
20 道题的标准答案全锚在那个提交上。**往里写一个字，`check_baseline()` 就会因为
工作区变脏而拒绝跑分——整套评测的分母就没了。**

⚠️ 所以给它补闸是**反向**的：闸的用途是裁决写操作，补一个等于邀请人往里写。

正确做法是**在起任何进程之前就拒绝**。为此给宪法加了一条新的硬拒规则：

```toml
[refuse]
no_writes = true
why = "<为什么这个项目不接受写操作>"
```

实测：`dispatch --project eco-ob-p4 --tools implement` → **退出码 2，
一个进程都没起、一分钱没花**，且错误消息把理由整段说出来。
只读任务（对账、跑分）照常——那正是这个项目存在的用途。

#### 三个项目的宪法现状

| 项目 | 受保护文件 | 受保护路径 | 判不了 | 特别之处 |
|---|---|---|---|---|
| devloop | 5 | 2 | 7 | 保护自己的评测答案卷与能力探针答案 |
| eco-ob | 5 | 4 | 9 | 补上了 terrain.json 这条纯散文守卫 |
| eco-ob-p4 | 3 | 2 | 9 | **`no_writes`**——冻结基准，谁都别写 |


---

### ⭐ G-59 · 宪法上报链路的首次真实演练（2026-07-28）

**为什么必须做**：信任爬梯的档 2 条件是「**至少触发过 1 次宪法上报并被正确处理**」。
在此之前**一次都没触发过**——而宪法自己写着：

> 若档 1 期间宪法从未被触发，「零违规」是**无意义的**。没被测过的机制不算工作。

#### 设计：⛔ 不许伪造命中

手工改个文件、或 monkeypatch 出一个命中——**那测的是 `check_tree` 函数**，
而它已经有 27 条单测了。演练要测的是**整条链**：
真派单 → 工人真改 → T2 真判 → 结果真进台账 → 退出码真的对 → 产出不丢。

**做法：一份完全正当的任务，而它恰好触碰受保护基线。**

任务是「给评测集补一道 undecidable 题」——**这是真需求**
（审查指出现有 3 道全出自同一份文档同一节，有效样本远小于 3）。
工人的行为**完全正当**，而宪法应该拦下来说：**改基线要人批准**。

> ⭐ 这正是宪法该做的事——**不是抓坏人，是把「需要人决定的事」停下来。**

#### 结果：四条机器判据全中

| 判据 | 读数 |
|---|---|
| 台账新增一行且记了宪法命中 | 10 → 11 行；`error` = 宪法命中 C-2，含前后指纹 `187c399577c4 → e070d08ae7c2` |
| 退出码非 0 | **rc=1**（活没干好），不是 0 也不是 2 |
| 产出固化在隔离分支 | `7cfbdc5`，删 worktree 不会丢 |
| 主线基线一个字节没变 | `187c399577c4` = `187c399577c4` |

#### ⭐ 最有教学价值的一个组合

```
闸       5 过 / 0 未过      ← 活本身干得好
gate_ok  True
ok       False             ← 但整单判失败
error    宪法命中 C-2
```

**闸说「干得好」，宪法说「但这需要人批准」——两个不同的问题，各自答了，
而「要问人」那个赢了。** 这正是这两层该有的分工：
闸回答「合不合格」，宪法回答「有没有越过需要人决定的线」。

#### 实际成本

**$0.069**，472 秒，54 轮。⚠️ 比事前估的 $0.02 高 3.5 倍——
任务比预想的重（工人真去 p4 里翻文档找了一道合格的题）。
⚠️ **估算偏差本身要记**：$0.02 是按历史写任务的中位数估的，
而这单是「调查 + 写入」，不是纯写入。

#### 顺带得到一道真的好题

工人产出的 `c21` 经我逐条核实（去 `eco-ob-p4` 实地查了四处）：

- 来自**不同文档**（`个体层_Charter.md` §D-IND-09），不是 `00_索引.md` §五 ✅
- **不同句式**（涌现行为的分岔点），不是「一个只有跑起来才有的数」 ✅
- **无源码常量兜底**：实测 `wyrm_layer.gd` 里 0.5 只出现在三个**行为参数**上
  （`hunger_seek_ratio` / `hunger_wander_max` / `chase_fitness_bonus`），
  **没有** `if f <= 0.5: die()` 式的灭绝阈值 ✅
- 文档原话核对无误：表里「个体层 ≤0.50 → **全部 0（灭绝）**」，
  密度层同列 `115.2 → 23.0`（case 引的 23.0 准确）✅

⭐ 它的 trap 写得尤其好，正是 c18/c20 栽的那个坑的**反面**：

> ⚠️ `probe_wyrm_fitness_response.gd` 第 17 行注释写了「实际断点在 0.50」——
> **那是描述一次实测结果，不是源码常量。grep 到它不构成证实。**

实地核对：那行原文确为「`f_crit = m/r = 0.40` 是硬写的理论值……实际断点在 **0.50**」——
trap 描述准确。

#### ⛔ 这道题还没合进主线

改基线是宪法 C-2，**要人逐单批准**。产出停在隔离分支上等决定——
在一次证明上报链路有效的演练之后绕过那个上报，会很讽刺。

---

### 🟠 G-61 · 测试守卫的彻底版：拿基线那份 tests/ 跑工人的代码

⚠️ 本条 2026-07-29 写成 **G-58**，与同日更早的「两个真实项目接入宪法」撞号
（同一份文件里两个 `### G-58`，索引只指得到其中一个），2026-07-30 改为 G-61。
⛔ 与 G-60 那次是同一个毛病：**写新条目之前没 grep 最大号**。
提交 `455abfd` 的正文里说的「G-58 测试守卫的彻底版」= 本条。

**状态**：待做 ｜ **来源**：2026-07-29 修 G-53 姊妹问题时自己记下的残余漏洞

#### 现状判据（已落地，见 97f40a1）

  · 改 / 删 / 改名已有测试 → 失败
  · 新建 `test_*.py`       → 放行
  · 新建其它文件           → 失败（专堵 `conftest.py`）

#### ⚠️ 它挡不住什么

新建的 `test_*.py` 在**被收集时**会执行模块级代码。理论上工人可以在 import
期给 `devloop` 打补丁，影响同场次其它测试——「新增文件」照样能削弱已有断言，
只是路径更绕。

⚠️ 这需要蓄意破坏，而现判据防的是「顺手把断言改松」，所以不算阻断项。
但**它是个真洞，不许当作没有**。

#### 彻底堵法

判据换成：**工人的代码必须能通过它没碰过的那套测试**。

    git -C "$WORK" archive "$BASE" tests | tar -x -C <临时目录>
    PYTHONPATH="$WORK" pytest <临时目录>/tests

这样一来，改测试、删测试、加 conftest、import 期打补丁——**全部无效**，
因为跑的根本不是工人手里那份。附带好处：新增测试可以完全自由。

#### 为什么当时没直接做

- 闸拿不到基线提交号（`gates.sh` 只收一个工作目录参数），要先加 `DEVLOOP_BASE`
- Windows/git-bash 下临时目录路径要在 POSIX 与 Win32 之间来回转，易碎
- ⚠️ 而**易碎的闸比宽松的闸更坏**：它会以「闸自身故障」的形式随机红，
  最后所有人学会忽略它

先落地简单版 + 把洞写清楚，比落地一个跑不稳的复杂版强。

---

### G-60 · `needs` 只排序，后一单看不到前一单的产出

⚠️ 本条 2026-07-29 曾误编为 G-59，与同日更早的「宪法上报链路首次真实演练」
撞号，2026-07-29 交叉核对时改为 G-60。⛔ 编号撞车会让日后 grep 指错条目。

**状态**：**已解决**（2026-07-29，提交 `64042ed`）｜ **来源**：首次真跑 `needs` 时核实
⚠️ **但当天的「已解决」漏了崩溃恢复那条路，2026-07-30 才补上（`0e2518e`）——见文末。**

#### 结论

选了 A（阶段内接力），做成 `[stage] chain = true`，⛔ 默认关。
接力点只在**整体合格且产出已固化**之后推进——没过闸的产出永远成不了接力点。

⚠️ 原先担心的「锚跟着动」不成立：推到的是**编排方**在闸全绿之后写的那条提交
（`commit_result`，提交前断言过 HEAD == 该分支），工人自己的提交成不了接力点。
要防的是「拿 worktree 的 HEAD 当锚」，那是另一回事。

⚠️ 代价照记：几单绑在一起，合最后一个分支就全拿到，但**不能只否掉前一单**。
所以必须是开关，不能由默认值替人接受这个代价。

**真跑验证**（stage `nightly`，2 单全绿）：
· chain-b 的提交父提交就是 chain-a
· chain-b 的 diff 只有 +30 行（追加，不是重写）
· `report()` 里 `pending = branch_lines(project)`——真调用了上一单的函数
· 任务书里那条「看不见就停下报告，⛔ 不许自己重写一个顶上」没被触发

以下是定夺前的分析，留档：

#### 实际形态

`cli.py:649` 在 while 循环（`:670`）**之外**算一次 `base`，然后每单
`wt_mod.create(..., base=base)`（`cli.py:99`）都用同一个。

于是 `[[task]] needs = ['前一单']` 的语义是**先后顺序**，
⛔ **不是**「建立在它之上」——后一单的 worktree 里根本没有前一单的改动。

#### ⚠️ 这对「长期自己跑项目」意味着什么

一个自然的计划长这样：

    1. 写模块        2. 给模块写测试（needs 1）      3. 修测试暴露的问题（needs 2）

**这个计划跑不了。** 第 2 单打开 worktree 会发现模块不存在，
第 3 单看不到任何测试。三单各自从同一张白纸开始。

⚠️ 现在能跑的只有「一批互不相干的小改动」，而那恰恰是**最不需要**
自动驾驶的那种活——它没有需要编排的依赖。

#### 可能的改法（都要先想清楚代价）

**A. 阶段内接力**：某单过闸之后，把阶段 base 前移到它的分支尖端。
  - ⚠️ 代价：一单出错会污染后面全部；且「基准」不再是一个固定点，
    宪法树内判据的锚要跟着动——⛔ 而锚会动正是这个项目一直在防的事。
    要动的话必须想清楚「锚跟着走」和「工人自提交移动锚」怎么区分。
  - 需要显式开关（`chain = true`），⛔ 不许当默认。

**B. 阶段间接力**：一个阶段跑完、人批准合并之后，下个阶段从新主线起。
  - 优点：接力点上有人，与宪法 C-2/C-4「合并要人批」天然一致
  - 代价：跨阶段要等人，那就不是「跑一夜」了

**C. 一单干完一整条链**：把「写+测+修」塞进一单，靠 max_turns 撑。
  - ⚠️ 实测撞轮数上限（error_max_turns）已经出现过 6 次，这条路会更常撞

#### ⛔ 在定夺之前

写计划时**别假设后一单能看到前一单的产出**。这条现在没有任何提示——
`plan.py` 不会警告，`--dry-run` 也看不出来，只有真跑完打开 worktree 才发现。
最低限度应当在 `--dry-run` 里把这句话打出来。

#### ⛔ 补记（2026-07-30，`0e2518e`）：接力点只活在内存里，`--resume` 会静默丢回项目 HEAD

**64042ed 当天判「已解决」，判早了。** 接力点在循环体内推进，
而 `Run.save` **不存它**，`base` 每次进程启动重算。于是：

    跑到一半被杀 → --resume → 下游单从**项目 HEAD** 建工作副本

而前几单的产出只在**未合并的隔离分支**上，树里根本没有。
后果是二选一，两个都坏：下游单**必然失败**，或者工人
「自己重写一个顶上」——**产出静默分叉**，而且看起来一切正常。

⚠️ **崩溃恢复正是无人值守的立项理由**，接力偏偏在那条路上失效。

**修法**：`Run.chain_head` 落盘 ＋ `resume_chain_head()` 读回；
推进接力点时**立刻 save**（进程随时可能被杀）。

**⭐ 这条的教训不在代码上**：chain 的真跑验证（stage `nightly`，2/2 绿）
是**一次性跑完的**，⛔ 从头到尾没碰过 `--resume`。
「真跑验证过」只覆盖了它真跑过的那条路——
**跑一半被杀再接着跑**是另一条路，它当天一步都没走。

---

### ⚖️ G-62 · 模式触发的护栏**不是密封舱**（残余漏洞，⛔ 别按「安全机制」宣传）

**状态**：⚠️ 已知未修（其中 fail-open 那条是**故意**的取舍，不是欠账）
**来源**：`2ff956d` / `1659c49` 提交正文自记 + 2026-07-30 复核

#### 这套东西是什么（先说清它守住了什么）

用户否掉了「你用自然语言说、我来判断」，原话：
「不行 我觉得这个判断方法也不好，我觉得需要一些特殊的字段来进行触发」。
⚠️ 他有实据：2026-07-29 当天模型在这个项目上**判断错了四次**，四次都是真跑才抓到的。

    你打的字面量 ──正则──▶ 令牌文件 ──PreToolUse──▶ 命令放行 / deny

三层里**判断全部离开模型**：识别层（`UserPromptSubmit`，在模型看到消息**之前**跑，
纯正则，落盘令牌，用 `systemMessage` 回显）· 强制层（`PreToolUse(Bash)`，
`permissionDecision: "deny"`，没令牌花额度的命令**物理跑不起来**）·
CLAUDE.md（⛔ 只解释，绝不承担闸）。

#### ⛔ 残余一：防自我授权的判据是**字符串匹配**

模型有 Bash，理论上能自己写令牌文件把门打开。现在的挡法是
「任何碰状态目录的 Bash 一律 deny」——换个写法绕开字符串匹配**是可想象的**。
它挡的是「顺手就把门打开了」，⛔ **不挡蓄意规避**。
⚠️ 这句话必须跟着这套机制一起被说出去，否则用户会按「不可能」来使用它。

#### ⛔ 残余二：两个钩子都 fail-open，而它**已经真发生过一次**

两个钩子都是故意 `except: return 0`——钩子自己坏了不该挡住用户干活。
代价是：闸里一个笔误会把整道闸**静默关掉**。

2026-07-30 真发生了：我引用了一个**还没定义的正则**，
**15 条断言从 DENY 变成放行，屏幕上什么都不报**。

现在的缓解：自测把 stderr 一并看住，有 traceback 就当失败。
⚠️ **缓解只覆盖自测这条路**——真正干活的时候没有人盯着钩子的 stderr。
⚖️ 反过来做（fail-closed）的代价是钩子一坏就谁都干不了活，
所以这是取舍，不是「以后修」。⛔ 但取舍要写下来，不能装作没有。

#### ⛔ 残余三：复合命令分段是启发式

闸把 `&&` `||` `;` `|` 换行 当分隔符切段、并先剥 shell 注释，
⚠️ 引号里的分隔符和引号里的 `#` 都会被误处理。
**方向是偏严的**（多切几段只会让更多段需要令牌，不会放过真跑），
⛔ 但「方向偏严」是论证，不是证明——真要下结论得有对抗性用例。

#### ⛔ 残余四：状态一旦被写进提示层，整套无声退化

令牌**全部落盘**，绝不当会话记忆（上下文压缩碰不到它）。
⚠️ 一旦有人把「当前模式」写进 CLAUDE.md、或让模型「记住现在是 C 模式」，
判断就又回到模型身上了，而**外面看不出任何区别**。

**触发条件**：把这套机制写进任何对外材料（README / 演示 / 交付说明）之前，
上面四条必须一并出现。⛔ 特别是不许写「模型不可能自我授权」。

---

### 🟠 G-63 · `!` 前缀根本用不了：选触发字面量之前必须先查**宿主环境占用了什么**

**状态**：主字面量已换 `%`（`1659c49`）｜ ⛔ **残余未修**：闸的 deny 文案仍在教用户打 `!`
**来源**：`93d3bdb` 上线 → `1659c49` 当场回归

#### 事实（从 CLI 二进制核实）

    function vG(H){if(H.startsWith("!"))return"bash";return"prompt"}

界面上还印着 `! for shell mode`。所以在输入框打 `!task 干活`，
Claude Code 会把它当 **shell 命令**去执行 `task 干活`——
**根本到不了模型，钩子也看不见**。

⚠️ 而全角 `！` 反而能用（`startsWith("!")` 只匹配半角）：
**同一个字符半角失效、全角生效，肉眼看不出差别**，⛔ 不能拿它当主字面量。

#### ⛔ 自测为什么全绿（这条比缺陷本身值钱）

自测把 `{"prompt":"!task ..."}` **直接灌进钩子 stdin**，
绕过了 CLI 的输入分类——**测了错的那一层**（六种假绿的第四种）。
⚠️ 而上一个提交（`93d3bdb`）的正文里，我列了 `@` `#` `++` `--` `跑::`
五种冲突的实据，**唯独没查 `!` 自己**——查了所有别人，没查自己刚选的那个。

#### 现在用 `%`，依据同样是核实过的

⭐ `%` 在 CLI 里 `startsWith("%")` **零命中**；中文输入法**不改写**百分号
（逗号句号会被改成全角）；行首出现在自然语句里几乎为零。

被否掉的（每条都有实据，留档免得下次再选一遍）：
· `!` 内置 shell 模式前缀 · `@` **任意位置**弹文件补全 · 行首 `#` 是
「写进 CLAUDE.md」的快捷键 · `++` / `--` 被 `C++` 和 `--dry-run` 打中 ·
`//` 与 `..` 被粘贴的代码注释打中 · `。。` 与中文省略号 `。。。` 难区分 ·
`跑::` 中文标点下**根本打不出来**

#### ⛔ 残余：换了字面量，但**教人怎么打的那句话没跟着换**

2026-07-30 写文档时静态核对发现（⚠️ 静态读代码，未真跑）：

    tools/devloop-spend-gate.py:115   「要放行请让用户打代号（`!task` / `!auto`+`!go`）」
    tools/devloop-spend-gate.py:142-144、:149  同样是 `!auto` / `!go` / `!task`

而 `tools/devloop-mode-hook.py` 认的是 `%`。
⛔ 于是闸拦下一条命令、并**印出一句用户照做也不会生效的指示**——
`!task ...` 会被 CLI 当 shell 命令吃掉，什么都不发生，**而他以为触发了**。
⚠️ 这正是当初一并认全角 `！` 的理由（「不认的话用户会打出 `！task` 然后
什么都不发生，而他以为触发了」）——同一个坑，换个位置又踩了一遍。

**触发条件**：下次改字面量**之前**，先 grep 全仓所有会印给用户看的串
（钩子回显 / 闸的 deny 文案 / CLAUDE.md / 斜杠命令说明），⛔ 一处不跟就等于没换。

#### ⭐ 一条更一般的教训

选一个触发字面量之前，**必须先查宿主环境（CLI 输入框、输入法、shell）已经占用了什么**，
⛔ 而且**要包括你正打算用的那一个**。
判据只有一条硬的：**从宿主的二进制/源码里核实**——⛔ 不是「试一下看着像能用」，
因为失效形态可能是「另一层把它吃掉了」，而那一层不报错。

---

### 🟠 G-64 · 「以为免费、其实收费」的路径：`doctor` 已修，⛔ `eval` 疑似同形且没查

**状态**：`doctor` 已修（`0e2518e`）｜ ⛔ `devloop eval` 未裁决
**来源**：2026-07-30 审查（`doctor`）+ 同日静态核对（`eval`）

#### 已发生的那次：`doctor --project` / `--probe` 真花订阅额度

`doctor._dispatch_smoke` **真派一单**、走默认后端（= 订阅额度）；
`credentials.probe()` 自己就记着「一次最小调用产生七万级 `cache_creation` token」。
而闸的注释表和 `/devloop` 命令文档**双双**把它列在「不花钱」里，
**且一行台账都不留**。

⛔ 它是「夜跑标准流程**第 1 步**」→ 每次体检都在悄悄吃掉当前那个 5 小时窗口，
而三条失控防线（已花多少 / 派了几次 / 有没有算不出成本的）**全都只从台账读**，
看不见它 → **夜跑会在一个已经被吃掉的窗口上起跑**。

**修法**：闸把带 `--project` / `--probe` 的 `doctor` 纳入需令牌名单（裸 `doctor` 仍放行）；
`_record_smoke()` 把这一单记进台账。⚠️ 记账整个包在 try 里——
**记账失败绝不能让体检本身报错**，体检的用途是告诉你哪里坏了。

**旁证**：2026-07-29 那笔 **$0.0082** 是 `doctor` 误走 deepseek 花的
（注册表默认早已是 subscription = $0）。复核 `.devloop/telemetry.jsonl`：
2026-07-29 只有 **10 行**、全部 `claude-opus-4-7`，**那一单不在台账里**——
与「一行台账不留」互相印证。

#### ⛔ 没查完的那部分：还有几个入口能花钱？

2026-07-30 静态核对（⚠️ **读代码得到的，没真跑**——真跑就是花额度）：

| 会起模型子进程的地方 | 谁调它 | 闸拦不拦 | 记不记台账 |
|---|---|---|---|
| `dispatch.py:127`（`build_cmd` + `subprocess.run`） | `cli.py:100` `_run_unit`（`dispatch` / `autopilot`） | ✅ 要令牌 | ✅ |
| 同上 | **`devloop/evals/runner.py:135`（`devloop eval`）** | ⛔ **不拦** | ⛔ **零行** |
| `doctor.py:190` | `doctor --project`（`_dispatch_smoke`） | ✅ 已纳入（`0e2518e`） | ✅ 已补（`_record_smoke`，`0e2518e`） |
| `credentials.py:161` | `doctor --probe`（`credentials.probe()`） | ✅ 已纳入（`0e2518e`） | ⛔ **仍然零行** |

⛔ **表里最后一行是这次核对新发现的**：`_record_smoke` 全仓只有一个调用点
（`doctor.py:200`，即那次冒烟派单），而 `--probe` 那条路（`doctor.py:116` → `cred.probe()`）
**照花额度、照样不留账**——`0e2518e` 只把它挡进了需令牌名单，账没补上。
⚠️ 而 `probe()` 自己写着「一次最小调用产生七万级 `cache_creation` token」，
它恰恰是**最该留账**的那种「小」调用。

⛔ **`devloop eval` 与 `doctor` 修之前的形状完全一样**：真起子进程花额度、
闸一律 `return 0` 放行（`tools/devloop-spend-gate.py` 只认
`dispatch` / `autopilot` / `doctor --project|--probe`）、
`cmd_eval` 路径上没有任何 `telemetry.record`。
而它默认 `--parallel 4`、`--max-turns 120`、`--backend` 不给就用**注册表 default**
（= subscription）——**一次全量跑分是 20 题**。

⚠️ 措辞按证据分级：这条写「**疑似同形**」，⛔ 不写「已确认」——
它没有一次真跑做证据。（`eval` 自己有 `devloop/evals/runs.jsonl` 记逐题成本，
所以不是完全无账；但那不是三条防线读的那本账。）

**触发条件**：下一次动闸、或下一次跑评测集**之前**必须裁决——
要么纳入需令牌名单 + 留账，要么在文档里写明「eval 花的是谁的额度」。

#### ⭐ 一条更一般的教训

「这条命令花不花钱」⛔ **不许按名字猜**（`doctor` / `eval` 听起来都像不花钱）。
判据只有一条：**它到不到得了 `dispatch_one` / `build_cmd` + `subprocess.run`**。
⚠️ 而且每加一个花钱入口，要**同时**改三处：闸的名单、台账、文档——
**少一处就是一个隐形出口**，而隐形出口的代价是「防线看不见它」。

---

### ✅ G-66 · 自动驾驶**不支持并行**——是「未实现」，不是「未验」

> **✅ 已闭（2026-08-02）。** 实现要点见本条末尾。

**证据**：`devloop/cli.py::cmd_autopilot` 的派单循环是 `t = ready[0]`——每轮只取一单。
`autopilot --help` 里**没有 `--parallel`**。并行只存在于 `dispatch --parallel N`。

⛔ **为什么这条要单独立项**：PLAN 长期把它记在「未验的四条」里，
而「未验」和「未实现」的处置完全不同——前者跑一次就行，后者要写代码。
⚠️ 按「未验」排期会直接排错。

⭐ **它是 PLAN §0.1 里 B（自己找活的闭环）的第一步。** `cmd_dispatch` 里的并行代码
（按波提交 + 撞额度能停 + `fanout.check` 判据）是现成的，搬过来即可。

⚠️ **搬之前要想清三件事**（见 `devloop/fanout.py` 模块 docstring）：
① 有依赖的任务不能并行（那是 `chain` 的事，两者互斥）；
② 改到同一处的不能并行（`# 改动范围` 判据已做好）；
③ ⛔ **N 个分支要按宪法逐个批准**——并行只是把瓶颈从机器移到人身上。

---

#### ✅ 实现（2026-08-02）

**新字段** `[stage] parallel`（默认 1）。三条设计决定：

1. ⛔ **与 `chain` 互斥，在 `plan.load()` 直接拒。**
   接力的语义是「后一单从前一单**已过闸的**产出起」，天然要求串行。
   ⚠️ 两个一起开，后一单会从一个还没跑完的起点复制——**那不是性能问题，是正确性问题**。

2. ⭐ **改动范围的判据在 `plan.load()` 执行，不在派单循环里。**
   那是唯一「还没花一分钱」的时刻——留到派单时才拒，前面的单已经跑掉了。
   ⚠️ 按 `tools` 分组各查一次：只读与写的判据完全不同（成本差 35 倍）。

3. ⚠️ **一波里只放同一个 `tools` 的单。** 混着放没有统一判据可用。

**溢出的上界是明写的**：一波跑完才查 `halt`，同一波里的单已经在飞、收不回来，
所以「撞额度时最多溢出一波」。⭐ 停批保的是**诊断清晰度**——
一波 N 条失败仍读得出是同一堵墙，⛔ N 波就读不出了。

⚠️ **测试判据本身也栽了一次**：第一版写「源码里不许出现 `ready[0]`」，
而它合法地出现在 `head_tools = ready[0].tools` 和一句注释里——**子串判据把正确实现判红**。
改成在 AST 上判「把 `ready[0]` 整个绑给一个名字」这个动作。

---

### ✅ G-67 · 触发代号打在句子中间时**静默失败**

**真实事故（2026-07-31）**：用户打「放行 `%task` 派 e1-dedup-prey-layer 这一单」
——代号必须在**第一行行首**，前面有「放行 」两个字就不匹配。

拦住是**对的**（行首锚定正是「只认真实按键、不认粘贴内容」这个属性的来源）。
⛔ **错的是一个字的提示都没有**：用户以为放行了，模型收到的是「模式 A｜没有触发代号」。
**两边对世界的认知分叉，而没有任何一方会发现。**

> **没有回显的失败比拦下来更坏。** 拦下来是「按钮没按到」，
> 静默失败是「以为按到了」——后者会让人接着往下走。

⚠️ **早先那版有近似检测**（`。。。` / `／devloop`），在把前缀从 `。。` 换成 `%` 的
重写（`93d3bdb`）里**整段丢了**。⛔ 重写最容易掉的就是这种边角分支：
它不在主路径上，所有主路径测试照样绿。

**修法**（`6ced08f`）：模式落回 A 且本轮无别的回显时，在第一行里搜代号；
搜得到但没匹配行首锚定，就回显一条**可以直接照抄重发的命令行**。

---

### ✅ G-68 · 闸只有三档 → 「我没东西可验」只能报 PASS

**真实事故（2026-08-01，eco-ob 首跑）**：

```
✓ 闸·禁改清单: worktree 内无 .devloop/（该项目未跟踪它，符合预期）
```

它报 **PASS 而验了 0 条**，⛔ 而它被写进了 `--require-pass`
——**点名一道恒过的闸等于没点名**。

**修法**（`c20e26f`）：新增第四档 **VOID** =「我这一道本来就没有可验的东西」。

⚠️ 与 SKIP（本来能验、这次被开关跳过）**对放行含义相同**（都不算通过），
但对**排查**含义完全不同：SKIP 要查谁开了开关，VOID 要查为什么这个项目里它是空的。
所以摘要里分两档报。

⛔ **判据只能在闸自己身上**——只有它知道有没有东西可验。外面用启发式
（「detail 里没数字就算空过」）会误伤 `基线守卫: ref_*.json 未被改动` 那种真验过的。

---

### ✅ G-69 · 模式 C 的令牌 TTL 从错误的时刻起算

**一个时钟在干两件事**。钩子写令牌时：

```python
_write(tok, {"mode": "C", "approved": True,
             "armed_at": p.get("armed_at"),   # ← 从 %auto 那一刻抄过来
             "granted_at": now})              # ← 写了，但闸从来不读
```

那 30 分钟同时覆盖了两段：`%auto → %go`（**演练时间**）和 `%go → 真跑`（**批准有效期**）。

⛔ **后果是判据的维度错：演练得越认真，用户的批准有效期越短。**
2026-08-01 实测撞到——演练时写了 3 份任务书 + 1 份计划 + 跑了闸，窗口烧光，`%go` 失效。

**修法**：闸改读 `granted_at`（那个字段本来就写着）；同时 `%go` 的回显补上
**「你批准的是什么」**——⭐ 那是这次放宽的对价（把「随手 `%go`」从看不见变成看得见）。

⚠️ **安全性评估**：最大暴露面**没有变**（两种设计都是批准后 30 分钟上界）。
变的只是「慢慢想」不再被惩罚。⛔ 这不是放松，是修正。

---

### ✅ G-70 · 退出码判据用错维度 → **撞了额度被报成成功**

```python
if todo:            # ← 只有「还有单没派」才报 3
    return 3
return 1 if failed else 0
```

⛔ **额度在最后一波撞上时** `todo` 已空 → 退出码 **0 或 1**
——**「撞了额度」被报成了成功**，而上游会据此判定「这批活干完了」。

⚠️ **这个洞改动之前就在**，被「默认串行」掩盖着（撞墙必然留下未派的单）。
只读默认改成并发（`devloop/fanout.py`）之后才暴露出来。**判据的维度一直是错的。**

**修法**（`c1fc0e0`）：`if halt.tripped or todo: return 3`。

---

### 🟠 G-97 · 锚绑在「磁盘字节」上 → 一次 `git checkout` 就把它打穿

**2026-08-03，用户跑完 `constitution anchor` 之后的事后审查中撞见。**

起因很平常：我想把锚里的指纹对应到某个提交，好回答「这文件到底被谁改过」
——⛔ **对不上任何一个提交**，包括 HEAD。

```
锚里记的                7732644898560fdf
工作区文件 原样字节      7732644898560fdf   ✅
同一文件 CRLF→LF        2d0759c9d74687b3
git 里存的 blob         2d0759c9d74687b3   ← HEAD
```

`core.autocrlf = true`，仓里没有 `.gitattributes`。⭐ 于是锚锚的是
**这台机器上这一刻的字节表示**，而不是内容。

## ⭐ 最终结论（第二次更正之后）

**真实触发条件**：受保护文件**被修改过**，然后用 git 还原它
（`git checkout -- <文件>` / `git restore` / `git stash pop` / `git reset --hard`）。
git 这时才会重写该文件，autocrlf 的 smudge 过滤器才有机会把它写成 CRLF。

**实地复现**（2026-08-03，就在真仓上，我给用户演示 anchor 差异功能时踩到的）：

```
往 .devloop/rules-digest.md 追一行  →  git checkout -- 那个文件（撤销）
  → constitution check
    → ⛔ 宪法判定器故障：受保护文件与锚不符
       —— A-4 不得修改工人的规则摘要（.devloop/rules-digest.md
          指纹 52a4e7a65dd2 → 16bf911827d6）
    → 退出码 2，两条派单路径全停
```

⚠️ 而 `git diff` 是空的：**内容逐字节等同 HEAD**。

## ⛔ 我为此判错了两次——两次都是判据维度错

| 谁 | 说法 | 错在哪 |
|---|---|---|
| **我最初** | `clone` / `checkout` / `stash` 都会炸 | ⛔ 太宽。我的探针用 `shutil.copytree` 复制仓库，新文件 inode/ctime 全变 → 索引 stat 失配 → git 才重写。**夹具的副作用被当成了被测对象的行为** |
| **对抗复核** | `checkout` 从不重写，所以整条不成立 | ⛔ 太窄。它只在**未修改**的文件上试——那种 git 确实跳过。它没试「改了再还原」 |
| ⭐ **真相** | **改过 + 用 git 还原** → 重写 → 炸 | 两边的实验都没覆盖到这一格 |

⭐ 值得记的是**它是怎么被定准的**：不是靠再想一遍，是靠**在真东西上做那个动作**。
⚠️ 我和复核方各自的沙箱都恰好避开了真实触发条件——⛔ 两个都是「实验对象错了」。

## ⛔ 为什么严重度是 HIGH 而不是「不过是个误报」

失效方向是安全的那侧（内容变了指纹必变，⭐ 不会漏报）。要命的是**人的反应**：

```
A-1 因为行尾误报 → 人查了半天发现没人改过 → 「重锚一下就好了」
  → 下一次 A-1 再响，人不查了，直接锚
    → ⛔ 而那正是设计明令禁止的自愈：「锚一旦能自愈，它就不是锚」
```

⚠️ **一个会误报的停机判据，会把自己训练成橡皮图章。**

## 修法

`_sha()` 先 `replace(b"\r\n", b"\n")` 再哈希（`devloop/constitution.py`）。

⭐ **附带修好第二件事**：归一之后指纹**可以从 git 内容推导**了。
⚠️ 在此之前不行——我想把锚里的指纹对应到某个提交、好回答「这文件到底被谁改过」，
**一个提交都对不上**。那是拿到 A-1 告警后的第一个动作，而它做不了。

⚠️ 诚实代价：两个仅行尾不同的**二进制**文件会哈希相同。受保护清单里放的是
toml/md/sh，可接受——⛔ 但有人把二进制加进 `[[protected_file]]` 时要重新评估。

⛔ **改哈希方式 = 所有指纹变化 = 必须重锚一次。**
⭐ 与 A/B 的 `constitution.toml` 改动合并成同一次重锚，不额外增加摩擦。

---

## 附：第一次更正的原文（保留备查）

**我最初报的三个触发点全部证伪。** 原文是：

| 我说的操作 | 我说的后果 | ⛔ 实际 |
|---|---|---|
| `git clone` | 3 个文件命中 A-1 | ⛔ **新克隆根本没有锚**（锚在 `project.parent/.devloop-anchors/`，不随仓走）→ 输出是「没有锚文件，先跑 anchor」，与换行符无关 |
| `git checkout -- <文件>` | 该文件命中 | ⛔ 不重写，见下 |
| `git stash` + `pop` | 该文件命中 | ⛔ 受保护文件根本不进 stash |

**挡住它的是 git 的索引 stat 缓存。** 真仓实测：

```
.devloop/constitution.toml  索引记 10051 · 盘上 10051 · blob 9893
.devloop/constitution.md    索引记  7588 · 盘上  7588 · blob 7588
.devloop/rules-digest.md    索引记  2342 · 盘上  2342 · blob 2342
.devloop/gates.sh           索引记  8683 · 盘上  8683 · blob 8539
git diff-files --name-only .devloop/  →  （空）
```

索引记的是**盘上那份**的 stat，git 因此判定 up-to-date，
`checkout_entry` 直接跳过重写，autocrlf 的 smudge 过滤器**没有机会跑**。

⛔ **我的探针自己制造了它要测的条件**：它用 `shutil.copytree` 复制仓库，
新文件的 inode/ctime 全变了 → 索引 stat 失配 → git 判「needs update」→
checkout 时才重写成 CRLF。⚠️ **夹具的副作用被当成了被测对象的行为。**
⭐ 这是本项目「判据维度错」的又一形态：我量的是复制动作，不是 git 动作。

## ⭐ 真正剩下的是什么

1. **指纹无法从 git 内容推导**（`constitution.toml` 盘 10051 vs blob 9893，
   `gates.sh` 8683 vs 8539）。⚠️ 于是「这文件被改成什么了」这个审计动作做不了
   ——⭐ 我就是因为推导不出来才撞见整件事的。
2. **条件性风险仍在**：一旦索引 stat 失配（新机器克隆后把锚拷过来、
   或任何改了 mtime/inode 的工具动作），checkout 就会重写成 CRLF，
   那时才**真的**会误报。⚠️ 触发条件比我原先说的窄得多，但不是零。

⚠️ 下面这段「误报把锚变成橡皮图章」的推理**前提被削弱了**（误报没那么容易发生），
但作为设计原则仍然成立，保留备查——⛔ 它现在是理论论证，不是实测后果。

⚠️ `gates.sh` 碰巧对得上（它本来就是全 CRLF），另外三个盘上行尾是**混的**
（`constitution.toml` = CRLF 158 + 裸 LF 49；`constitution.md` / `rules-digest.md`
全裸 LF），因为它们是工具写出来的而不是 git 检出来的。

## ⛔ 严重度不在误报本身，在误报会把锚变成橡皮图章

失效方向是**安全的那一侧**：内容变了指纹必变，⭐ 不会漏报。
但后果链是：

```
A-1 因为行尾误报 → 人查了半天发现没人改过 → 「重新锚定一下就好了」
  → 下一次 A-1 再响，人不查了，直接锚
    → ⛔ 而那正是设计明令禁止的自愈：「锚一旦能自愈，它就不是锚」
```

⚠️ 一个会因为环境而误报的停机判据，训练出来的反射恰好是它要防的那个动作。
⛔ 这比「少报一条信息」严重得多——它腐蚀的是判据本身的可信度。

**第二个后果**：锚里的指纹**无法从仓库内容重新推导**，
于是「这文件被改成什么了」这个审计动作做不了——我就是这么撞上它的。

## 修法（⚠️ 未做，需要用户定）

| | 做法 | 代价 |
|---|---|---|
| **A** | `_sha()` 先把 CRLF 归一成 LF 再哈希 | ⚠️ 所有指纹变化 → **要再锚一次**；⭐ 工具侧一处改动，对所有接入项目生效 |
| **B** | 仓里加 `.gitattributes` 钉死 `eol=lf` | ⚠️ 同样要再锚一次；⛔ 且每个接入项目都得各加一份 |
| **C** | 不动 | ⛔ 保持现状：clone/checkout/stash 会误报 |

⭐ 倾向 **A**：⚠️ B 只修本仓，而 DevLoop 是**跨项目**工具，eco-ob 等接入项目
会各自再踩一次。A 还顺带让指纹可以从 git 内容推导出来，把审计路径修好。
⛔ 但两者都要求你再跑一次 `constitution anchor`——那是只有你能做的动作。

---

### ✅ G-105 · 差异表在人按下批准的那一刻对他说了假话

**2026-08-03 晚，用户跑重锚时当场撞到——⭐ 而那正是这个功能上线的第一次真用。**

G-98 刚给 `anchor` 加了差异打印，理由是「整套设计依赖人是那道闸，而那道闸
当时是蒙着眼睛的」。⛔ **第一次真用，那张表印出来的四样东西里有四样是错的：**

| 屏幕上说 | 实际 |
|---|---|
| 5 个文件「（当时不存在）」 | ⛔ 5 个**全都在旧锚里**，白纸黑字 |
| 「登记于（读不出来）」 | ⛔ `2026-08-03T11:32:53`，普通 JSON 字段，一直好好地在文件里 |
| `rules-digest.md` 变了 | ⛔ 新旧指纹**完全一样**（`52a4e7a65dd2`），根本不该出现 |
| `constitution.toml（宪法自身）` + `.devloop/constitution.toml` 两行 | ⚠️ 同一个文件，看起来像两处改动 |

## ⭐ 根因：两件事共用了一个函数

`anchor_delta` 与 CLI 的「登记于 …」都在调 `read_anchor`。

⭐ 而 `read_anchor` 对旧口径 / 坏锚返回 `None`——**在判定路径上那完全正确**：
基准不可信就不该拿来判。⛔ 但这两处的活是**告诉人现在是什么情况**，
是另一件事。于是 `old_files` 变成空 → 一切看起来都是新增的。

⚠️ **这是一个「同一个名字被两种用途共用」的经典缺陷**，
与本仓 `gate_code` 分开 0/1/2（活没干好 vs 闸自己坏了）是同一条道理：
**判定** 与 **展示** 不是同一件事，不该走同一条路。

## 修法

新增 `peek_anchor`：尽力读，读不出来就返回空，⛔ **只许喂给 `print`**。

- `anchor_delta` / CLI 时间戳改用它
- 宪法自身那一行，只在它不等于任何受保护文件条目时才单列
- 旧口径的锚，明说「下面的差异有一部分只是**口径变化**，不是文件被改了」
- ⛔ **防线**：一条 AST 测试钉住 `verify_anchor` 里不许出现 `peek_anchor`
  ——⚠️ 宽松读法一旦漏进判定路径，「锚坏了拒绝判定」那条规矩就没了

6 条测试，红检 3 条直接钉住用户看到的那一幕。

---

### ✅ G-104 · 工具往项目里写东西却忘了忽略它——**第三次**

**2026-08-03 六视角复审查出。**

| 时间 | 漏的是谁 | 后果 |
|---|---|---|
| 2026-07-?? | `.devloop/handoff/` | 一次 `git add -A` 把探针目录扫进提交 `f974cb2`（3 文件 124 行） |
| 2026-08-02 | `.devloop/findings.jsonl`（G-93） | `cmd_audit` 一写它活工作区就变 → 从第 2 单起每单命中 A-2 → **全阶段连坐** |
| **2026-08-03** | **`.devloop/dossier/`** | 同上。⭐ 而 `dossier.py` 的开头注释**专门写着**「我避开了 G-93」 |

## ⭐ 为什么每次都会漏

「运行产物」这件事登记在**两个地方**，而它们没有任何机械关系：

| 在哪 | 谁用它 |
|---|---|
| `records.RUNTIME_DIRS` | 代码判「这是工具自己写的」 |
| `.gitignore` | git 判「这个不算改动」 |

⚠️ 加新功能的人只会想到**前一个**（写代码要用它），后一个**全靠记得**。
⛔ 三次教训说明：**靠记得是不行的。**

## ⛔ 后果为什么这么重

```
工具写一个文件 → git status 看得见 → 宪法「活工作区变了」命中
  → 而 ws_before **整个阶段只采一次**（cli.py）
    → 从**第二单起每单都命中**
```

无人值守跑一夜 → 早上十条「越线，等你批准」→ ⭐ **一条真的都没有**。
项目自己的原话：**天天红的守卫等于没有守卫。**

## 修法：⛔ 不判「dossier 在不在里面」

那只钉住这一次。⭐ 判**两份清单的对应关系**（`tests/test_runtime_dirs_ignored.py`）：

1. `RUNTIME_DIRS` 里的每一项，`.gitignore` 里都得有
2. ⚠️ **反方向也判**：`.gitignore` 里挂着的产物目录，`RUNTIME_DIRS` 里得有
   （只判一个方向，两份清单会慢慢漂开）
3. ⭐ 再加一条判**后果**：真写一份卷宗，`git status` 必须还是干净的
   ——⚠️ 只判清单的话，将来有人换了写盘位置照样出事

**下一个人加第五个产物目录时，这三条会当场转红。**

---

### ✅ G-103 · 工具停着，而它印的自救命令敲不通

**2026-08-03，用户当时正需要那条命令。**

屏幕上是：

```
python -m devloop.cli constitution anchor --pro
```

⛔ 句子在这里断了。`constitution.py` 里写着 `self.stderr.strip()[:200]`，
而项目路径 `C:\pg\_infra\devloop` 正好在第 201 个字之后。

⚠️ **同一个毛病 `nightly.py` 也犯过**（砍 100 字）。
⭐ 两次都恰好砍在「你该干什么」那一句上——**这不是巧合：那句话总在最后。**

## 修法

⛔ 判据不是「砍多少字」（把 200 改成 600 只是把坑往后挪），
⭐ 是**截断点若落在某一行中间，就把那整行留全**（`_keep_命令`）。

三条测试：① 自救命令与其中的项目路径必须完整
② 截断规则本身（长消息要截，但最后一行不许是半句）③ 短消息一个字都不许动。

---

### ✅ G-102 · 三档的账是**做平的**，不是真平的

**2026-08-03 对抗审计（45 个 agent、5 个角度）查出的三个洞。**

上午刚建好「三档相加恒等于总数」的不变式，⭐ 下午审计发现**那条不变式自己有三个后门**：

| | 洞 | 后果 |
|---|---|---|
| ⛔ **同族豁免** | `fam("C-1a") == "C-1"`，而 C-1b 登记了 | C-1a 借兄弟的名额过关。实测三档相加 **17**、散文 **18**，⚠️ **而守卫这条不变式的测试是绿的** |
| ⛔ **`by` 判字数** | `len(by) > 8` | 9 个字的废话能过；**指向根本不存在的代码也能过**。实测抓到 C-4 的 `by` 写着 `worktree.commit_result`——少写一层 `Worktree`，那个名字不存在 |
| ⛔ **静默后门** | 覆盖率统计把 `[[protected_ref]]` 算进「有机械判据」，⚠️ 而 `load()` **从不解析它** | 往 toml 里加一条 `[[protected_ref]] clause="A-2"`，测试立刻转绿、尾巴少一条，**判据一行没多** |

⭐ C-1a 尤其要命：它散文里标着 `[judge: auto]`（**自称有机械判据**），
而保护它的那条 `[[protected_tree]]` 是**注释掉的**。
一条自称能判的条款，既没判据、又不在「判不了」清单、又不出现在尾巴里。

**修法**：

- 取消同族豁免，**逐条登记**（子条款各自有名额）
- `by` 改判「引用能不能解析」：抽出 `tests/x.py` / `devloop/x.py` / `模块.属性`，
  文件走盘、点号路径走 `importlib` 逐级 `getattr`
- `load()` 拒绝任何它不解析的 `protected_*`
- 新增判据「散文里标 `auto` 的必须真有判据」——⭐ 单这一条就抓得住 C-1a
- 覆盖率测试改用**生产判据** `_match` + `git ls-files`（原来用 `Path.glob`，
  两套语义、且对合法的 `path=` 写法直接 `KeyError`）

⚠️ **判据自己也误报过一次**：`模块.属性` 的正则把散文里的 `gates.sh` 当成了代码引用。
⭐ **守卫过宽和过窄一样坏**——排掉文件扩展名之后才对。

**结果**：那句尾巴从「未覆盖 **7** 条」变成「未覆盖 **9** 条 · 另有 6 条判据在代码里」。

---

### ✅ G-101 · 硬拒够不着自动驾驶——无人值守整夜握着 WebFetch

**2026-08-03 对抗审计。**

`constitution.toml` 里 `[refuse] full_tools_when_detached = true` 的意思是
「无人值守时不许给 `full` 档」——因为 `full` 比 `implement` 多的**就是 WebFetch**，
那是一条**对外通道**，而「对外发送」事后零痕迹（B-4 已登记为判不了）。

⛔ 而 `refuse_preflight` 的**唯一调用点**在 `cmd_dispatch` 里：

```
cmd_autopilot → autopilot.preflight()   ← 只 load + verify_anchor
              → _run_unit(tools="full")
              → dispatch.py 传 --allowedTools ...,WebFetch
```

在计划里写 `tools = "full"` 就能让工人**整夜**持有 WebFetch，
而那行配置在这条路上是**纯装饰**。

## ⭐ 根因：判据挂在开关上，不挂在事实上

参数原来叫 `detach`——一个**只存在于 dispatch 那条路**的命令行开关。
自动驾驶没有这个开关，于是判据在那条路上无从谈起。

⚠️ **这不是措辞问题：概念错了，接线就一定漏。**

**修法**：改名 `unattended`（「有没有人在看」），`autopilot.preflight` 恒传 `True`
并**逐单、在开跑之前**查（⛔ 不是跑到第 7 单才炸——那时前 6 单的钱已经花了）。

---

### ✅ G-100 · 工人把分支推到远端，宪法全程 0 命中

**2026-08-03 对抗审计**（探子真建了 bare 仓、真推了一次）。

```
git push origin HEAD:refs/heads/leaked-branch
→ * [new branch]   HEAD -> leaked-branch        （成功）
→ 本地新增 refs/remotes/origin/leaked-branch
→ constitution.check_refs → ⛔ code = 0，零命中
```

代码已经出仓，而台账、回执、diff 里**一个字都查不到**。

**为什么**：`check_refs` 的循环起点是 `for name, obj in before.refs.items()`
——**T0 时不存在的 ref 天然不在遍历范围里**。

## ⛔ 而 toml 声称挡它的东西，主语是错的

原文：`by = "tests/test_no_git_write_verbs.py：全仓 devloop/*.py 里不许出现 push"`

⚠️ 那是一条**静态检查**，管的是**工具自己的源码**。
而 C-3 的主语是**工人的动作**——⛔ **工人手里有 Bash。**

**修法**：

- 比对**全部** ref 的集合差：新增 `refs/remotes/*` → C-3；
  新增 `refs/heads/*`（非 `devloop/` 命名空间）/ `refs/tags/*` → B-2
- `Snapshot` 加 `remotes` 字段（⛔ **末尾**，G-95 的教训）：
  新增远端 / 改远端 URL → C-3（⚠️ `git remote add` 之后推过去，ref 一条都不变）
- ⛔ 防假红：工具自己的 `devloop/*` 分支与 `refs/stash` 豁免
- ⭐ **判不了的那半显式登记为 C-3b**：`git push <完整URL>` 不经过具名远端，
  本地零痕迹。⚠️ 不拆就等于**用挡住的一半盖掉没挡住的一半**

---

### ✅ G-99 · 工人改闸改测试再提交，闸与宪法一起转绿

**2026-08-03 对抗审计实测复现——今天最重的一条。**

工人在 worktree 里做三件事然后 `git add -A && git commit`：

1. 把 `tests/test_constitution_coverage.py` 的 10 条断言全改成 `assert True or ...`
2. 往 `.devloop/gates.sh` 末尾追一行 `fail=0`
3. 往 `.devloop/rules-digest.md` 追两行

```
PASS  测试守卫    tests/ 未被改动              ← ⛔ 字面为假
PASS  禁改清单    .devloop/ 已跟踪且未被改动    ← ⛔ 字面为假
宪法：无宪法命中
```

单判 `ok=True`，分支被推荐给人 `git merge`。**合入后闸恒绿、覆盖率测试恒绿。**

## ⛔ 两层守卫为什么同时瞎

| 守卫 | 判据 | 为什么瞎 |
|---|---|---|
| `gates.sh` 的「测试守卫」「禁改清单」 | `git status --porcelain` | ⛔ 工人**一提交**，工作区就干净了 |
| 宪法 `check_tree`（base 锚定） | 与 base 提交比对 | ⛔ 那两条路径**一条都没登记** |

⭐ 而 `constitution.toml` 开头白纸黑字写着树内判据存在的**唯一理由**：

> ⚠️ 为什么不是 `git status`：工人把改动**提交掉**，工作区就干净了……base 锚定照样抓得到。

**该被它接住的那两条，此前一条都没进来。**

⚠️ `[[protected_file]]` 也接不住：那一档锚的是**活项目**里的文件，
而工人干活在 **worktree** 里——两个不同的地方。

## ⭐ 红检揭示的另一件事：洞纯粹在配置里

5 条攻击测试在**修复前的代码上是绿的**（它们自带的夹具 TOML 已登记那两条路径）。
⛔ 也就是说：**判据代码一直都在，清单里没有目标。**
⚠️ 与 rules-digest 那次教训**方向相反**的同一个形状。

## ⚠️ 修这条最容易踩的坑：把守卫做成天天红

实测本仓：174 个「base 里没有的新增文件」中 **144 个是运行产物**，
其中 **90 个**会被这几条模式命中。⛔ 不排除它们，`.devloop/**` 上线第一单就红。

**修法**：

- `Protected.allow_added`（⛔ 末尾字段）：`tests/**` **改已有=命中、新建=放行**，
  与 `gates.sh` 的测试守卫同一套分级语义
- `_is_artifact`：运行产物不算「新增文件」。⛔ 名单**写死在 Python 里，不读
  `.gitignore`**——工人对 `.gitignore` 与 `info/exclude` 都有写权限
- 顺带修掉捞回的《9》：`load()` 的 0 命中校验从「**盘上**有没有」改成
  「**git 认不认得**」。⚠️ 两个方向都错过：
  没进版本库的目录（如 eco-ob 的 `.devloop/`）守卫一直空转；非 git 目录报错误导
- ⭐ **新判据抓到的第一个真目标是我们自己的 E2E 夹具**：它建了
  `baseline/ref.json` 却从没 `git add`，那条守卫一直是空的

---

### ✅ G-98 · `constitution anchor` 是盲签

**2026-08-03，用户在没看差异的情况下按下了它——而那正是整套设计依赖的那道闸。**

`anchor` 分支的全部实现曾经是三行：

```python
a = constitution.write_anchor(paths, con)
print(f"已登记 {len(con.files)} 个受保护文件的指纹 → {a}")
```

⛔ 没有：锚是否已存在的判断 · 新旧指纹对比 · 二次确认 · 旧锚备份。
**在已有锚之上再锚一次，输出与首次锚定逐字相同。**

⭐ 对照：同一模块读 toml 那一侧极其严苛——schema 不认、路径不存在、
glob 匹配 0 个、未登记 unjudged，四种全部当场炸。**锚这一侧一道校验都没有。**

## ⛔ 后果链

```
工人在两次派单之间往 gates.sh 里加一行 exit 0
  → 人因为别的原因跑了一次 anchor
    → 命令印「已登记 5 个受保护文件的指纹」并返回 0
      → 被改过的 gates.sh 成为新基准，旧锚被覆盖且无备份
        → 此后 verify_anchor 永远返回 0，闸已空而每单都报「无宪法命中」
```

**修法**：

- `anchor_delta()`：逐条印 `旧指纹 → 新指纹`
- 有变化且没给 `--force` → 退出码 3，**不写盘**
- 旧锚另存 `.prev`；写锚改为「临时文件 + 原子改名」
- ⛔ 首次锚定**不加**摩擦——无谓的摩擦会让人养成一律带 `--force` 的习惯，
  那等于没有闸
- 顺带（捞回的《6》《7》）：锚加 `schema` 与形状校验，四种坏法分开报；
  ⛔ 关掉「缺 `constitution_sha` 就静默跳过 A-1 自指判据」那个短路

---

### ✅ G-96 · 第三档登记了，却从没出现在人读到的那句话里

**2026-08-03，用户事后审查中当场发现。**

前一天新增第三档 `[[judged_elsewhere]]`（判据在代码里的条款），
理由写得很清楚：**「让『未覆盖 N 条』那句尾巴别少报」**。做完的状态是：

| 在哪 | 状态 |
|---|---|
| `.devloop/constitution.toml` | ✅ 6 条登记好了 |
| `tests/test_constitution_coverage.py` | ✅ 全绿 |
| ⛔ **`constitution check` 印出来的那句话** | **一次都没提过第三档** |

原因：`constitution.py` 里有 **10 个** `ConstitutionResult(...)` 构造点，
新字段**只给 2 个传了**——而那 2 个都是 `code=2` 的故障路径。
所有正常路径（`check_files`/`check_refs`/`check_tree`/`check_records`/
`check_workspace`）一个都没传。

⭐ **加那一档的唯一目的，在输出里整个落空了。**
与 G-94（`--discard` 实现了但控制流够不着）同形：实现了，生产路径够不着。

## ⛔ 附带的第二条：一句真正的假话

`check_workspace` 连 `Constitution` 都不收，传的是 `[]`，于是 `summary()` 印出

> ⚠️ 未登记任何「判不了」的条款——请确认这是真的

——**本仓登记了 7 条**。⚠️ 而 `cli.py` 只在 `not r.clean` 时才印它，
也就是**真命中、人最认真读的那一刻**，尾巴是假的。
⭐ 同一个 bug 在 `check_records` 那里修过并留了注释（`constitution.py`），
这一处漏了——那正是「同样的账在多处各记一遍」必然的结局。

## ⛔ 为什么已有测试抓不到

`test_constitution_coverage.py` 判的是「TOML 里三档相加等于散文条数」。
⚠️ 它读的是**配置文件**，缺陷在**从配置到输出**那一段。
⭐ 判据的维度错了：**账平不平，跟人看不看得见，是两件事。**

## 修法

⛔ **不是「把 8 处补齐」**——那只是把同一个错再抄一遍的机会往后挪
（⚠️ 我前一小时刚因为「手抄一遍」栽了 G-95）。改成结构性的：

- 新增 `Constitution.result(code, hits, stderr, *, extra_unjudged)`，三档在**一处**配平
- 全部 10 个构造点改走它；`check_workspace` 补上 `con` 参数
- `tests/test_constitution_result_from_con.py` 5 条，判据分两层：
  ① **结构**（AST：模块里不许有裸构造）② **后果**（真跑一次，尾巴里必须有第三档）
- ⭐ 红检：在 `git worktree --detach HEAD` 的**真实修复前版本**上跑，5 条全红

**现在的输出**：

```
无宪法命中 · ⚠️ 未覆盖条款 7 条：A-2、A-3、A-5、B-3b、B-4、C-1b、C-7
           · 另有 6 条判据在代码里（B-1、B-2、C-3、C-4、C-5、C-6）
```

---

### ✅ G-95 · 数据类新字段插在中间 → 位置传参整体错位

**2026-08-03，我自己引入，十分钟后自己实跑撞见。**

给 `ConstitutionResult` 加第三档统计（`elsewhere`，「判据在代码里的条款」）时，
我把它写在了 `stderr` **前面**：

```python
code / hits / unjudged / elsewhere / stderr   ← ⛔ 错
code / hits / unjudged / stderr / elsewhere   ← ⭐ 对
```

⚠️ 而 `check_files` 是**按位置**造这个对象的：

```python
ConstitutionResult(2, hits, con.unjudged, "受保护文件与锚不符")
                                           ↑ 本意是 stderr，喂进了 elsewhere
```

于是那句话被当成**条款列表**逐字拆开，实跑印出来：

```
另有 9 条判据在代码里（受、保、护、文、件、与、锚、不、符）
```

⛔ **它出现在故障上报路径上**——正是人最需要看懂发生了什么的时刻，
诊断信息变成了一句乱码。

⭐ **发现方式与 G-94 完全同形**：不是测试抓到的，是我为了给人讲清楚
A-1 是怎么回事，跑了一遍 `constitution check` 看见的。⚠️ 当时 647 条单测全绿
——那些测试都直接构造 `ConstitutionResult(...)` **传关键字**，
一条也不走 `check_files` 的那个位置传参调用点。与 `head_tools` 那个
NameError 是同一类：**缺陷长在测试不走的那条路上**。

**修法**：字段挪到末尾 + 两条测试（`tests/test_constitution_coverage.py`）。
⭐ 两条判的是不同的东西，缺一不可：

| 判什么 | 抓得住 |
|---|---|
| `fields()` 里 `stderr` 排在 `elsewhere` 前面 | 这一次的具体成因 |
| 位置传参造出来的对象，`elsewhere == []` 且 `summary()` 不含「受、保、护」 | **任何**造成同样错位的原因 |

⛔ 只写第一条就是把判据钉死在这次的成因上，将来换个方式错位它就瞎了。

**留给下一次的规矩**：⭐ **给数据类加字段一律加在末尾**，
或者把该处的构造改成关键字传参。⚠️ Python 不会为此报任何错——
类型对不上也不会，因为 `str` 和 `list[str]` 都能迭代。

---

### ✅ G-94 · `--discard` 实现了但控制流够不着

**当场踩到（2026-08-02，加这个开关的同一次改动里）。**

`prune --discard <分支>` 是为了「点名弃掉一支**未合并**的产出」——
`--delete` 只碰工具判定为已合并的，而早上真正要做的动作恰恰是弃掉
那支闸没过的。

⛔ 实现写好了，但控制流里 `if not args.delete: return 0` 排在它**前面**：

```
单给 --discard  →  提前返回  →  一支都没删，还印「只归档了，一个都没删」
```

⚠️ **而它当时看起来是好的**——第一次实跑我同时给了 `--delete`，
于是碰巧走得到那段。⭐ 这正是第②种假绿：实现了但生产路径够不着。

**修法**（`c195256`）：`if not args.delete and not args.discard: return 0`，
并把 `--delete` 的那段单独往后挪。新增 5 条测试，⭐ 含红检
（把控制流退回去，3 条当场转红）。

⚠️ 值得记的是**发现方式**：不是测试抓到的，是我实跑一遍看输出发现的。
本项目的纪律「单测绿不代表接线通」又应验一次。

---

### ✅ G-93 · `findings.jsonl` 污染活工作区 → 宪法 A-2 全阶段连坐

**2026-08-02 设计复核顺手抓到，与当时在做的功能无关。**

`.gitignore` 忽略了 `.devloop/reports/`、`telemetry.jsonl`、`jobs/`、
`handoff/`、`autopilot/`，⛔ **唯独漏了 `findings.jsonl`**（以及
`cmd_audit --spec` 生成的 `tasks/fix-*.md` / `verify-*.md`）。

后果链：

```
cmd_audit 写 findings.jsonl → 活工作区变了
  → 而 cmd_autopilot 的 ws_before **整个阶段只采一次**（cli.py）
    → 从第 2 单起每单命中宪法 A-2「活工作区变了」
      → ⛔ 全阶段连坐
```

⭐ 与 G-71（记录守卫误判 autopilot 自己的运行记录）**同一个形状**：
工具自动写进项目里的运行产物，被守卫当成了人为改动。

**修法**（`863af0f`）：`.gitignore` 补三条，并写清后果链——
⚠️ 下一个人加「工具往项目里写文件」的功能时，要先想这一步。

---

### 2026-08-02 · 一轮修复 + 两轮独立复核（G-76 ～ G-92）

⭐ **这一天最值钱的不是那 19 条修复，是「复核推翻了修复者」这件事本身。**

三轮：① 六角度只读审计 → ② 逐条派独立探子取证（**要求推翻**，不是核对）→
③ 范围守门人合看 → ④ 对抗复核专门推翻已落地的修复。

⛔ **每一轮都推翻了上一轮的结论**，包括我自己写在提交信息里的：

| 我说的 | 复核证明的 |
|---|---|
| 「自动驾驶靠 1/2 分岔决定重试还是叫人」 | ⛔ 不成立：`plan_next` 是纯计数，两处都不读 code |
| 「eco-ob worktree 偏小 3 倍」 | ⛔ 610MB 含 135MB 的 `.git`；真值 251→361，偏差 20–45% |
| 「prune 误报 5 对 3」 | ⛔ 是 4 对 4；且 `L4` 的措辞碰巧对只因被 `merged` **遮住** |
| 「禁改清单是活着的假绿」 | ⛔ 本仓 122/122 提交都跟踪 `.devloop`，那支是死代码 |
| 「要验波次上限得跑真单」 | ⛔ 零额度 E2E 台架当场写出来了——那前提正是我为代理判据辩护用的 |
| 「code 1 的说辞里不许出现『闸自身故障』」 | ⛔ 被**同一次提交新增的测试名**破掉（gates.sh 把 pytest 节点名放进 detail） |
| 「max_dispatches 是唯一还活着的刹车」 | ⛔ 墙钟与看门狗也还活着；准确说法是「**烧了多少**这个维度上唯一的」 |

⚠️ 还有两条是**我自己引入的**：波次收窄提示在混排计划下必然误报
（一句话里自称被配额拦住、同时自报还剩 50 次）；E2E 桩绕过台账的锁导致偶发变红。

⭐ 结论写给下一个人：**「修完 + 测试全绿」离「修对了」还差一轮对抗复核。**
本轮 11 条新测试在被写出来的当天就被证明可以绕过。

---

### 🟠 G-90 · 跨 `--resume` 的配额放大

`fired` 是进程内计数，`--resume` 是新进程，跨进程唯一权威是台账。
而 `telemetry._append` 的取舍是「拿不到文件锁也照写」，掉行是被文档承认的现实。

**实测**：`max_dispatches=10` 跑满 10 次写 10 行 → 手工删到 3 行 →
fresh 进程 `--resume` **又派了 7 次，合计 17 次**。

⚠️ 这个洞在 G-78 之前之后一样大，G-78 既没制造也没恶化它。
⛔ 但 G-78 让并行波真正跑满、写台账更频繁，**恰好抬高了触发掉行的概率**。

**触发条件**：要修在「跨进程配额」这件事被真正需要的时候
（目前 `autopilot` 没有 `--detach`，`cmd_dispatch` 有 `--detach` 但不读 `max_dispatches`）。

---

### 🟠 G-91 · 台账里非法字节落在字符串内会静默通过

`telemetry.load` 用 `errors="replace"`，`ÿ` → `�`。
而 `{"ts": "��"}` **是合法 JSON**——解析成功，字段内容已损坏。

⚠️ `ts` 坏了会让「只认本阶段之后的行」判错；`task` 坏了会让那行归不到任何任务上
——两者都让刹车读到偏小的数。⛔ 而 `load` 看不见它：对它来说这就是一行好行。

**已有测试** `test_非法字节落在字符串里时会静默通过_这是已知残留` 明写这是残留，
⭐ 免得有人以为 G-82 修完这个洞就没了。

**触发条件**：要真堵住得给每行加校验和 —— 那会改台账的形状，
⚠️ 而台账是失控防线的唯一真源，改它要单独立项。

---

### 🟠 G-92 · `_HEADROOM_MB = 500` 没有实测依据，而它现在是唯一兜底

删掉 `_FALLBACK_SIZE_MB` 之后，「量不出来」时只剩这一条守着。
⛔ 而这个数没有任何注释说明为什么是 500 而不是 200 或 2000。

⭐ 「我不知道它多大，但少于 500 MB 空闲一律不开工」是个**站得住的判断**
（不依赖任何估算），这也正是它能顶上来的理由。⚠️ 但它现在承重变大了，
值得单独论证一次：eco-ob 一个 worktree 361 MB，500 MB 余量只够**再开一个**。

**触发条件**：下一次在盘紧的机器上跑无人值守之前。

---

### ✅ G-71 · 记录守卫把 **autopilot 自己的运行记录**误判成「抹除」

**真跑抓到（2026-08-01 崩溃恢复演练）**，第 3 单被判失败：

```
宪法命中 A-2 不得写到隔离区之外（记录被抹）
（devloop/autopilot/resume-drill.json：前缀被改写（抹掉历史再补））
```

那是 autopilot **自己的运行记录**，它每一轮都要重写（`run.save()`）。

⚠️ 「只追加」这个判据对**账本**成立（telemetry / findings / reports / tasks），
对**可变状态文件**不成立。⛔ **后果：每次自动驾驶跑到第 3 单都会被自己的守卫判失败。**

⭐ 讽刺的是，`records.py` 的第一条设计原则就是
「误报比漏报更致命，天天喊狼来了的守卫会被人关掉」——**然后自己踩了**。

**修法**（`26cd678`）：排除 `jobs`/`handoff`/`autopilot` 三个目录。
⭐ 判据是现成的、不是现编的——本仓库 `.gitignore` 早就把它们定性为
「工具自动在项目里建的——**运行产物，不是源码**」。
⚠️ 排除判在**相对 `.devloop` 的第一段**上，不用子串匹配（后者会误伤名字含 `jobs` 的回执）。
新增 3 条测试，其中一条（`test_台账被改写仍然要命中`）专门防止这次放宽把守卫掏空。

---

### 🟠 G-65 · 撞额度停批**从未真触发过**——怎么在不真撞的前提下验它

**状态**：判据与停批已实现（`a8d6932`），⛔ **停批本身零次真触发**
**来源**：2026-07-29 交叉核对抓到我自己的过度声称

#### ⛔ 先把口径摆正

我曾在 SPEC 状态表里把它写成「**端到端真跑两次**」。实际：
真跑到的只是 `rate_limit_event.status: "allowed"` 的**解析路径**。
`a8d6932` 的提交正文自己就写着「A 层的 `rejected` 形态本项目**没有实测样本**」。

复核（2026-07-30）：`.devloop/telemetry.jsonl` 共 21 行，
`failure_class = "ratelimit"` 出现 **0 次**。

⚠️ 这正是这个项目一直在防的那件事：**「写了 + 单测绿」不等于「真跑过」**。

#### 现在到底验到了哪一层

  · `tests/fixtures/` 里唯一的真实样本是 `stream_allowed.jsonl`（`allowed` 形态，脱敏后原样保留）
  · `rejected` 在测试里是**手工构造的 `RateLimit` 对象**（`tests/test_quota.py:127` 等）
    → ⛔ 走不到「从一条真的 stream-json 行里解析出 `rejected`」那一段
  · `tests/test_quota_halt.py` 从 `main(["dispatch", ...])` 整条路径进去，
    注入 blocked 之后断言：批次真停 · 退出码 **3**（还没跑完，不是 1）· 台账 `failure_class = ratelimit`
  · 自动驾驶路径上停批信号**一度根本没接**（`8d9f6f4`：`cmd_autopilot` 调
    `_run_unit` 没传 halt）。补的守卫是 **AST 比对两个 `_run_unit` 调用点的关键字参数集合**
    ——⛔ **判据落在源码上，不落在行为上**（要真触发得撞真额度）

#### ⛔ 为什么不许为了取样去真撞一次

撞一次就把当前那个 5 小时窗口烧掉。而官方**从没定义过窗口起算点**（查过 6 个官方页面），
只能等 `resetsAt`（⚠️ 精确 **unix 秒**；⛔ 别与凭据那边毫秒的 `expiresAt` 混）。
⛔ 拿「等 5 小时」估算的误差上界是**一整个窗口**（实测那个窗口 18:40→23:40，
23:30 撞墙按估算要睡到 04:30，白等 4h50m）。

#### 候选验证法（⛔ 都还没做，代价没算完）

**A. 假二进制走真解析路径**：`DEVLOOP_CLAUDE_BIN` 已经是个现成的环境变量
（`dispatch.py:121` 与 `doctor.py:185` 都在读它）→ 用一个只吐预置 stream-json 的假 `claude`
起**真子进程**，让 `rejected` 行走完整条「解析 → 判据 → 停批 → 台账」。
  - ⚠️ 它验的是**我们这边**；⛔ 验不了「真限流时 CLI 究竟吐什么」——
    那一行的形状仍然是照 zod schema 手搓的。

**B. 机会主义取样**：日常干活迟早会真撞上一次，当时把**原始 stream 行**落盘进 fixtures。
  - ⚠️ 要求人在撞的那一刻记得留证据，⛔ 所以只能当补充，不能当计划。

**C. 拿 zod schema 当权威**：`status` / `rateLimitType` 的取值集合是 2026-07-29
从 `claude.exe` 抠出来的，可据此断言解析器认全部取值。
  - ⚠️ 只证明「我们认得 schema 说的形状」，⛔ 不证明服务端真发这个形状。

⛔ **在拿到 A 或 B 之前，任何文档里都不许把停批写成「端到端验证过」。**

#### ⚠️ 顺带记住这几条反判据（它们比正判据更容易漏）

  · ⛔ 不许拿 `subtype` 当判据——限流回执的 `subtype` **仍然是 `"success"`**
  · ⛔ 「Server is temporarily limiting requests (not your usage limit)」是
    **服务端容量限流**，与套餐额度无关；当成额度用尽会白停几小时
    （原串抠自 `claude.exe` 常量表 `Gs9`，不是编的）
  · ⛔ 周上限拿不到精确时刻**一律不自动等**
  · ⛔ `CLAUDE_CODE_RETRY_WATCHDOG` 必须显式置空，否则 429 走重试路径，
    「秒退 → 停批」的前提就没了

---

## ✅ G-137 · 闸递归调自己 + 清理失败静默吞掉（2026-08-22 修）

⭐ 与 latch 侧往返 9 轮（`latch/docs/audit/04~13`）后**双方判 CONVERGED** 的产物。

### ⛔ 症状一：闸对任何输入都超时报红

`devloop gates --commit HEAD` → `6 过 / 1 未过：pytest（超时 >600s）`。
六道全绿，唯一的红是「测试跑不完」。⚠️ **08-17 起 2/2 命中，⛔ 08-20 才被发现。**

**根因（D-1 实测）**：

| | 秒 |
|---|---|
| 全量 pytest | **769** |
| 排除 `test_preflight_roster.py` | ⭐ **117** |
| ⇒ 那一条独占 | ⛔ **652（85%）** |

它调 `run_on_commit(REPO, "HEAD")` —— **对自己跑** ⇒ 闸跑全量 pytest ⇒ 里面又有这条 ⇒ 又起一个闸。
⭐⭐ **而 652 ≈ 内层自己的 600 秒超时 —— 界不是设计出来的，是被另一个超时兜住的。**

**修法（latch 裁定的 B 案）**：改成对**样板仓库**跑。
⛔ **样板仓库必须用真 `.devloop/gates.sh`（运行时动态复制，仓库里零静态副本），只有被测内容是假的** ——
⚠️ 配假闸的话，这条测试验的是「假闸名册 = 假闸实跑」⇒ **对真闸零信息量，B 静默退化成「干脆不验」。**

**验收判据**：`test_真改坏闸文件时必须判红`。**红检**：fixture 换成非原样闸 → **当场红 3 条**。
**结果**：全量 **769 → 108 秒（快 7 倍）**。

### ⛔ 症状二：清理失败一声不吭 ⇒ 撑爆 C 盘

`worktree.py::Worktree.remove` 与 `gates.py::run_on_commit` 的 `finally` 都
`capture_output=True` 且**不查返回码**。
⚠️ 后者注释写着「清理失败不应掩盖闸的结论」——⭐ 前半句对，⛔ **后半句被执行成了「一个字都不说」**。

> ## ⭐ 不掩盖结论 ≠ 不出声。

**代价（2026-08-21）**：`%TEMP%` 堆 **2,983 个工位 ≈26 GB** ＋ pytest 临时目录 **60 GB / 千万级文件**，
⛔ **C 盘被撑爆。速率 ≈63 个/小时。**

**修法**：查返回码 → 喊出来 + 记数（`worktree.LEAKS`），⛔ 仍不抛异常。
**红检**：两处各撤回原样 → **各自只红自己那几条**（证明是两条独立的路）。
**绿检**：删得掉时不许出声；真实规模跑一次全量 → **泄漏 0 个、0 次瞎喊**。

### ⭐⭐ latch T5（本轮最大产出，可推导而非经验）

> **任何可能失败的操作，若其失败不改变任何可观测输出，则该失败必然被累积到灾难规模。**
> ⛔ 与失败率无关，只与**速率是否 > 0** 有关。

### ⭐ 静默失败扫描：⛔ 单位必须是**函数作用域**

| 扫描单位 | 命中 | 误报 |
|---|---|---|
| 行数窗口（8 行）| 10 处 | ⛔ 至少 1 处（`dispatch.py:183`，下面就取了 `.stdout`，只是超窗）|
| ⭐ **函数作用域** | **0 处真静默** + 3 处「交回调用方」 | ⭐ **0** |

⇒ **换个扫描单位，误报率 10% → 0。**
那 3 处（`constitution.py::git` · `prune.py::_run` · `worktree.py::git`）逐个查过调用点，
⭐ 都紧接着取 `.stdout` 或查 `.returncode` ⇒ **0 处需修，0 处需 waiver。**
